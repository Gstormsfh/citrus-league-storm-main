# Phase 4.5 — Project Plan & Roadmap

Schedule, dependencies, and risk callouts for Phase 4.5 implementation through v1 production launch (NHL fantasy season opener, October 2026).

**Authority:** Garrett Storms, founder/CEO. Proposed 2026-04-29.
**Status:** Ratified 2026-04-30. Subject to revision per the same supersession-note convention used for ADR-001 if any date materially changes.

**Companion docs:**
- [`PHASE_4_5_ARCHITECTURE.md`](./PHASE_4_5_ARCHITECTURE.md) — canonical architecture (Zach's design).
- [`PHASE_4_5_ARCHITECTURE_ANSWERS.md`](./PHASE_4_5_ARCHITECTURE_ANSWERS.md) — Citrus-specific answers binding the architecture to product reality.
- [`PHASE_4_5_PLAN.md`](./PHASE_4_5_PLAN.md) — chunk-by-chunk implementation plan for chunks 11g.0–11g.10.
- [`adr/ADR-001-persistent-node-draft-engine.md`](./adr/ADR-001-persistent-node-draft-engine.md) — original architectural decision record with supersession history.

---

## Anchor Dates

- **Today:** April 29, 2026
- **Web Summit Vancouver:** May 11, 2026 *(playoff pool product — separate from Phase 4.5)*
- **NHL fantasy season opener (v1 launch target):** ~October 7, 2026 *(confirm with Zach against actual NHL schedule)*
- **6-week pre-launch load test gate:** ~August 26, 2026
- **Available work weeks for Phase 4.5:** ~21 weeks (May 12 – October 7)

---

## Roadmap

Backward-planned from the launch date. Date windows are target ranges, not bullseye commits — the goal is "done by end of window."

### Phase A: Web Summit & recovery (May 1 – May 18)

Web Summit Vancouver is May 11. No Phase 4.5 implementation work scheduled this window. Playoff pool product is the focus.

### Phase B: Snake/linear engine implementation (May 19 – July 31)

Chunks 11g.2 through 11g.10 from [`PHASE_4_5_PLAN.md`](./PHASE_4_5_PLAN.md). ~10 weeks of dev for the locked plan.

**Dependencies:**
- Chunk 11g.2 reshape brief landed (deploy target swap, GCE platform spike).
- Zach onboard to validate GCE machine type, region, container runtime decisions.

**Acceptance gate to Phase C:** chunks 11g.2–11g.10 complete, snake/linear drafts run end-to-end against the "done" definition in the architecture doc (1,000 simulated clients, process restart, network partition, no lost picks).

### Phase C: Parallel workstreams (June – August)

Three workstreams running in parallel with Phase B implementation. Each has its own owner, dependencies, and acceptance gate.

#### C1: Auction state machine (June 1 – August 9)

Citrus committed to all three draft formats (snake, linear, auction) for v1. Auction has fundamentally different state semantics than snake/linear and requires a separate state machine design.

| Sub-phase | Window | Output |
|---|---|---|
| C1.1 — Auction ADR + design doc | June 1 – June 21 | New ADR (e.g. `ADR-004-auction-state-machine.md`) + design doc covering: nomination order, bid timers (reset-on-bid semantics), budget tracking, simultaneous multi-bidder handling, integration with locked Phase 4.5 primitives (event sourcing, single-writer queue, discovery, durability). |
| C1.2 — Auction implementation | July 1 – August 9 | Auction `LobbyManager` (or format-aware `LobbyManager`), tests, reaches "done" definition equivalent to snake/linear. |

**Risk:** auction design is the rate-limiting step for the entire C1 stream. If C1.1 slips into July, C1.2 slips into August, which compresses load testing margin in Phase D. **Hard sub-deadline: auction design ratified by July 1.**

#### C2: Co-manager schema migration (June 15 – July 5)

Co-manager support requires net-new schema (likely `team_managers` join table with `role` column, or `head_manager_id` + `co_manager_ids[]` on `team`) and authorization-check refactor across the entire app — not just the draft engine. **Scheduled intentionally early** to give the migration ≥6 weeks of soak time before load testing exercises it.

| Sub-phase | Window | Output |
|---|---|---|
| C2.1 — Schema design + migration script | June 15 – June 21 | Migration script, RLS policies, rollback path. |
| C2.2 — Authorization refactor | June 22 – July 5 | All pick-submission paths updated; `team_managers` reads wired. |

**Risk:** this touches whole-app authorization. **Do not** schedule co-manager migration in the final month before launch. If C2 slips into August, defer co-manager support to v1.1 rather than ship a rushed authorization migration.

#### C3: Push notification infrastructure (July 6 – August 2)

FCM (Firebase Cloud Messaging) bring-up for web + mobile. Notification system itself stays the existing one; push is a delivery channel.

| Sub-phase | Window | Output |
|---|---|---|
| C3.1 — FCM design + setup | July 6 – July 19 | FCM project configured, web push tokens flowing, opt-in UX. |
| C3.2 — Mobile push (iOS + Android) | July 20 – August 2 | Native push delivery on both platforms, end-to-end test. |

**Fallback:** if push slips, in-app notifications still work. Push is enhancement, not gate. Lower priority than C1 and C2.

### Phase D: Load test gate (August 24 – September 4)

**Hard date.** Chunk 11g.11 (new — to be added to [`PHASE_4_5_PLAN.md`](./PHASE_4_5_PLAN.md) as part of this plan). Simulated peak load against a production-equivalent GCE deployment.

**Test parameters (per `PHASE_4_5_ARCHITECTURE_ANSWERS.md` Q4.1):**
- 1,000 concurrent WebSocket connections (lower bound of the 2,400–4,800 production estimate; scale up if Day 1 single-VM holds at 1,000).
- 300 concurrent active drafts (upper bound of the 150–300 production estimate).
- Mixed format: 70% snake, 20% linear, 10% auction.
- Sustained 4-hour test window (matches longest realistic draft duration).
- Mid-test simulated process restart and network partition (per architecture doc "done" definition).

**Acceptance gate:**
- Zero lost picks across the test window (P0 SLO bar).
- p95 manual pick latency ≤ 300ms, p99 ≤ 500ms (per existing performance mandate in `CLAUDE.md`).
- p95 reconnect recovery ≤ 2s.
- No instance saturation (CPU < 70%, memory < 70%).

**If saturation observed:** trigger Phase E (Stage 2 implementation). Otherwise, skip directly to Phase F.

### Phase E: Conditional Stage 2 implementation (September 7 – September 18)

Conditional on Phase D outcome. HAProxy in-VM + N uWS workers per vCPU, per the architecture doc's Stage 2 design. Application code unchanged (per the locked principles); supervisor + HAProxy config + container entrypoint updates only.

**Re-test gate:** repeat Phase D load test against Stage 2 deployment. Same acceptance criteria.

### Phase F: Pre-launch hardening (September 21 – October 4)

Final 2-week window before season opener.

| Work item | Window | Output |
|---|---|---|
| Sentry / client-side error tracking | September 21 – September 25 | Sentry wired into web client; error grouping configured; alert routing to on-call. |
| Alert routing + on-call setup | September 21 – September 28 | GCP alerts → SMS/email or Pagerduty; runbooks for top-5 expected incidents. |
| GCP environment tags | September 28 – September 29 | Hygiene fix on both projects. Quick. |
| Final smoke tests + draft-night dry runs | September 29 – October 4 | Internal dry-run drafts at low volume, end-to-end. |

### Phase G: Launch (October 7, 2026)

Season opener weekend. Deploy freeze in effect (per `PHASE_4_5_ARCHITECTURE_ANSWERS.md` Q4.2). On-call active. No code changes during opener weekend absent a P0 incident.

---

## Critical Path & Dependencies

```
Phase B (snake/linear, May 19 – Jul 31)
├─→ Phase D (load test gate, Aug 24 – Sep 4) ──┐
│                                                │
Phase C1 (auction, Jun 1 – Aug 9) ─────────────────→ │
│                                                │
Phase C2 (co-manager, Jun 15 – Jul 5) ─────────────→ │
│                                                │
Phase C3 (push, Jul 6 – Aug 2) [optional] ─────────→ │
                                                  ↓
                                  Phase E (conditional, Sep 7–18)
                                                  ↓
                                  Phase F (hardening, Sep 21 – Oct 4)
                                                  ↓
                                  Phase G (launch, Oct 7)
```

**Critical path:** Phase B → C1 → D → F → G. Auction is the rate-limiter.

---

## Risk Register

Risks ranked by impact-to-launch.

### R1 — Auction design slips past July 1 *(High impact, medium probability)*

If auction ADR isn't ratified by July 1, auction implementation compresses August, which compresses load testing margin.

**Mitigation:** auction ADR is a tracked work item with a dedicated window (June 1 – June 21). Garrett owns. Zach reviews. Re-evaluate weekly during June. **Tripwire:** if no ADR draft by June 14, escalate to Zach for direct involvement.

### R2 — Solo founder bandwidth *(High impact, medium probability)*

The plan assumes Garrett full-time on Phase 4.5 May 19 – October 7. Day job, fundraising, content work, advisor management, and Web Summit follow-up will compete for attention.

**Mitigation:** Zach onboarding earlier is the biggest unlock. If Zach is full-time by mid-June, Phase C workstreams can be split.

### R3 — Load test reveals Stage 2 is required *(Medium impact, medium probability)*

Phase D may surface saturation requiring Phase E (Stage 2 implementation). The plan budgets 2 weeks for this.

**Mitigation:** Phase E budget is real. If Stage 2 needs more than 2 weeks, Phase F hardening compresses.

### R4 — Co-manager migration too risky to ship *(Medium impact, low probability)*

Co-manager schema touches whole-app authorization. If C2 surfaces unexpected complexity (RLS policy edge cases, breaking changes to existing endpoints), it may not be safe to ship for v1.

**Mitigation:** scheduled early (June 15) for ≥6 weeks of soak time. **Tripwire:** if C2 isn't passing all existing test suites by July 5, defer co-manager support to v1.1.

### R5 — Solo on-call during opener weekend *(High impact if it happens, low probability)*

Garrett alone responding from his phone at 11pm during opener weekend. Fine for 50 leagues; risky for 300 concurrent drafts and 4,800 connections.

**Mitigation:** if Zach is available, he's on rotation. If not, identify at least one trusted contact who can be raised for a P0. Heavy observability investment in Phase F is the best preventative.

### R6 — Push notification slips *(Low impact, medium probability)*

Push is enhancement, not gate. In-app notifications work without it.

**Mitigation:** explicit fallback — defer push to v1.1 if C3 surfaces problems.

---

## Decision Log

Material schedule decisions captured here for traceability.

| Date | Decision | Rationale |
|---|---|---|
| 2026-04-29 | Plan proposed by Garrett, awaiting Zach ratification | Anchored against October 7 launch and 6-week pre-launch load test gate. |
| 2026-04-29 | Co-manager migration scheduled early (Phase C2, June 15) | Whole-app authorization touches require ≥6 weeks of soak time before load testing. |
| 2026-04-29 | Auction ADR sub-deadline July 1 | Auction is critical-path; later ratification compresses load test margin. |
| 2026-04-29 | Push notification flagged as deferrable to v1.1 | Push is enhancement; in-app fallback works. |
| 2026-04-29 | Load test (chunk 11g.11) added as hard gate | Path B from `PHASE_4_5_ARCHITECTURE_ANSWERS.md` Q4.1 rests on this test. |
| 2026-04-30 | Chunk 11g.2.0 spike executed by Garrett, all findings recorded in `PHASE_4_5_GCE_PLATFORM_NOTES.md` | `e2-medium` right-sized at 15.5 MiB idle (saving ~$150/mo vs original `e2-standard-4` plan); regional decision deferred pending multi-city benchmark; `--container-image` flag deprecated by GCP (chunk 11g.2 deploy pattern adjusted); ~$97/mo Cloud Workstations drain killed during pre-spike audit. |
| 2026-04-30 | Zach ratified all five decision points (path B, three formats, co-manager v1, provisional Montreal, `e2-medium` VM tier) | Async Slack response. Architecture doc, project plan, and platform notes now operational. |
| 2026-04-30 | ADR-002 (auction state machine) drafted | Five architectural decisions ratified by Garrett. Path A event-sourcing pending Zach confirm. Auction load profile flagged as net-new chunk 11g.11 acceptance criterion. |
| 2026-04-30 | ADR-003 (co-manager authorization model) drafted | Five architectural decisions ratified by Garrett. `team_managers` join table, centralized authorization helper, exclusivity enforced. 4-phase migration plan: centralize first, then schema, then features, then cleanup. |
| 2026-04-30 | Zach ratified ADR-002 and ADR-003 in-person meeting | All architectural decisions in both ADRs locked. Auction state machine and co-manager authorization model are operational design references for chunks 11g.6 implementation. |
| 2026-05-04 | Chunk 11g.2 functionally complete | Server scaffold + JWT auth + Docker parity + production Dockerfile + GCE staging deploy + Cloud Logging integration. End-to-end smoke tests passed against real Montreal VM at 34.19.223.135. Staging resources torn down post-validation; service account, registry, and image preserved for chunk 11g.4 re-provisioning. |
| 2026-05-04 | World-class observability foundation scheduled under chunk 11g.7 | Structured JSON logging via `@citrus/shared` `logger` refactor, Cloud Monitoring dashboards-as-code, log-based metrics, alert policies. Deferred from chunk 11g.2 step 7 because dashboards/alerts should be designed against the real `LobbyManager` + event-sourced state machine (chunks 11g.4–11g.6), not the chunk 11g.2 hello-world echo scaffold. Native Google tooling (gcloud/Config Connector), no Terraform — per Zach's "all in one uniform place" principle. |
| 2026-05-05 | **Chunk 11g.5/11g.6 blocker logged: persistent engine writes to `submit_pick_v2` will fail `auth.uid()` check under admin-client mode.** Resolution requires an ADR. Recommend `submit_pick_v2` accept `service_role + actor.kind='user'` when the engine has independently verified the actor (Option 3 in chunk 11g.4 step 4 recon). Surfaced during chunk 11g.4 step 4 (LobbyRegistry + connection management) implementation; tracked here so chunks 11g.5 (resync protocol) and 11g.6 (state machine + auction handlers) don't land real picks before the auth path is settled. | The migration `20260425140000_draft_engine_v2_rpcs.sql` line 838 enforces `auth.uid() = teams.owner_id` when `actor.kind = 'user'`. The persistent draft engine constructs ONE `DraftServiceV2` at startup backed by the admin Supabase client (`getSupabaseAdmin()`), shared across all `LobbyManager` instances per chunk 11g.4 step 4. Under admin client `auth.uid()` returns null → `unauthorized`. Step 4 itself is unblocked because the LobbyManager unit tests mock `submitPick`. JSDoc flags placed in `server/src/draft/LobbyRegistry.ts` and `server/src/draft/index.ts`. |
| 2026-05-05 | Heartbeat / keepalive protocol scheduled under chunk 11g.7 | Server-driven WS ping/pong every 30s with 10s pong timeout, plus uWS `idleTimeout` configuration. Critical for mobile clients (carrier networks silently drop idle connections after 30-60s, leaving the client and server with mismatched views of liveness). Deferred from chunk 11g.4 step 5 because it's an operations-layer concern that pairs naturally with monitoring/alerting (idle-disconnect rate is a key observability metric) — both land together in chunk 11g.7. |
| 2026-05-05 | Per-event-type rate limiting + prioritization scheduled under chunk 11g.11 | Beyond brute disconnect-on-overflow (chunk 11g.4 step 5's 1 MiB per-connection threshold), world-class platforms prioritize state-changing events (picks, bids) over chat/presence events and gracefully degrade (drop chat first, preserve picks). Deferred to chunk 11g.11 (load test) where real bandwidth profiles per format inform the prioritization scheme. Step 5 ships the simple-but-correct floor; the smart degradation lands once we have real numbers to design against. |
| 2026-05-05 | Backpressure check is O(connections) per broadcast — accept for now, revisit at chunk 11g.11 | Chunk 11g.4 step 5's `LobbyManager.sweepBackpressure` iterates every connection on every broadcast and calls `ws.getBufferedAmount()`. Trivial cost for 12-team drafts (~12 connections per lobby × 252 picks ≈ 3,024 sweep iterations across a full draft). Becomes a concern only if a single lobby grows to 1000+ concurrent connections, which is not a real scenario for fantasy draft formats. Tracked alongside the chunk 11g.11 load test so real concurrency numbers inform whether to switch to lazy backpressure (only sweep recently-broadcast-to ws) or an async sweep timer. |
| 2026-05-05 | Five `draft_events` event types are durably emitted today but skipped-with-log by chunk 11g.4 step 6b's bootstrap — chunk 11g.4 step 6c (timers) and a future chunk own the real handlers | Step 6b's bootstrap dispatches all 11 currently-shipping `event_type` values per the `draft_events` CHECK enum. Of those, 6 are handled with state-machine semantics (`pick`, `pick_undone`, `commissioner_override`, `draft_completed`, `draft_cancelled`, plus the bootstrap's own seq-contiguity check). The remaining 5 are skipped-with-debug-log: `draft_paused` / `draft_resumed` / `draft_extended` (timer/deadline state — becomes 6c-relevant when timers land in the engine), `autopick_failed` (diagnostic), and `generation_bumped` (internal versioning). Skip-with-log is correct for state-machine integrity in 6b — none of these affect `picksMade` or the ring buffer. The pause/resume/extend trio will gain real handlers in chunk 11g.4 step 6c alongside the per-lobby `setTimeout` deadline timers per the architecture doc. ADR-002 §4.1 auction event types (`auction_*`) are not in the shipping CHECK constraint and would hit the bootstrap's `default:` branch as forward-compat warn-and-skip; chunk 11g.6 ALTERs the CHECK and ships the auction state machine that emits and consumes them. |
| 2026-05-05 | Autopick player selection ships projections-only in chunk 11g.4 step 6c. User-defined per-team draft queue is a future enhancement requiring a separate schema migration + UI. | Chunk 11g.4 step 6c's recon revealed no `team_draft_queues` table or `teams.draft_queue` column exists — autopick queue infrastructure is greenfield. Today's `autopickStrategy.ts` ships with a single-strategy chain (`[projectionsStrategy]`) that picks the highest `total_projected_points` player from `player_ros_projections` filtered against already-drafted (via `draft_picks_v2`). The chain-of-strategies architecture is forward-compatible: when `team_draft_queues` lands (separate schema migration + UI), adding `queueStrategy` to the front of the chain is a single-line array push at `DEFAULT_STRATEGIES`. No refactor of the autopick entry point or the LobbyManager's `handleAutopickTimeout`. Tracked as a v1.1 enhancement; v1 ships with the projections-only fallback that Yahoo / ESPN / Sleeper all default to when the user has not set a queue. |
| 2026-05-05 | **Chunk 11g.4 functionally complete.** | Step 6c ships the final sub-step of chunk 11g.4: pick deadline tracking + autopick on timeout, completing the live-draft engine for snake/linear formats. The LobbyManager now handles the full lifecycle: state machine (6a), event-log bootstrap (6b), per-lobby `setTimeout` deadlines, autopick fire on expiry via the chain-of-strategies, pause/resume bookkeeping mirroring the `draft_resume` RPC's fresh-full-clock behavior, and graceful shutdown that cancels pending timers. Auction format remains stubbed for chunk 11g.6 (ADR-002 auction state machine + anti-snipe). Next chunk: 11g.5 (resync protocol — server primitive shipped in step 5; chunk 11g.5 is the client-side reconnect state machine consuming it). After 11g.5 ships, chunk 11g.6 begins. |
| 2026-05-05 | Wire-protocol types moved to `packages/shared/src/types/draftWire.ts` for type-safe cross-boundary use | Chunk 11g.5a's recon revealed the shared types boundary (`packages/shared/src/types/`) was server-only — wire types lived at `server/src/draft/types.ts`. Moved the cross-boundary subset (`WIRE_PROTOCOL_VERSION`, `DraftServerMessage`, `DraftClientMessage`, `BufferedDraftEvent`, `DraftSnapshot`, `DraftStateSnapshot`, `LobbyStatus`, `DraftFormat`) to `packages/shared/src/types/draftWire.ts`. Server-only types (`DraftAction`, `DraftActionResult`, `DraftSocketUserData`, `DraftOrderSlot`, `GetEventsSinceSeqResult`, `TeamAuthorizationResult`, helper functions) stay in `server/src/draft/types.ts`. Server re-exports the moved types for backwards compat (zero churn on existing server imports). The engine's `DraftStatus` was renamed to `LobbyStatus` cross-boundary to avoid colliding with the existing product-level `DraftStatus` in `league.ts`; server-side aliases preserve `DraftStatus` as the engine-facing name. Eliminates type duplication and enforces the contract — chunk 11g.5a's client imports `@citrus/shared` types directly. |
| 2026-05-05 | HTTP snapshot endpoint for chunk-11g.4 `DraftSnapshot` shape deferred — chunk 11g.5a runner stubs the fetch | The chunk-11g.4 in-memory `DraftSnapshot` (with `stateSnapshot` + `recentEvents` from the engine's ring buffer) has no HTTP endpoint yet. The v1 endpoint at `server/src/routes/draft.ts:347` returns a different shape. Chunk 11g.5a's runner has a `defaultFetchSnapshot` placeholder that throws `snapshot_fetch_not_implemented`; the state machine routes through `snapshot_fetch_failed` → `reconnecting` cleanly. The snapshot-via-HTTP path matters when the server's ring buffer evicts past a client's `lastSeenSeq` cursor — at chunk-11g.4 step 5's 200-event capacity, this is a long-disconnect scenario (~30+ minute outage during a draft). Tracked as a chunk 11g.7 followup (alongside heartbeat / observability). When the endpoint ships, replacing `defaultFetchSnapshot` is a one-line change with no state-machine modifications. |
| 2026-05-05 | Server-side ping/keepalive remains chunk 11g.7; chunk 11g.5a ships forward-compat client pong handler | Chunk 11g.5a's runner registers the WebSocket message handler with full message-type dispatch via `reduce`, so future server-emitted ping messages would be handled by adding a case to the `ws_message` switch. The current implementation has no ping handler — the server doesn't emit pings yet. The `visibilitychange` event handler is forward-compat scaffolding for chunk 11g.7's liveness detection: when chunk 11g.7 ships ping/pong, the visibility-change-based manual-resync fallback that 5a's recon described becomes unnecessary (server pings detect dead connections faster than the visibility-change heuristic). |
| 2026-05-05 | Cutover strategy for chunk-11g.5b UX integration: parallel `DraftRoomV2.tsx` at `/draft-v2/:leagueId/:draftId?` route | Chunk 11g.5b's recon revealed v1 `DraftRoom.tsx` is a 4805-line monolith (player pool, draft logic, Supabase Broadcast subscription, auction flow, queue management, snapshot rendering, history tabs all in one component). In-place migration to consume the chunk-11g.4 persistent engine alongside this much v1 plumbing is high risk. Parallel `DraftRoomV2.tsx` is the only safe answer — fresh, lean page that consumes 5a's `DraftClientRunner` for snake/linear formats. v1 `DraftRoom.tsx` continues to serve `/draft` and `/draft-room` for the cutover-safety window. League-by-league rollout via the v2 URL (commissioner clicks "Start v2 draft" → user lands at `/draft-v2/...`). Chunk 11g.9 (legacy autopick infrastructure removal) is the natural cutover-completion moment. |
| 2026-05-05 | Optimistic action 4-path reconciliation pattern (chunk 11g.5b) | Client-side optimistic pick submissions track via `pendingActions: Map<correlationId, PendingAction>` in `draftClientStore`. Four reconciliation paths handle the resolution: (1) **broadcast match** — server's `event` message arrives with the same correlationId; remove from pending. (2) **resync match** — after reconnect, the server's resync events include our pick (committed before disconnect); remove from pending. (3) **rejection** — submit_pick RPC returned `{ ok: false, reason }`; transition to `rolled_back`, UI flashes-out, toast surfaces the reason. (4) **network-failure-with-resync** — submit_pick promise rejected AND subsequent resync events do NOT contain our correlationId; treat as rejection. Pure functions in `apps/web/src/lib/draftClient/optimistic.ts` consumed by the store's reducers. |
| 2026-05-05 | Presence toast throttle window 5s (chunk 11g.5b) | First presence event in a 5s window fires a toast; subsequent events within the window are silently dropped. Rationale: 5s window absorbs the draft-start spike when 12 users connect simultaneously and would otherwise produce 12 toasts in rapid succession. Window is small enough that mid-draft join/leave events still feel responsive (a single user joining at minute 47 produces a toast within ~5s of their actual connection). Aggregate-batching ("3 users joined") is a future polish if user feedback warrants. Implementation in `apps/web/src/lib/draftClient/toasts.ts`. |
| 2026-05-05 | Chunk 11g.5b ships minimum v2 surface — full visual integration of reusable v1 components is iterative post-5b | 5b ships: `DraftRoomV2.tsx` page + `ConnectionBanner.tsx` + `PendingPickIndicator.tsx` + `PresenceDot.tsx` + `draftClientStore` + `optimistic.ts` + `toasts.ts`. The 8 v1 components that 5b's recon flagged as reusable (DraftBoard, PlayerPool, DraftHistory, DraftTimer, RosterDepthChart, TeamRosters, DraftQueue, DraftSnapshotView) are NOT integrated into the v2 page in 5b — they're presentation primitives that need data adapters from the new state-machine shape. Iterative work in follow-up PRs. 5b's barebones state view (current pick, on-clock team, presence count, recent events list) proves the protocol-to-UX wiring works end-to-end; the visual richness lands incrementally. |
| 2026-05-05 | ADR-004 (persistent-engine authorization model) drafted | Option 3 selected — engine becomes trusted executor; `submit_pick_v2` accepts `service_role + actor.kind='user'` when the engine has independently verified the actor at the application layer. Decision rationale leads with precedent: the existing `draft_pause` / `draft_resume` / `draft_extend` RPCs in the same migration (`20260425140000_draft_engine_v2_rpcs.sql` lines 1000-1007, 1119-1126, 1263-1270) already accept `service_role` as a trusted-application-layer bypass for commissioner-kind operations. ADR-004 extends this pattern from commissioner-kind to user-kind. Pending Zach ratification. Unblocks chunks 11g.4 step 6, 11g.5, 11g.6. Migration shape is ~5 lines of SQL change to the user-kind branch of `submit_pick_v2`; engine-side team-authorization verification (the trust contract) is chunk 11g.6 work and is independent of ADR-003's `team_authorized()` migration sequence. |
| 2026-05-06 | ADR-004 ratified by Zach | Option 3 (engine-as-trusted-executor) confirmed verbally. Migration to modify submit_pick_v2's user-kind branch + engine-side team-authorization verification both unblocked for chunk 11g.6 implementation. |
| 2026-05-06 | Chunk 11g.6 sub-step 6a co-design approach: state-machine foundation + nominate/place_bid handlers + 3 atomic RPCs in a single chunk | Sub-step 6a's brief specified a tight co-design rather than incremental scaffolding — wire types, RPCs, LobbyManager state, action handlers, bootstrap event handlers, and the format-aware clock dispatch all land together so the auction format becomes end-to-end functional in one commit. Anti-snipe extension semantics (ADR-002 §3.3) are the only deferral within auction-active behavior, and they live in 6b. Auction pause/resume/commissioner override and tiered bid-increments are 6c work. Rationale: a half-shipped auction state machine is harder to test and debug than a complete one; the atomic-RPC contracts already enforce the consistency boundary at the durability layer, so the in-engine integration is the remaining piece. |
| 2026-05-06 | Schema correction during 6a recon: brief proposed a new `teams.budget_remaining` column; existing `auction_budgets` table is canonical | The brief's recon section proposed adding `teams.budget_remaining INTEGER` for engine state hydration. Recon found `auction_budgets` already exists from the v1 implementation (per ADR-002 §5.1 "v1 schema stays") with `remaining_budget NUMERIC` and `players_won INTEGER` columns. The migration was simplified accordingly: only the `draft_events.event_type` CHECK enum + `auction_nominations.nomination_event_id` back-link + partial index get added — no `teams` table change. `auction_nominations.player_id` is TEXT (vs snake/linear's INT `draft_picks_v2.player_id`); wire types use `string` to preserve the format. Engine-side `LobbyManager.teamBudgets` mirrors `auction_budgets.remaining_budget` cents-as-NUMERIC. |
| 2026-05-06 | Sub-step 6a deferred-work list (binding for 6b/6c) | **Deferred to 6b**: anti-snipe extension semantics (ADR-002 §3.3 — bid in last N seconds extends bid window). 6a's `auction_bid_placed` event carries `clockDeadline` as forward-compat scaffolding; the timer extension itself lands in 6b. **Deferred to 6c**: auction pause/resume + commissioner override (state machine has placeholder switch arms with debug log), nomination-window/bid-window split (ADR-002 §3.4 — separate timers for "you must nominate" vs "you must bid"), auto-nominate algorithm for nominators who fail to act inside the nomination window, ADR-002 §4.3 tiered bid-increments (6a uses flat `auctionMinBid`). 6a uses the existing `LeagueFormatSettings.auctionNominationTime` field as a single value covering both nomination + bid windows. |
| 2026-05-06 | World-class atomicity contract for the 3 new auction RPCs (nominate_player_v2, place_bid_v2, close_nomination_v2) | Each RPC documents an explicit BEGIN…COMMIT atomicity block in its SQL header comment, listing every row that participates: `nominate_player_v2` (5 writes — auction_nominations + auction_bids + draft_events + leagues counter + nomination_event_id back-link), `place_bid_v2` (4 writes — auction_bids + auction_nominations.current_high_bid update + draft_events + leagues counter), `close_nomination_v2` (5 writes — auction_nominations status + auction_budgets decrement + draft_picks insert + draft_events + leagues counter; no_sale variant skips the budget/picks writes). The PostgreSQL function bodies are SECURITY DEFINER which gives them transactional context implicitly; documenting the atomicity contract in the SQL comments + ADR-002 §3 prevents a future engineer from relaxing the boundary without an ADR supersession. Engine-side budget reserve calculation (`(slotsRemaining - 1) × auctionMinBid`) mirrors v1 `AuctionService.placeBid`. |
| 2026-05-06 | **Chunk 11g.6 sub-step 6a complete.** | Auction state machine foundation lands in a single commit on `phase-4-5-implementation`: 3 SECURITY DEFINER atomic RPCs (`nominate_player_v2`, `place_bid_v2`, `close_nomination_v2`) + `draft_events` CHECK enum extended with 9 auction event types + `auction_nominations.nomination_event_id` back-link + partial index for auction event queries. Engine: `LobbyManager` gains 7 auction state fields (`nominationOrder`, `draftRounds`, `auctionMinBid`, `bidWindowMs`, `nominationsCompleted`, `currentNomination`, `teamBudgets`/`teamPlayersWon` maps); `processNominate` + `processPlaceBid` action handlers; `handleClockExpired` format-aware dispatcher (rename of `handleAutopickTimeout`) routing to `handleNominationTimeout` for auction lobbies; 4 bootstrap event handlers (`applyAuctionNominationStartedEvent`, `applyAuctionBidPlacedEvent`, `applyAuctionNominationClosedEvent`, `applyAuctionNominationExpiredEvent`); `getCurrentState` + `getSnapshot` populate `auctionState` for auction lobbies. Wire types in `packages/shared/src/types/draftWire.ts` extended with full `BufferedDraftEvent` shapes for the 4 auction event variants + `AuctionStateSnapshot` interface. `DraftServiceV2` gains `nominatePlayer`, `placeBid`, `closeNomination` methods mirroring `submitPick`. 712 server tests pass (+14 net: +15 new auction tests, -1 obsolete forward-compat-skips test). Deferred items: anti-snipe → 6b; auction pause/resume + auto-nominate + tiered increments → 6c. Next sub-step: 6b (anti-snipe extension semantics + commissioner override). |
| 2026-05-07 | Anti-snipe configuration field names + defaults (chunk 11g.6 sub-step 6b) | Two new optional fields on `LeagueFormatSettings` per ADR-002 §3.3 / §4.3: `auctionAntiSnipeThresholdSeconds` (default 30; range 0-120; **0 disables anti-snipe entirely**) and `auctionAntiSnipeExtensionSeconds` (default 30; range 5-120). camelCase matches the existing `auctionNominationTime`/`auctionMinBid` convention. `mergeWithDefaults` coalesces `??`-style so a stored `0` is honored as "disabled" rather than falling back to the default. Engine reads from settings via `lookupLobbyConfig`; threads through `LobbyConfig` → `LobbyManagerOptions` → `LobbyManager` → every `place_bid_v2` RPC call. Snake/linear lobbies set both to 0 (unused). The brief proposed `auctionAntiSnipeSeconds` (singular) but the dual-field naming is clearer: threshold is "when does extension fire" and extension is "how long is the new window." |
| 2026-05-07 | Anti-snipe boundary: strict-less-than (`seconds_remaining < threshold`) | Per ADR-002 §4.4 example: "Bid placed at second 29 of a 30-second window with anti-snipe threshold 30s: window extends to second 59." With 1s remaining and threshold=30 → extends. At exactly 30s remaining (boundary) → does NOT extend. Codified in `place_bid_v2` as `IF p_anti_snipe_threshold_seconds > 0 AND v_seconds_remaining < p_anti_snipe_threshold_seconds`. Strict-less-than is the ONLY semantic that makes "threshold = 0" mean "disabled" naturally — at threshold 0, no `seconds_remaining` value can be `< 0`, so extension never fires. |
| 2026-05-07 | **Bootstrap event-replay is canonical for in-memory state machine; `lookupLobbyConfig`'s row reads are informational/diagnostic only.** | The chunk 11g.6 sub-step 6b recon surfaced this as a load-bearing architectural principle when it found that 6a's bootstrap derives `currentNomination.expiresAt` from the `auction_nomination_started` event payload rather than from `auction_nominations.expires_at` (which already reflects extensions). This means **every state-affecting auction event must have an apply-during-replay handler; log-and-skip is incorrect for any event whose semantics change in-memory state.** Override of the brief's recommendation that `auction_bid_extends_timer` should log-and-skip during replay — log-and-skip would leave `currentNomination.expiresAt` stale by every extension count, and `init()`'s post-replay timer schedule would fire too early. The 6b implementation adds `applyAuctionBidExtendsTimerEvent` that mutates `currentNomination.expiresAt` to the event's `new_expires_at`. Applies retroactively to all of chunk 11g.6: every state-affecting auction event handler must apply during replay. The 6c sub-step (pause/resume + commissioner override) MUST follow this pattern — pause/resume events change `pauseState`, commissioner override events can change rotation pointers and the active nomination, all must apply during replay rather than relying on row reads. Aligns with PHASE_4_5_ARCHITECTURE.md Principle 3 (event-sourcing to Postgres as the system of record). |
| 2026-05-07 | Anti-snipe extension is **atomic with the bid write** in `place_bid_v2` — same SECURITY DEFINER transaction, 4-or-7 writes depending on whether extension fires | Per ADR-002 §3.3 / §4.4: the threshold check (`seconds_remaining < threshold`) and the resulting `auction_nominations.expires_at` update + `auction_bid_extends_timer` event INSERT all live inside the same RPC transaction as the bid INSERT and `current_high_bid` UPDATE. A bid that committed without its corresponding extension would leave the system in an inconsistent state where another concurrent bid could win against the original (un-extended) deadline despite the spec saying the deadline moved. Two `draft_events` rows are written when extension fires: the parent `auction_bid_placed` (seq N) gets the original `idempotency_key`; the derived `auction_bid_extends_timer` (seq N+1) gets `md5('extends:' \|\| parent_key::text)::uuid` so the unique constraint distinguishes the two. Same `actor` JSONB and same `correlation_id` on both — the extension is a consequence of the user's bid, not a separate engine action; audit trail reads "user X placed bid Y; system extended timer Z" with shared correlation. Mirrors chunk 11g.4 step 6c's autopick-actor-flow-to-event pattern. Same `payload_hash` on both rows because the bid's hash uniquely identifies the operation that triggered the extension. Retries of the same parent bid exit early at the idempotency check (no re-extension). |
| 2026-05-07 | **Chunk 11g.6 sub-step 6b complete.** | Anti-snipe timer extension lands as a single commit on `phase-4-5-implementation`. New migration `20260507000000_auction_anti_snipe.sql` `CREATE OR REPLACE`s `place_bid_v2` with two new int parameters (`p_anti_snipe_threshold_seconds`, `p_anti_snipe_extension_seconds`); atomic 4-or-7-write block depending on whether extension fires; returns `was_extended: boolean` + `extends_event_seq: bigint?` plus the (possibly post-extension) `clock_deadline`. The 6a CHECK enum already admits `auction_bid_extends_timer` (forward-compat); 6b does not ALTER the CHECK. Wire: full `BufferedDraftEvent` variant for `auction_bid_extends_timer` with 9 fields including `triggeringBidId` / `triggeringBidderTeamId` / `triggeringBidAmount` for client-side correlation rendering. `LeagueFormatSettings`: 2 new fields + `mergeWithDefaults` coverage. `DraftServiceV2.PlaceBidParams` + `PlaceBidResult` extended; `LobbyManager` gains `antiSnipeThresholdSeconds` + `antiSnipeExtensionSeconds` config from constructor; `processPlaceBid` post-success path handles `was_extended` (cancel timer + reschedule + ring buffer + broadcast extension event with same `correlationId` as parent bid); `applyAuctionBidExtendsTimerEvent` bootstrap handler mutates `currentNomination.expiresAt` per the canonical-replay principle (entry above). 722 server tests pass (+10 new 6b tests: non-final-window no-extension, final-window extension, cascade depth 3, late-bid-rejected, anti-snipe-disabled threshold=0, bootstrap apply, bootstrap integrity throws on no-active-nomination + nomination-mismatch, wire round-trip, end-to-end multi-bid-with-extension flow). Deferred items: auction pause/resume + commissioner override + auto-nominate + tiered bid increments → 6c. Next sub-step: 6c. |
| 2026-05-07 | Auction resume restores captured remaining time, NOT a fresh full window — divergent from snake/linear (chunk 11g.6 sub-step 6c1) | Per ADR-002 §4.4 (locked) auction `auction_resumed` "resumes from where it stopped, not reset." This is conceptually distinct from snake/linear `draft_resumed` which gives a fresh full pick clock per `20260425140000_draft_engine_v2_rpcs.sql:1136-1141`. The auction bid window represents **all teams' opportunity to react to the current high bid** — restoring remaining-time is fairness-correct (a fresh full window after pause would penalize teams who had already prepared their next bid against the captured deadline). Pause RPC computes `captured_remaining_seconds = ceil(EXTRACT(EPOCH FROM (expires_at - now())))` and persists in the `auction_paused` event payload; resume RPC `SELECT ... FOR UPDATE`s the most recent `auction_paused` row to recover this and computes `auction_nominations.expires_at = now() + captured_remaining_seconds * interval '1 second'`. **`LobbyManager.pauseState.remainingMs` is load-bearing for auction** (unlike snake/linear where it's informational only). Engine bootstrap APPLIES both events during replay per the canonical-replay principle from 6b. |
| 2026-05-07 | Path B state storage: `leagues.draft_state='paused'` is canonical; `auction_nominations.status` stays `'active'` (chunk 11g.6 sub-step 6c1) | The `auction_nominations.status` CHECK enum at migration `20260222000000_create_pool_auction_faab_tables.sql:109` admits only `('active', 'sold', 'no_sale')`. Adding `'paused'` would require a CHECK migration. Recon found that "paused" is more cleanly modeled as a **league-level state** rather than a nomination-level status: the nomination is logically still active (the same player, the same leading bid, the same nominator); the auction-as-a-whole is just frozen. `leagues.draft_state` already has the `'paused'` value (snake/linear uses it via `draft_pause`/`draft_resume`); auction reuses the same column with the same value. Engine reads `draft_state` agnostically; per-format pause semantics live in the event payloads (snake/linear emits `draft_paused`, auction emits `auction_paused`). Avoids a schema migration and a transient nomination-status that has to round-trip back to `'active'` on resume. |
| 2026-05-07 | `auction_nominations.expires_at` left untouched during pause (chunk 11g.6 sub-step 6c1) | Pause RPC does NOT NULL out `expires_at` or write a sentinel; the column simply becomes informational/stale until resume overwrites it. Canonical truth for "how long was left when we paused" lives in the `auction_paused` event payload's `captured_remaining_seconds` field. Rationale: minimizes write surface on the projection table (one less UPDATE per pause), keeps the row's audit fields consistent with its `nomination_event_id` back-link, and makes resume's UPDATE a clean overwrite (`expires_at = now() + captured_remaining_seconds * interval`). Engine respects `pauseState !== null` as the gate for reads / displays — a resume that lands successfully refreshes the row to a valid future timestamp. |
| 2026-05-07 | No `generation_bumped` events for auction RPCs (chunk 11g.6 sub-step 6c1) | Snake/linear's `draft_pause` / `draft_resume` emit a `generation_bumped` event before the lifecycle event for pgmq message-staleness coordination (Phase 4 worker). Chunk 11g.9 retires pgmq + the autopick worker entirely. Auction 6a/6b/6c1 RPCs follow the auction-precedent pattern of single-event-per-RPC (no generation bump); the snake/linear pattern is preserved for backwards compat in legacy RPCs only. |
| 2026-05-07 | RPC-side `draft_state='paused'` rejection in `place_bid_v2` and `nominate_player_v2` is defense-in-depth (chunk 11g.6 sub-step 6c1) | Engine fail-fast in `LobbyManager.processPlaceBid` / `processNominate` is the primary protection (returns `'auction_paused'` reason without RPC call). 6c1 also `CREATE OR REPLACE`s both RPCs to add a `SELECT draft_state FROM leagues` check that raises `illegal_state` when paused. World-class default: durable RPC gate protects against future code paths that bypass the engine — admin tools, migration scripts, manual psql, batch jobs, or future server architecture changes. The two layers serve different consumers: engine-gate is for runtime UX (typed `'auction_paused'` rejection so clients render a specific toast); RPC-gate is for durability (any caller, including non-engine, sees the consistent error). Same defense-in-depth pattern applies to 6c4 commissioner override when it lands. |
| 2026-05-07 | Engine `pauseAuction`/`resumeAuction` route through the single-writer queue — divergent from snake/linear's sync `pauseDraft`/`resumeDraft` (chunk 11g.6 sub-step 6c1) | Snake/linear's `pauseDraft`/`resumeDraft` are sync (not queued) because they were originally driven by bootstrap-replay where queue routing is unnecessary. For runtime commissioner actions during an active auction, **the queue gives strictly better semantics**: (1) deterministic test behavior — bid-then-pause-then-bid serializes in submission order; (2) audit trail in event-seq order matches wall-clock action order; (3) eliminates the in-flight-bid race entirely — a bid in flight when pause is queued processes BEFORE the pause, so the user's bid lands and they aren't stranded mid-action when the commissioner clicks pause. Pause/resume are rare actions (a few per draft at most); queue serialization overhead is negligible (<1ms per call at expected concurrency). The override of the recon's "match snake/linear sync precedent" recommendation is captured here for posterity — when 6c4 (commissioner override) ships, it should also route through the queue for the same reasons. Bid in-flight-race test (`'queue ordering: bid in flight when pause queues — bid is processed BEFORE pause (deterministic)'`) regression-locks this behavior. |
| 2026-05-07 | Typed `'auction_paused'` rejection reason in `DraftActionResult` (chunk 11g.6 sub-step 6c1) | Engine fail-fast on bid/nominate during `pauseState !== null` returns `{ok: false, reason: 'auction_paused'}`. Cleaner client UX than reusing generic `'invalid_state'` — clients can render "Auction paused by commissioner — bids reopen on resume" toast instead of a generic error. Symmetric with the 6a-introduced `'no_active_nomination'`, `'nomination_already_active'`, etc. |
| 2026-05-07 | World-class race-protection: `auction_resume_v2` `SELECT ... FOR UPDATE`s the prior `auction_paused` event row (chunk 11g.6 sub-step 6c1) | The resume RPC must read the most recent `auction_paused` event's `captured_remaining_seconds` to compute the new `expires_at`. Without a row-level lock, two simultaneous commissioner clicks (different sessions, same league) could both read the same paused event row, both pass the `draft_state='paused'` state-machine guard, and both INSERT `auction_resumed` events — producing two resume rows for one pause and double-firing the deadline restoration. The `idempotency_key` parameter handles user-initiated retries (same key → cached response); the **`SELECT ... FOR UPDATE`** handles the simultaneous-different-commissioners-clicks race at the row level. Two layers, different concerns. Pattern note: any future RPC that reads back state from a recent event for state-transition purposes must apply the same lock. Documented inline at `auction_resume_v2` Step 3. |
| 2026-05-07 | **Chunk 11g.6 sub-step 6c1 complete.** | Auction pause/resume lands as a single commit on `phase-4-5-implementation`. New migration `20260507010000_auction_pause_resume.sql` adds 2 RPCs (`auction_pause_v2`, `auction_resume_v2`) and `CREATE OR REPLACE`s `nominate_player_v2` + `place_bid_v2` to add the `draft_state='paused'` rejection gate. `auction_resume_v2` `SELECT ... FOR UPDATE`s the prior pause event row for race-protection. Wire: 2 new `BufferedDraftEvent` variants (`auction_paused`, `auction_resumed`) with optional fields for the active-nomination case. `DraftServiceV2.pauseAuction`/`resumeAuction` mirror the existing service pattern. `LobbyManager`: `pauseState` is now load-bearing (carries captured `remainingMs` for resume math); `pauseAuction`/`resumeAuction` public methods route through the single-writer queue (override of recon's snake/linear sync precedent — reasoning in the queue-routing entry above); `processPlaceBid`/`processNominate` engine fail-fast on `pauseState !== null` returning typed `'auction_paused'` reason; bootstrap APPLIES `auction_paused`/`auction_resumed` events during replay per the canonical-replay principle from 6b (mutates `pauseState` and `currentNomination.expiresAt` from event payloads). 734 server tests pass (+12 new 6c1 tests: pause happy path with timer cancellation, snake-format pause rejected, double-pause rejected, resume happy path with timer reschedule, resume-when-not-paused rejected, bid rejected during pause, nominate rejected during pause, queue-ordering deterministic with in-flight bid, bootstrap mid-pause sets pauseState, bootstrap multi-cycle clears pauseState, bootstrap pause-resume restores deadline, idempotent pause retry). Deferred remaining 6c sub-steps: tiered bid increments → 6c2; auto-nominate → 6c3; commissioner override (forced nominations / forced close / undo) → 6c4. Next sub-step: 6c2. |
| 2026-05-07 | Default tier table = flat $1 per ADR-002 §4.3 (chunk 11g.6 sub-step 6c2) | The 6c2 brief recon initially guessed an industry-tiered default (`[{below:10,inc:1},{below:50,inc:2},...]`) but ADR-002 §4.3 explicitly specifies "Flat $1. Default is flat $1 to preserve v1 behavior." Honored the spec. Default tier table is `[{below: Number.MAX_SAFE_INTEGER, increment: 1}]` — single tier covering all bid values at +$1 minimum. **This preserves all 6a/6b/6c1 test behavior unchanged** (every existing auction test bids in the single-digit-to-low-double-digit range; the default tier produces +$1 minimum increment for all of them, identical to the prior flat-$1 logic). Commissioners opt-in to tiered increments via `leagues.settings.auctionMinBidIncrementTiers`. |
| 2026-05-07 | Strict-less-than tier boundary (`leading_bid < tier.below`) per ADR-002 §4.5 example (chunk 11g.6 sub-step 6c2) | The §4.5 example "$1 below $10, $5 below $50, $10 above" is unambiguous: a leading bid of exactly $10 falls to the next tier (the below-50 tier with +$5 increment) rather than staying in the below-10 tier with +$1. Codified in both the SQL `compute_min_next_bid()` function (`p_leading_bid < v_below`) and the engine `computeMinimumNextBid()` (`leadingBid < tier.below`). Inline SQL examples in the migration document this for future engineers; pure-function tests in `auctionBidIncrement.test.ts` regression-lock the boundary cases. |
| 2026-05-07 | Path A gracious fallback when leading bid exceeds all tiers (chunk 11g.6 sub-step 6c2) | When the leading bid is ≥ every `below` value in the configured tier table (e.g., a $1500 bid in a league configured with `[..., {below: 999, increment: 10}]`), the function returns the **last tier's increment** rather than failing. Robust against high-budget leagues that out-bid configured ceilings. Brittle alternative ("Path B": reject with a new error) was considered and rejected — the spec doesn't address this case explicitly; gracious fallback survives misconfiguration cleanly. Documented with an inline example in the SQL migration: "Leading $1000 → no tier matches → +$10 → $1010 (Path A gracious fallback)". |
| 2026-05-07 | `auctionMinBid` decoupled from minimum increment per ADR-002 §4.3 (chunk 11g.6 sub-step 6c2) | Pre-6c2: `auctionMinBid` served TWO purposes — opening-bid floor AND minimum increment for subsequent bids. Post-6c2: those two are decoupled. `auctionMinBid` is opening-bid floor only (used in `processNominate`'s opening-bid validation); minimum increments come exclusively from `auctionMinBidIncrementTiers`. **Pre-launch decision** — auction shipped 2 days ago in 6a, no production leagues yet, so no migration concern. A league configured with `auctionMinBid: 5` but no custom tiers would see the default flat-$1 increment (not +$5) for bid steps. Per ADR-002 §4.3 these are listed as separate fields with separate defaults; the decoupling is the spec's intent, not a behavior change forced by 6c2's implementation. Reserve calculation `(slotsRemaining - 1) * auctionMinBid` is **unchanged** — reserve is the cheapest possible roster fill (tier-independent). |
| 2026-05-07 | Tier table validated at `lookupLobbyConfig` time (chunk 11g.6 sub-step 6c2) | `validateBidIncrementTiers()` (in `server/src/draft/auctionBidIncrement.ts`) runs at lobby construction and throws on invalid config: non-empty array, each element has positive numeric `below` + `increment`, and `below` values are strictly increasing across the array (no overlapping ranges, monotonic). Surfaces commissioner setup errors at draft-start rather than at first-bid — the engine refuses to construct the lobby for an invalid league config. Same fail-fast pattern as 6a's `nominationOrder` validation and 6c1's draft_state validation. |
| 2026-05-07 | Defense-in-depth: tier check at both engine and RPC layer (chunk 11g.6 sub-step 6c2) | Engine fail-fast in `LobbyManager.processPlaceBid` for cheap rejection (no round-trip when the bid is obviously below tier minimum) AND RPC-side enforcement via `compute_min_next_bid()` inside the `place_bid_v2` atomic transaction (durable enforcement against future code paths that bypass the engine — admin tools, manual psql, batch jobs). Same pattern as 6c1's `draft_state='paused'` gate. The two layers serve different consumers: engine surfaces typed `'bid_increment_violation'` reason + `minimumNextBid` field for client UX; RPC raises a structured exception that `mapRpcError()` translates back to the same engine reason for any caller that goes direct-to-RPC. |
| 2026-05-07 | `minimumNextBid?: number` field on `DraftActionResult` rejection arm (chunk 11g.6 sub-step 6c2) | Engine populates this field when `reason === 'bid_increment_violation'` so clients can render the actionable error "Minimum next bid: $X" instead of generic "bid increment violation." World-class polish detail with no architectural cost: `DraftActionResult` is a discriminated union with this field as an optional addition to the `{ok: false, reason}` arm. The field is meaningful only for that one reason value (RPC-side admin rejections leave it unset because they bypass the engine path that knows the value). Test regression-locks the asymmetry: `bid_too_low` rejection does NOT carry `minimumNextBid` (only `bid_increment_violation` does). |
| 2026-05-07 | Existing rejection reason `'bid_increment_violation'` reused (chunk 11g.6 sub-step 6c2) | The 6c2 brief suggested adding a new `'bid_below_min_increment'` rejection reason "for clarity." Recon found that 6a already added `'bid_increment_violation'` to `DraftActionResult.reason` for exactly this case (when the increment-specific check fails, vs. `'bid_too_low'` for the strict-greater check). **Recommend keeping `'bid_increment_violation'`** — semantically equivalent to the proposed alternative; renaming would just churn the union and break any downstream consumer (UI toast mappings, log filters, etc.). |
| 2026-05-07 | **Chunk 11g.6 sub-step 6c2 complete.** | Auction tiered bid increments lands as a single commit on `phase-4-5-implementation`. New migration `20260507020000_auction_tiered_bid_increments.sql` adds the `compute_min_next_bid(numeric, jsonb)` STABLE SQL function (with inline comment examples for tier-transition boundary cases) and `CREATE OR REPLACE`s `place_bid_v2` to add a new `p_min_bid_increment_tiers jsonb` parameter and the increment-validation gate alongside the existing strict-greater bid check + 6b anti-snipe + 6c1 pause gate. `LeagueFormatSettings.auctionMinBidIncrementTiers` field added with default `[{below: Number.MAX_SAFE_INTEGER, increment: 1}]` (flat $1 per ADR-002 §4.3); `mergeWithDefaults` returns default when not configured. New pure helper module `server/src/draft/auctionBidIncrement.ts` exports `BidIncrementTier`, `computeMinimumNextBid`, `validateBidIncrementTiers`, `DEFAULT_BID_INCREMENT_TIERS`. `LobbyConfig` + `LobbyManagerOptions` + `LobbyManager` thread the tier table through; `processPlaceBid` replaces the prior flat-`auctionMinBid` increment check with `computeMinimumNextBid` + populates `minimumNextBid` on rejection. `DraftServiceV2.PlaceBidParams` + `placeBid` RPC call extended. `DraftActionResult` rejection arm gets optional `minimumNextBid?: number` field. 760 server tests pass (+26 net: +16 pure-function tests in `auctionBidIncrement.test.ts` covering tier boundaries / Path A fallback / fractional increments / validation rejection cases, +10 LobbyManager integration tests covering tier transitions / engine RPC plumbing / `minimumNextBid` field population / backward-compat with default flat-$1). Deferred remaining 6c sub-steps: auto-nominate → 6c3; commissioner override → 6c4. Next sub-step: 6c3. |
| 2026-05-07 | Pre-launch field rename: `auctionNominationTime` → `auctionBidWindowSeconds` (chunk 11g.6 sub-step 6c3) | The 6a engine reused the existing `auctionNominationTime` setting (default 30s) as its bid-window duration, despite the name implying nominator's-clock semantics. ADR-002 §3.4 specifies these as **separate primitives** (`bid_window_seconds` default 30 + `nomination_window_seconds` default 60). Pre-launch rename brings engine semantics in line with the spec: drop `auctionNominationTime` entirely; add explicit `auctionBidWindowSeconds` (default 30) + `auctionNominationWindowSeconds` (default 60). Auction shipped 2 days ago in 6a; no production leagues yet; the rename is free at this point. Same pre-launch-cleanliness justification as 6c2's `auctionMinBid` decoupling. Backward-compat via field rename in `LeagueFormatSettings`, `LobbyConfig`, `LobbyManagerOptions`, `lookupLobbyConfig`, and the test helper. |
| 2026-05-07 | Single-timer-handle architecture with `currentTimerKind` discriminator (chunk 11g.6 sub-step 6c3) | Pre-6c3 `LobbyManager` had one timer handle (`currentPickTimerHandle`, used for snake/linear pick deadline AND auction bid window). 6c3 introduces auction's nomination-window timer — the engine now needs to know which timer is active when it expires. **Recommend kept and adopted: single timer handle + discriminator** (instead of two parallel handles). Renamed `currentPickTimerHandle` → `currentTimerHandle` and `currentPickDeadline` → `currentTimerDeadline` for semantic clarity; added `currentTimerKind: 'pick' \| 'bid_window' \| 'nomination_window' \| null`. **Mutually exclusive at runtime by construction** — at most one timer is active. Snake/linear uses `'pick'`; auction uses `'bid_window'` (active nomination open for bidding) or `'nomination_window'` (between nominations — current nominator has the clock to pick a player). `handleClockExpired` dispatches by format AND kind. Tests verify never-both. The wire-protocol field name `currentPickDeadline` (in `DraftStateSnapshot`) is **unchanged** — the rename was internal-engine-only. |
| 2026-05-07 | `pauseState.pausedTimerKind` extension + optional `pausedTimerKind` on `auction_paused` event payload (chunk 11g.6 sub-step 6c3) | When a pause fires during nomination-window state, `pauseState` needs to know which timer was running so resume restores the correct one. Extension to `pauseState`: `{pausedAt, remainingMs, pausedTimerKind: 'bid_window' \| 'nomination_window'}`. Wire event `auction_paused` gets optional `pausedTimerKind?` field; backward-compat default to `'bid_window'` for 6c1 events that didn't have this field. Snake/linear's `pauseDraft` populates `'bid_window'` as a harmless backward-compat default — the engine consults `format` to decide the restore path; this field is load-bearing only for auction. The bootstrap event-replay handler reads the field with the backward-compat default so 6c1-era event logs replay correctly. |
| 2026-05-07 | **Path Y extension of ADR-002**: force-skip when auto-nominator can't afford (chunk 11g.6 sub-step 6c3) | ADR-002 §4.4's only auto-nominate edge case is the strategy-chain-exhausted path (queue empty + ML returns nothing + commissioner pre-set unset → critical alert + draft pause). The spec is silent on the realistic case where the strategy returns a player but the nominator can't afford even `auctionMinBid + reserve`. The 6c3 brief proposed Path Y (skip + cascade to next nominator) and recon recommended it. **Implemented Path Y.** Better UX than disrupting all 11 other teams for one nominator's budget exhaustion. Decision rationale recorded here: "Force-skip when auto-nominator can't afford auctionMinBid + reserve is an extension of ADR-002. The spec is silent on this case; Path X (spec-conformant pause-and-alert) would disrupt all 11 other teams; Path Y (skip + cascade to next nominator) preserves auction flow. World-class user experience favors skip; **Zach should push back if he disagrees with the extension**." Worst-case all-teams-skip → auction completes early via the existing `nominationsCompleted >= teams × draftRounds` check (no new completion logic required). New event variant `auction_nomination_skipped` with mixed reason discriminator: `'insufficient_budget'` (Path Y skip-and-cascade) and `'no_eligible_players'` (Path X paired with pause-and-alert; pause-wiring lands in chunk 11g.7 alongside ops alerting). |
| 2026-05-07 | Auction auto-nominate strategy chain mirrors snake/linear autopick architecture (chunk 11g.6 sub-step 6c3) | New module `server/src/draft/auctionAutoNominateStrategy.ts` implements the chain-of-strategies pattern, mirroring chunk 11g.4 step 6c `autopickStrategy.ts` exactly. Today ships with `[projectionsAuctionStrategy]` only — pick the highest-projected player not yet nominated (any status: 'active' / 'sold' / 'no_sale'). Future chains can prepend `queueAuctionStrategy` when `team_draft_queues` schema lands (per chunk 11g.4 step 6c Decision Log) and append `commissionerPresetAuctionStrategy` when that UI exists; both are single-line array changes at `DEFAULT_AUCTION_STRATEGIES`. Architectural mirror keeps the cognitive load low for engineers familiar with the snake/linear pattern. Differences from snake/linear: auction uses TEXT player IDs (vs int) and the strategy returns an `openingBid` (today fixed at `auctionMinBid`; future strategies could bid higher). |
| 2026-05-07 | Deterministic idempotency keys for engine-fired auction actions (chunk 11g.6 sub-step 6c3) | Pattern from 6b (`md5('extends:'\|\|...)::uuid`) extended to 6c3's auto-nominate + skip paths. Engine derives keys via `md5UuidFromSeed()`: `md5('auto-nominate:' + leagueId + ':' + nominationsCompleted)::uuid` for auto-nominates AND `md5('skip:' + leagueId + ':' + nominationsCompleted)::uuid` for skips. Same retry-safety guarantee — engine restart mid-fire produces the same key; RPC's idempotency check returns the cached result with `was_duplicate=true`. The `nominationsCompleted` component ensures distinct keys across the auction's lifecycle. Wired in `LobbyManager.deriveAutoNominateIdempotencyKey()` + `deriveSkipIdempotencyKey()` private helpers; SQL-side derivation matches via Postgres' `md5()::uuid` (deterministic across both layers). |
| 2026-05-07 | Cascade infinite-loop guard at top of `processAutoNominate` (chunk 11g.6 sub-step 6c3) | `handleNominationWindowTimeout` first-line check: `if (nominationsCompleted >= teams × draftRounds) → transition to 'completed' and exit`. Worst-case all-teams-skip → auction completes early via this guard rather than infinite-looping through nomination_window timer fires. The 6a `handleNominationTimeout` post-close logic already had a similar completion check; 6c3 adds the symmetric guard at the auto-nominate path's entry. Test regression-locks the cascade exhaustion case ("engine completion via cascade: all teams skip → status becomes completed"). |
| 2026-05-07 | Bootstrap timer-kind reconstruction from final event log state (chunk 11g.6 sub-step 6c3) | Per the canonical-replay principle from 6b: every state-affecting event has an apply-during-replay handler. New 6c3 bootstrap handlers `applyAuctionAutoNominatedEvent` (treats event symmetrically with `auction_nomination_started` for state-machine purposes — both set `currentNomination`) and `applyAuctionNominationSkippedEvent` (advances `nominationsCompleted` without setting `currentNomination`). Post-replay logic in `init()` extends the 6a/6c1 reconstruction: when `currentNomination` is null AND auction is in_progress AND `initialDraftState='active'`, schedule the nomination-window timer for the current nominator (locally-computed deadline `now() + nominationWindowMs` since no DB row tracks nomination-window deadlines — they're transient). Fresh-start auctions stay at `not_started` until `initialDraftState` reaches `'active'` AND init() runs; the very first nomination-window timer is the bridge between the lifecycle init and the running auction. Backward-compat: snake/linear bootstrap unchanged. |
| 2026-05-07 | **Chunk 11g.6 sub-step 6c3 complete.** | Auction auto-nominate + nomination-window timer + force-skip cascade lands as a single commit on `phase-4-5-implementation`. New migration `20260507030000_auction_auto_nominate.sql` adds the `auction_nomination_skipped` event_type to the CHECK enum and a new `auction_nomination_skip_v2` RPC (atomic 2-write block). Wire types: full `auction_auto_nominated` `BufferedDraftEvent` variant (replaces the chunk 11g.4 step 3 placeholder doc reference; carries the full nomination_started shape + `fallbackSource` discriminator) and new `auction_nomination_skipped` variant with mixed reason discriminator. `auction_paused` extended with optional `pausedTimerKind?` field (backward-compat default `'bid_window'`). `LeagueFormatSettings`: `auctionNominationTime` renamed to `auctionBidWindowSeconds` (default 30) + new `auctionNominationWindowSeconds` (default 60). New pure helper module `server/src/draft/auctionAutoNominateStrategy.ts` exports `selectAuctionAutoNominate`, `projectionsAuctionStrategy`, `DEFAULT_AUCTION_STRATEGIES`. `DraftServiceV2.skipNomination` method added; `pauseState` extended with `pausedTimerKind` discriminator. `LobbyManager`: timer rename (`currentPickTimerHandle` → `currentTimerHandle`, `currentPickDeadline` → `currentTimerDeadline`) + new `currentTimerKind` discriminator field; `setPickDeadline` accepts `kind` parameter; `handleClockExpired` dispatches by format AND kind for auction; new `handleNominationWindowTimeout` (auto-nominate handler) + `fireAutoSkipEvent` (Path Y skip helper) private methods; new `applyAuctionAutoNominatedEvent` + `applyAuctionNominationSkippedEvent` bootstrap handlers; `processPauseAuction` captures `pausedTimerKind`; `processResumeAuction` restores correct timer kind; `init()` schedules the very first nomination-window timer for fresh-start auctions when `initialDraftState='active'`. New helper `md5UuidFromSeed()` for deterministic idempotency keys. 780 server tests pass (+20 net 6c3 tests: +9 pure-function strategy tests in `auctionAutoNominateStrategy.test.ts`, +11 LobbyManager integration tests covering auto-nominate happy path / insufficient_budget skip / cascade exhaustion / user-nominate-cancels-window-timer / pause-during-nomination-window / bootstrap mid-window / bootstrap mid-auto-nominate / bootstrap-with-skip-events / no_eligible_players reason / cascade-completion / deterministic-idempotency-key). All 760 pre-6c3 tests stay green (backward-compat preserved). Deferred: commissioner override (forced nominations / forced close / undo) → 6c4. Next sub-step: 6c4 (last micro-sub-step in chunk 11g.6 sub-step 6c). |
| 2026-05-07 | **Seven-action commissioner override extends ADR-002 §4.4** (chunk 11g.6 sub-step 6c4) | ADR-002 §4.1 catalogs `auction_commissioner_override` with three illustrative actions ("forced nomination, forced close, undo") and an underspecified `{commissioner_user_id, override_action, prior_state, new_state}` payload. **6c4 ships seven actions** — `revert_bid`, `force_close_nomination`, `award_to_team`, `adjust_opening_bid`, `adjust_budget`, `cancel_nomination`, `extend_bid_window` — the operational toolbox commissioners need on day 1. Same Path Y treatment as 6c3: explicit "**Zach should push back if he disagrees with the broader scope**" framing. The seven actions don't conflict with the spec; they extend it. Decision rationale: commissioners need to fix bid races (revert), end stalled biddings (force_close), correct system errors (award_to_team), enforce league policies mid-draft (adjust_opening_bid + adjust_budget), undo bad nominations (cancel_nomination), and grant grace time (extend_bid_window). Shipping less than the full toolbox would force commissioners to either cancel + restart the entire draft or manually mutate the database — both worse than shipping the full operational set. |
| 2026-05-07 | Polymorphic single-event-variant architecture for commissioner override (chunk 11g.6 sub-step 6c4) | One RPC (`auction_commissioner_override_v2`), one event variant (`auction_commissioner_override`), one engine handler (`processCommissionerOverride`). Per-action dispatch happens via the `overrideAction` discriminator at three layers: SQL (IF/ELSIF ladder inside the RPC), TypeScript engine handlers (switch on `action.kind`), and bootstrap APPLY (switch on `event.payload.override_action`). **No additional side-effect events are emitted** — `force_close_nomination` does NOT also emit `auction_nomination_closed`; engine state mutations match what natural events would have produced (e.g., budget deductions, players_won increments, `draft_picks` insert), but the canonical durable event is a single `auction_commissioner_override` row. Avoids redundant event pairs; canonical-replay logic is single-handler. |
| 2026-05-07 | Status CHECK enum extension: `'cancelled'` value added to `auction_nominations.status` (chunk 11g.6 sub-step 6c4) | Pre-6c4: `('active', 'sold', 'no_sale')`. The `cancel_nomination` action transitions the row to `'cancelled'` to preserve audit trail (distinguishes commissioner-initiated cancellation from natural no-sale). Two-line ALTER (DROP + ADD CHECK CONSTRAINT). Engine reads/handles `'cancelled'` purely through the override event payload; no other code path observes nominations in `'cancelled'` status. |
| 2026-05-07 | `force_close_nomination` with only-opening-bid → no_sale outcome (chunk 11g.6 sub-step 6c4) | Mirrors 6a's natural-close `no_sale` branch (when only the nominator's opening bid exists). The `force_close` override semantics match the natural timer expiry semantics for this case; commissioner uses `force_close` to skip the wait, not to override the outcome. If commissioner wants to award the player despite no follow-up bids, use `award_to_team` instead. |
| 2026-05-07 | `revert_bid` requires bid_count > 1 — reverting only the opening bid is illegal (chunk 11g.6 sub-step 6c4) | Reverting the nominator's opening bid would orphan the nomination (nothing left to revert to). RPC raises `no_bids_to_revert`; engine surfaces as typed rejection. Commissioner uses `cancel_nomination` instead for that case. Distinct error paths for distinct intents. |
| 2026-05-07 | `adjust_opening_bid` bidirectional safeguards including new budget-coverage check (chunk 11g.6 sub-step 6c4) | Three rejection rules: (1) floor < current_high_bid → reject with `opening_bid_below_current_leading` (nonsensical — commissioner cannot retroactively undermine a committed bid); (2) floor > current_high_bid AND leader's budget can't cover (new_floor + reserve) → reject with `insufficient_budget_for_floor_increase` (Zach's safeguard from the 6c4 approval message — commissioners cannot force a leader into a budget-violating state); (3) floor == current_high_bid → no-op, allowed. When floor > current_high_bid AND leader can afford, current_high_bid bumps up to match (commissioner-authorized force of leader's commitment). The new `'insufficient_budget_for_floor_increase'` rejection reason is distinct from the standalone `'insufficient_budget'` (used by `adjust_budget` debit) for client UX clarity. |
| 2026-05-07 | `cancel_nomination` does NOT advance `nominationsCompleted` — redo semantics, not skip (chunk 11g.6 sub-step 6c4) | Compare to `auction_nomination_skipped` from 6c3 which DOES advance `nominationsCompleted` (skip semantics — that team can't proceed due to budget exhaustion or strategy exhaustion). `cancel_nomination` is the redo path: the same nominator gets another chance. Engine reschedules the SAME nominator's nomination-window timer (or, if paused, mutates `pauseState.pausedTimerKind` to `'nomination_window'` so resume restores correctly). |
| 2026-05-07 | `cancel_nomination` during pause flips `pausedTimerKind` AND resets captured `remainingMs` (chunk 11g.6 sub-step 6c4) | Zach's safeguard from the 6c4 approval message. When cancel happens during pause, the engine: (a) flips `pauseState.pausedTimerKind` from `'bid_window'` to `'nomination_window'` — the bid-window timer no longer exists post-cancel; (b) **resets** `pauseState.remainingMs` to `nominationWindowMs` (the captured time was for the now-nonexistent bid_window timer; resume gives the new current nominator a fresh nomination window). Without this reset, resume would restore the old (bid-window) `remainingMs` value to the new (nomination-window) timer — semantically incorrect timer duration. Test regression-locks this behavior. |
| 2026-05-07 | All seven override actions allowed during pause (chunk 11g.6 sub-step 6c4) | Commissioner overrides are precisely the tool for fixing things that need fixing — including in-paused-state. Engine state mutation applies normally; timer state stays paused (the override doesn't auto-resume). World-class default: commissioner toolbox is fully available regardless of pause state. |
| 2026-05-07 | `auction_bids` is append-only — no row deletion on revert/cancel (chunk 11g.6 sub-step 6c4) | The `revert_bid` action does NOT delete the reverted bid's row; the `cancel_nomination` action does NOT delete the cancelled nomination's bid rows. Append-only audit principle: the `auction_commissioner_override` event with the discriminator + `priorState`/`newState` snapshots is the canonical "logically reverted/cancelled" marker. Avoids destructive writes to the audit log. |
| 2026-05-07 | `verifyCommissionerAuthorization` callback (chunk 11g.6 sub-step 6c4) | New engine-side authorization callback parallel to `verifyTeamAuthorization` from chunk 11g.4 step 4. Returns `CommissionerAuthorizationResult` discriminated union (`{authorized: true} \| {authorized: false; reason: 'not_commissioner' \| 'league_not_found'}`). Production implementation in `server/src/draft/index.ts` queries `leagues.commissioner_id` directly. Engine fail-fasts at `processCommissionerOverride` BEFORE the RPC call; RPC enforces the same check. Defense-in-depth pattern matches 6c1's `draft_state` gate. Engine returns coarse-grained `'unauthorized'` to clients; granular reason logged at info level (no information disclosure). |
| 2026-05-07 | Caller-provided idempotency keys for commissioner overrides (chunk 11g.6 sub-step 6c4) | Different fire pattern from 6c3's auto-nominate (engine-derived `md5UuidFromSeed` keys). Commissioner overrides are UI-driven; the UI provides the idempotency key. RPC's idempotency check at the top of the function returns the cached result on retry. No `md5UuidFromSeed` derivation needed for this path. |
| 2026-05-07 | `commissionerOverride` routes through the single-writer queue (chunk 11g.6 sub-step 6c4) | Same architectural pattern as 6c1's `pauseAuction`/`resumeAuction` queue routing. Commissioner overrides serialize against in-flight bids/nominations deterministically; audit trail in event-seq order matches wall-clock action order; in-flight-action race eliminated. |
| 2026-05-07 | Canonical-replay principle extended to all seven override action types (chunk 11g.6 sub-step 6c4) | `applyAuctionCommissionerOverrideEvent` dispatches on `event.payload.override_action` to per-action APPLY logic. Each branch mutates engine state to mirror what the runtime handler did. Per-action APPLY shapes: `revert_bid` mutates `currentNomination.leadingBidderId`/`leadingBid`; `force_close_nomination` deducts winner budget + advances `nominationsCompleted` + clears `currentNomination`; `award_to_team` mirrors force_close but for a non-leader winner; `adjust_opening_bid` mutates `currentNomination.leadingBid`; `adjust_budget` mutates `teamBudgets`; `cancel_nomination` clears `currentNomination` (no `nominationsCompleted` advance); `extend_bid_window` mutates `currentNomination.expiresAt`. |
| 2026-05-07 | **Chunk 11g.6 sub-step 6c4 complete. Chunk 11g.6 functionally complete. Auction format is production-ready.** | Auction commissioner override (all seven action variants) lands as a single commit on `phase-4-5-implementation`. New migration `20260507040000_auction_commissioner_override.sql` extends `auction_nominations.status` CHECK enum with `'cancelled'` + adds the polymorphic `auction_commissioner_override_v2` RPC with seven IF/ELSIF action branches (each atomically wrapping its action-specific multi-table writes). Wire types: full polymorphic `auction_commissioner_override` `BufferedDraftEvent` variant with seven-value `overrideAction` discriminator + action-specific `priorState`/`newState` JSONB snapshots. `DraftActionResult.reason`: 5 new typed rejection reasons (`no_bids_to_revert`, `insufficient_budget_for_award`, `opening_bid_below_current_leading`, `extension_below_current_deadline`, `insufficient_budget_for_floor_increase`). `DraftServiceV2.commissionerOverride` method + `CommissionerOverrideAction` discriminated union. New `verifyCommissionerAuthorization` callback wired through `LobbyRegistry` + production implementation. `LobbyManager`: new public `commissionerOverride` method routed through single-writer queue + private dispatcher + 7 per-action handlers + shared `fireCommissionerOverrideRpc` helper + `applyAuctionCommissionerOverrideEvent` bootstrap handler with seven-branch APPLY dispatch. 800 server tests pass (+20 net 6c4 tests covering all 7 actions × happy path + key rejection edge cases + authorization rejection + cancel-during-pause timer-kind flip + bootstrap mid-override-sequence + idempotent retry). All 780 pre-6c4 tests stay green (backward-compat preserved). **Chunk 11g.6 functionally complete.** Auction draft format ships with: full state machine (6a) + anti-snipe extension (6b) + pause/resume (6c1) + tiered increments (6c2) + auto-nominate + force-skip cascade (6c3) + full commissioner override toolbox (6c4). Auction format is production-ready. Next: chunk 11g.7 (operations layer — heartbeat/keepalive, observability, alerting, snapshot persistence). |
| 2026-05-07 | Structured logger module — new alongside legacy (chunk 11g.7 sub-step 7a) | New `packages/shared/src/utils/structuredLogger.ts` ships alongside the legacy `logger.ts` (which stays for non-engine code that hasn't migrated). The new `StructuredLogger` interface has 4 methods (`debug`/`info`/`warn`/`error`); the `error` method accepts an optional `err: unknown` third arg whose `name`/`message`/`stack` get extracted into the JSON payload (handles both `Error` instances and unknown thrown values; stack traces truncated to 2 KB). The default singleton is no-op (mirrors `logger.ts` convention); the engine entry point at `server/src/draft/index.ts` activates it via `Object.assign(structuredLogger, createConsoleStructuredLogger())`. Two coexisting loggers is intentional — the structured logger is the canonical engine-side API; the legacy logger remains for non-engine code (web app components, services that haven't migrated). Forward-compat: client-side code can import the new logger if chunks 11g.7 7d/7e need it for client-side observability. |
| 2026-05-07 | GCP-canonical severity-string mapping (chunk 11g.7 sub-step 7a) | GCP Cloud Logging Agent's stdout-JSON auto-parser requires canonical severity names: `DEBUG\|INFO\|NOTICE\|WARNING\|ERROR\|CRITICAL\|ALERT\|EMERGENCY`. Internal API uses `warn` for ergonomics; on-wire JSON outputs `WARNING` for compatibility. Future cloud platform changes (e.g., AWS CloudWatch) would need similar mapping; the `LogSeverity` type in `structuredLogger.ts` is the single point of change. The `time` field (ISO 8601, GCP-canonical key name; `timestamp` also accepted but `time` is preferred) is auto-populated. Internal `WARN` env value is normalized to `WARNING` at logger construction. |
| 2026-05-07 | Top-level field schema (chunk 11g.7 sub-step 7a) | Standardized fields every engine log line MAY include: `severity`, `time`, `event`, `message?`, `correlationId?`, `leagueId?`, `lobbyId?`, `userId?`, `teamId?`, plus action-specific fields via the arbitrary `[key: string]: unknown` extension on `LogContext`. Alert policies query the standardized fields directly; action-specific extensions flow through as `jsonPayload.<key>` in Cloud Logging. The `event` discriminator is the primary identifier (e.g., `'lobby.constructed'`, `'auction.bid.placed'`, `'rpc.broadcast_failed'`); future commits incrementally restructure the bulk-renamed free-text-as-event holdover into canonical dot-namespaced names. |
| 2026-05-07 | PII policy (chunk 11g.7 sub-step 7a) | **Auction state is public to all draft participants; logged identifiers are opaque UUIDs; no display names, email addresses, auth tokens, or free-text user input are logged today. If future work adds (a) display names, (b) email addresses, (c) auth tokens or secrets, or (d) free-text user input (chat messages, dispute notes), redaction policy is revisited at that point. The current set — UUIDs and auction state — does not require redaction.** This Decision Log entry is the canonical reference; any future PR adding logged content must update this policy or stay within the existing inventory. |
| 2026-05-07 | No sampling primitive in 7a (chunk 11g.7 sub-step 7a) | Peak event rate calculation: highest-volume event is `auction_bid_placed` during a hot anti-snipe cascade. Realistic worst case ~50 bids over 5 minutes (~0.16 bids/sec) per nomination; 12 simultaneous lobbies = ~2 bids/sec sustained peak. Well below the ~10/sec threshold where sampling matters. **Sampling primitive deferred to chunk 11g.11 load-test** if real need surfaces. The `LogContext` extension shape is forward-compat: future per-event sampling can read a `sampleRate` field from a `LOG_SAMPLE_<EVENT>` env var without touching call sites. |
| 2026-05-07 | Vitest source-scan enforcement instead of ESLint (chunk 11g.7 sub-step 7a) | Server has no ESLint config; adding it for a single rule is heavy. Instead, `server/src/__tests__/noConsoleOrLegacyLogger.test.ts` scans `server/src/draft/`, `server/src/services/DraftServiceV2.ts`, and `server/src/lib/draftToken.ts` and fails if it finds `console.<log\|warn\|error\|debug\|info>(` calls or `logger.<log\|warn\|error\|debug\|info>(` calls (the legacy logger from `@citrus/shared`). The legacy logger may still be IMPORTED in engine entry-point code (e.g., for `Object.assign(logger, createConsoleLogger())` activation), but engine code MUST emit through `structuredLogger`. Failure message clearly explains: "Engine code must use structuredLogger; raw console and legacy logger are not permitted." The scan covers both single-line and multi-line code patterns. Comment-only references (e.g., explanatory JSDoc mentioning `console.log`) are stripped before matching to avoid false positives. |
| 2026-05-07 | LOG_LEVEL=SILENT default for tests (chunk 11g.7 sub-step 7a) | Vitest setup file at `server/src/__tests__/setup.ts` sets `process.env.LOG_LEVEL='SILENT'` if unset. Default INFO for production, DEBUG for development, SILENT for tests. Engine tests don't spew hundreds of structured log lines into vitest's output; specific tests that need to assert log calls (e.g., `engineLogAssertions.test.ts`) override `process.env.LOG_LEVEL` in `beforeEach` and create a fresh logger instance OR use `vi.spyOn(structuredLogger, 'error')` to intercept calls regardless of the active level filter. |
| 2026-05-07 | Refactor strategy for 7a — bulk JSON shape, incremental event-name canonicalization (chunk 11g.7 sub-step 7a) | The bulk-rename pass converted 97 emit sites across 8 files (`logger.<sev>(...)` → `structuredLogger.<sev>(...)`) to land the JSON output shape with GCP-compatible severity strings, ISO timestamps, and `LOG_LEVEL` filtering in a single commit. Existing free-text template literals become the `event` field on the JSON payload — this is a shallow form of structuredness (Cloud Logging-queryable as substring match) but not the canonical dot-namespaced form (`'lobby.auth.unauthorized'` + `{correlationId, lobbyId, userId, teamId}` context). **Per-event-name canonicalization is incremental work.** The 5 `DraftServiceV2.ts` emits already use the canonical shape (e.g., `'draft_v2.broadcast_fetch_failed'`); they're the reference pattern. Future commits restructure clusters of related emits at a time (e.g., all bid-validation rejections in one PR). The 7a foundation establishes the infrastructure; subsequent restructuring is purely additive on top of it. |
| 2026-05-07 | Two-arg `logger.error(message, err)` legacy form converted to three-arg `structuredLogger.error(event, context, err)` (chunk 11g.7 sub-step 7a) | Pre-7a pattern: `logger.error('msg with interpolated fields', err)` (variadic, second arg an Error). Post-7a: `structuredLogger.error('msg with interpolated fields', {}, err)` (3-arg form: event-as-template-literal, empty context placeholder, err). The empty `{}` context is a transitional shape — the bulk regex pass inserted it to make the call signature compile against the new logger interface. Future restructuring fills the `{}` with real context fields (e.g., `{correlationId: action.idempotencyKey, lobbyId: this.lobbyId}`). Non-error severities (`debug`/`info`/`warn`) had their `err` arg dropped (these are diagnostic-only emits where the message text already describes the condition; structured `err` extraction is `error`-only by design). |
| 2026-05-07 | Foundation for chunks 11g.7 7b–7e (chunk 11g.7 sub-step 7a) | Subsequent operational-hygiene primitives all depend on consistent log structure for their own observability: 7b (HTTP snapshot endpoint) emits structured logs on snapshot fetch; 7c (snapshot persistence) emits structured logs on snapshot write/read; 7d (heartbeat/keepalive) emits structured logs on dead-connection detection; 7e (live event subscription) emits structured logs on cross-process event delivery. Each subsequent sub-step builds on the structured-logger module from 7a; the standardized field schema (correlationId / leagueId / lobbyId / userId / teamId) means alert policies written for 7a's existing emits automatically apply to the new emits in 7b–7e. |
| 2026-05-07 | **Chunk 11g.7 sub-step 7a complete.** | Structured logging refactor for the draft engine lands as a single commit on `phase-4-5-implementation`. New module `packages/shared/src/utils/structuredLogger.ts` exports `StructuredLogger` interface, default no-op `structuredLogger` singleton, and `createConsoleStructuredLogger()` factory. Engine entry point (`server/src/draft/index.ts`) activates the structured logger alongside the legacy logger. All 97 emit sites across 8 files refactored: `LobbyManager.ts` (65), `uws-server.ts` (12), `DraftServiceV2.ts` (6), `draft/index.ts` (5), `uws-helpers.ts` (3), `LobbyRegistry.ts` (2), `autopickStrategy.ts` (2), `auctionAutoNominateStrategy.ts` (2). Vitest source-scan test (`noConsoleOrLegacyLogger.test.ts`) bans raw `console.*` and legacy `logger.<sev>(` calls in `server/src/draft/`, `server/src/services/DraftServiceV2.ts`, and `server/src/lib/draftToken.ts` — engine code must use `structuredLogger`. `LOG_LEVEL=SILENT` default for tests via vitest setup file. 818 server tests pass (+18 net 7a tests: +12 logger unit tests + +1 source-scan test + +5 ERROR-level critical-path log assertions). All 800 pre-7a tests stay green (backward-compat preserved). Per-event-name canonicalization is incremental follow-up work; the 7a foundation establishes the JSON output shape, GCP severity mapping, LOG_LEVEL filtering, correlation-ID propagation infrastructure, PII policy, and source-scan enforcement. Next: chunk 11g.7 sub-step 7b (HTTP snapshot endpoint). |
| 2026-05-07 | Two-process deployment topology + Path A snapshot architecture (chunk 11g.7 sub-step 7b) | Recon for 7b confirmed the deployment is **TWO separate Node processes**: (1) the main Citrus API server at `server/src/index.ts` → `server/src/app.ts` runs on port 3001 with the full `/api/...` route surface; (2) the draft engine at `server/src/draft/index.ts` runs separately with a minimal Hono on port 3001 + uWS on port 3002 — the `LobbyRegistry` and all in-memory state live ONLY in this engine process. The two processes share Postgres but NOT in-memory state. Three architectural paths for the snapshot endpoint were considered: **Path A** (DB-driven reconstruction in main API server) — feasible, ~50ms latency, mirrors discovery endpoint location; **Path B** (IPC from main API to engine) — adds new mechanism, two-hop; **Path C-prime** (snapshot in engine's Hono) — direct in-memory read, but client must learn engine HTTP URL via discovery. **Recommended and implemented Path A** — single client-facing host, consistency with discovery endpoint location at `server/src/routes/drafts.ts`, reuses existing auth middleware. Trade-off: ~50ms vs ~5ms latency is immaterial because snapshot fetch is rare (post-disconnect resync after >5 min outage). |
| 2026-05-07 | Snapshot consistency model: < 1 sec staleness boundary (chunk 11g.7 sub-step 7b) | Path A snapshot is a point-in-time DB read. Events committed but not yet broadcast to clients (the < 1 sec window between `INSERT draft_events` commit and `LobbyManager.broadcast`) may be missed by the snapshot. **The client's WS resync flow handles drift**: after fetching the snapshot, the client reconnects WS and issues `resync(sinceSeq=snapshot.lastSeenSeq)` which delivers any events that committed during the gap. This is the canonical Citrus pattern (event log is canonical; engine + snapshot are derived views with bounded staleness). The architecture is the same as v1's poll-based snapshot + resync after reconnect. Documented for future contributors who might be tempted to add caching to "freshen" the snapshot — the bound is acceptable; caching would just add complexity without UX benefit. |
| 2026-05-07 | Auth scheme matches route group, not resource access pattern (chunk 11g.7 sub-step 7b) | **Architectural principle**: the auth scheme used by an HTTP route is determined by which route group it lives in, not by the underlying resource's other access patterns. The WS upgrade endpoint uses draft tokens (HMAC HS256, 5-min TTL, issued by the discovery endpoint and verified by the engine process). The HTTP snapshot endpoint uses Supabase JWT via `authMiddleware` because that is the convention for routes in `server/src/routes/drafts.ts`. Both verify league membership; different mechanisms. The 7b recon initially proposed mirroring WS auth (per the brief), but recon found the snapshot endpoint lives in the main API server's route group where Supabase JWT is the established pattern. Mixing auth schemes within the same `/api/drafts/...` route group would be cognitive overhead. Override accepted; this Decision Log entry is the canonical reference for any future operational endpoints that may face the same choice. |
| 2026-05-07 | No operational machinery in 7b — caching/compression/rate-limit deferred (chunk 11g.7 sub-step 7b) | Path A's DB queries are fast (~50ms total). Two simultaneous disconnect-then-reconnect flows for the same lobby would each compute their own snapshot, ~100ms aggregate — not enough load to justify caching infrastructure for 7b. Snapshot sizes for 12-team auctions cap at ~50 KB JSON; compression provides marginal benefit at the cost of CPU + middleware complexity. Snapshot endpoint is rare-call by nature (post-disconnect resync after >5 min outage); rate limiting would prevent abuse but adds a new dependency for prevention of a not-yet-observed problem. **All three (caching, compression, rate-limit) deferred to chunk 11g.11 load-test** if real load surfaces a need. The 7b foundation establishes the endpoint shape; subsequent operational hardening is purely additive. |
| 2026-05-07 | `structuredLogger` voluntarily in non-engine route code (chunk 11g.7 sub-step 7b) | The 7a source-scan ban covers engine code only (`server/src/draft/`, `server/src/services/DraftServiceV2.ts`, `server/src/lib/draftToken.ts`). The 7b snapshot endpoint lives in `server/src/routes/drafts.ts` which is engine-adjacent — not engine code, but operationally aligned. **The endpoint uses `structuredLogger` voluntarily** even though the source-scan doesn't enforce it there. The architectural boundary: legacy `logger` remains valid for non-engine API code (existing services that haven't migrated); engine and engine-adjacent operational code (snapshots, future heartbeat/event-subscription endpoints) use `structuredLogger`. Future operational endpoints in `server/src/routes/` that need structured observability can opt in voluntarily without expanding the source-scan ban scope. Decision Log captures the boundary so subsequent contributors know which logger to use. |
| 2026-05-07 | `apiClient` statusCode surfacing deferred from 7b (chunk 11g.7 sub-step 7b) | The current `apiClient` doesn't surface HTTP status codes on errors; it only returns `{error: string}`. The chunk 11g.5a `reduce` function classifies errors via the `statusCode` field on the dispatched event payload (4xx → fatal, 5xx → transient retry). For 7b, the snapshot fetcher's caught error doesn't carry `statusCode`, so all failures fall through the message-pattern path (fatal-vs-transient based on string matching). **This is acceptable for 7b minimal scope** because snapshot fetch is rare and existing error-message conventions handle the common cases. If real production data shows mis-routing of 4xx vs 5xx, enhance `apiClient` at that point as a focused future commit. Not blocking 7b's operational utility. |
| 2026-05-07 | Snapshot reconstruction in standalone helper service (chunk 11g.7 sub-step 7b) | New module `server/src/services/snapshotService.ts` exports `buildSnapshot(leagueId, supabase): Promise<DraftSnapshot \| null>`. Pure function (modulo DB I/O); doesn't depend on engine code. Reads `leagues` for config, `draft_events` (200 most recent, reverse-sorted) for ring buffer, `draft_picks_v2` + `draft_order` for snake/linear state, `auction_nominations` + `auction_budgets` for auction state. Translates `draft_events.event_type` rows into the wire's `BufferedDraftEvent` discriminated union via per-type translators that mirror the engine's bootstrap apply-handlers. **Reusable in chunk 11g.7 sub-step 7c** (snapshot persistence) when the engine snapshots its own state to a `draft_snapshots` projection table for fast bootstrap — the same `buildSnapshot` helper backs both the HTTP endpoint and the persistence path. Cleanly testable with a mocked Supabase client; ~80 LOC of straight-line DB queries + translations. |
| 2026-05-07 | Client `fetchSnapshot` parameter rename: leagueId → draftId (chunk 11g.7 sub-step 7b) | Pre-7b: `defaultFetchSnapshot(leagueId)` in `apps/web/src/lib/draftClient/runner.ts:497`. Post-7b: `defaultFetchSnapshot(draftId)`. The data model treats them as identical (`leagueId === draftId` per the discovery endpoint header comment), so this is purely naming hygiene to match the URL path `/api/drafts/:draftId/snapshot`. The runner's call site at `runFetchSnapshot` was updated to pass `this.params.draftId` (instead of `this.params.leagueId`). Test helper signature updated to match. Visible to tests but transparent in production. |
| 2026-05-07 | **Chunk 11g.7 sub-step 7b complete.** | HTTP snapshot endpoint for client resync lands as a single commit on `phase-4-5-implementation`. New module `server/src/services/snapshotService.ts` exports `buildSnapshot(leagueId, supabase)` — pure DB-driven reconstruction of the wire `DraftSnapshot` shape. New route `GET /api/drafts/:draftId/snapshot` in `server/src/routes/drafts.ts` mirrors the discovery endpoint's auth pattern (Supabase JWT via `authMiddleware` + `LeagueMembershipService` check) + delegates to `snapshotService.buildSnapshot`. Status codes 200/400/401/403/404/409/500. Client-side: `apps/web/src/lib/draftClient/runner.ts` `defaultFetchSnapshot` placeholder replaced with real `apiClient.get<DraftSnapshot>` call; parameter renamed `leagueId` → `draftId` for naming hygiene. Closes the deferred-fetch gap from chunk 11g.5a's recon. 826 server tests pass (+8 new snapshot endpoint route tests covering 200/401/403/404/409/400/500/null-snapshot paths) + 84 web draft-client tests pass (+3 new 7b runner wiring tests for parameter rename + success/failure shapes). All 818 pre-7b server tests + 81 pre-7b web tests stay green. Decision Log captures: two-process topology + Path A choice rationale, snapshot consistency model + < 1 sec staleness boundary, auth-scheme-by-route-group principle, no operational machinery deferral, structuredLogger voluntary boundary in routes, apiClient statusCode deferral. **Next: chunk 11g.7 sub-step 7c (snapshot persistence — engine periodically snapshots in-memory state to a `draft_snapshots` projection table for fast bootstrap; reuses `buildSnapshot` helper from 7b).** |
| 2026-05-07 | Path C snapshot architecture: `snapshot_payload` (wire shape) + `engine_state` (orchestration fields) (chunk 11g.7 sub-step 7c) | Each `draft_snapshots` row carries TWO JSONB payloads: `snapshot_payload` is the wire `DraftSnapshot` shape from 7b's `buildSnapshot()` helper (same shape clients see over HTTP/WS) and `engine_state` is a tiny well-bounded JSONB with three engine-internal orchestration fields — `currentTimerKind` (the 7a discriminator), `pauseState` (with pausedTimerKind from 6c1+6c3), and `eventsSinceLastSnapshot` (the milestone-trigger continuity counter). Path A (DB-derived snapshot only) was insufficient because the engine's two-timer architecture has orchestration state not derivable from DB. Path B (engine in-memory serialization) would have duplicated the wire shape. Path C reuses `buildSnapshot()` for the wire portion AND captures only the truly engine-internal fields in a separate column. Tiny serialization surface; no duplication; future contributors know exactly which fields need engine_version migration when they evolve. |
| 2026-05-07 | DB-derived snapshot via `buildSnapshot()` reuses 7b helper (chunk 11g.7 sub-step 7c) | Engine has no "in-flight in-memory state not yet in DB" — all state mutations go through RPCs (`submit_pick_v2`, `nominate_player_v2`, `place_bid_v2`, etc.) which atomically update projection tables (`draft_picks_v2`, `auction_nominations`, `auction_budgets`). The engine-internal fields (timer kinds, pauseState, milestone counter) are pure orchestration; they're not derivable from DB but they're not "data" either. **DB-derived snapshot is correct**: it eliminates serialization duplication; the snapshot is consistent with the wire shape clients see; engine_state captures only what can't be re-derived. The HTTP snapshot endpoint and the persistence-layer snapshot are the same JSONB payload. |
| 2026-05-07 | RLS enabled but no policies on `draft_snapshots` (chunk 11g.7 sub-step 7c) | Engine writes via service_role (bypasses RLS); no user-facing read path in 7c. The lack of policies is **intentional, not an oversight**. Future commits that expose snapshots to users (e.g., commissioner audit UI showing the engine's snapshot history) add the appropriate read policy at that point. SQL comment on the table documents this for future contributors. |
| 2026-05-07 | `ENGINE_SNAPSHOT_VERSION` bump policy (chunk 11g.7 sub-step 7c) | Constant in `server/src/draft/snapshotPersistence.ts`, initial value 1. **Bump when the `engine_state` JSONB shape changes in a way that prior-version deserialization would silently corrupt state.** Adding a new optional field is OK without bumping (deserialization handles missing fields gracefully via nullish defaults). Removing or renaming a field, OR changing a discriminator's value space (e.g., adding a new `currentTimerKind` enum value), requires a bump. Mismatch → bootstrap WARN log with structured `reason: 'version_mismatch'` + fall back to full event-replay. The constant's JSDoc is the canonical reference for contributors evolving the engine state shape. |
| 2026-05-07 | Snapshot cadence: 30s + every-50-events + lifecycle (chunk 11g.7 sub-step 7c) | Three trigger sources, all queue-routed through the single-writer queue: (1) **periodic** every `SNAPSHOT_INTERVAL_MS` (env, default 30000ms) via `setInterval` per active lobby; (2) **milestone** every `SNAPSHOT_EVENT_MILESTONE` events (env, default 50) — incremented by `appendEventAndCount` wrapper around runtime `events.append` calls; (3) **lifecycle** on draft completion + cancellation. Skip during pause (no new state to capture; previous snapshot remains current). Tests pass `SNAPSHOT_INTERVAL_MS=0` and `SNAPSHOT_EVENT_MILESTONE=0` via the vitest setup file to disable both timers; specific tests trigger via `lobby.scheduleSnapshot()` directly. |
| 2026-05-07 | Snapshot generation routes through single-writer queue (chunk 11g.7 sub-step 7c) | `scheduleSnapshot()` chains a promise onto `this.queue` (same pattern as 6c1's `pauseAuction` and 6c4's `commissionerOverride`), not a new `DraftAction` variant. Snapshot generation latency (~100ms DB write) contributes to action processing latency for actions queued during snapshot. At 30s cadence, this is ~0.3% of wall-clock time — well within tolerance for 7c. **Future load-test (chunk 11g.11) revisits if it surfaces a real concern**; possible mitigation would be moving snapshot generation off the single-writer queue and into a separate async path that reads (but doesn't mutate) state. Today's tradeoff favors atomicity over latency. |
| 2026-05-07 | `appendEventAndCount` runtime/replay distinction (chunk 11g.7 sub-step 7c) | New private method on `LobbyManager`: wraps `this.events.append(event)` with `this.eventsSinceLastSnapshot++` + milestone-threshold check. **Use this method for runtime event emissions (action processing).** DO NOT use during bootstrap event-replay — replay should call `this.events.append` directly so `eventsSinceLastSnapshot` reflects only post-bootstrap activity in the milestone counter. 11 runtime callsites refactored (`processSubmitPick`, `processNominate`, `processPlaceBid`, `processPlaceBid` extends-timer, `handleNominationTimeout` close + no-sale, `handleNominationWindowTimeout`, `fireAutoSkipEvent`, `processPauseAuction`, `processResumeAuction`, `fireCommissionerOverrideRpc`); 14 replay callsites in `apply*Event` handlers preserved as `events.append` directly. JSDoc on the wrapper is the contributor reference. |
| 2026-05-07 | Retention pruning: 5 most recent for in-progress, ALL for completed/cancelled (chunk 11g.7 sub-step 7c) | After each successful snapshot write, `writeSnapshot` runs retention: in-progress drafts keep only `RETENTION_IN_PROGRESS=5` most recent; completed/cancelled drafts keep ALL snapshots for audit trail. Pruning gated on the engine's `draftStatus` at write-time (passed into `writeSnapshot` params). **Completed/cancelled drafts accumulate ~4 snapshots indefinitely (mild long-term waste).** Future consolidation job could collapse completed drafts to single audit snapshots — 7c defers this work; revisit when total snapshot row count becomes operationally relevant. Pruning is two-step (read keep-list, then delete the rest) because PostgREST/Supabase doesn't expose subquery DELETE; pruning failure is non-fatal (mild row growth). |
| 2026-05-07 | Bootstrap fallback to full event-replay on ANY snapshot issue (chunk 11g.7 sub-step 7c) | The `bootstrap()` method tries snapshot+delta replay first. Falls back to the existing `bootstrapFullEventReplay()` (chunk 11g.4 step 6b's canonical bootstrap path) on ANY of: missing snapshot (first-deploy), `engine_version` mismatch, `lastAppliedSeq > currentMaxSeq` (snapshot ahead of log — impossible state), `lastAppliedSeq + 1 < currentMinSeq` (snapshot older than oldest event — would miss intermediate events), `engine_state` shape invalid (missing required fields, unknown discriminator value), `snapshot_payload` shape invalid (missing required fields). Each failure logs `snapshot.bootstrap.fallback_full_replay` (warn) with a structured `reason` discriminator (`version_mismatch \| seq_ahead_of_log \| seq_below_oldest_event \| payload_deserialization_failed \| engine_state_invalid`) so alert policies can distinguish "expected version mismatch after deploy" (transient high-frequency) from "snapshot ahead of event log" (genuine bug). Belt-and-suspenders: chunk 11g.4 step 6b path remains the canonical bootstrap. |
| 2026-05-07 | Fallback structured `reason` discriminator for observability (chunk 11g.7 sub-step 7c) | The `snapshot.bootstrap.fallback_full_replay` (warn) event carries a structured `reason` field with one of five discriminator values: `version_mismatch`, `seq_ahead_of_log`, `seq_below_oldest_event`, `payload_deserialization_failed`, `engine_state_invalid`. Same event name across all reasons (one alert subscription); structured filter on reason value distinguishes operational concerns. Example alert policies: `reason='version_mismatch'` → throttle alerts (transient post-deploy spike, then silent); `reason='seq_ahead_of_log'` → page on first occurrence (genuine bug indicator). The `validateSnapshotForBootstrap` helper returns this discriminator as a typed `SnapshotBootstrapValidation` discriminated union. Test suite regression-locks each reason value via `snapshotPersistence.test.ts`. |
| 2026-05-07 | **Chunk 11g.7 sub-step 7c complete.** | Snapshot persistence for engine restart optimization lands as a single commit on `phase-4-5-implementation`. New migration `20260507050000_draft_snapshots.sql` creates the `draft_snapshots` table with `snapshot_payload` JSONB (wire shape from 7b's `buildSnapshot()`) + `engine_state` JSONB (orchestration fields) + `engine_version` integer + retention-driven indexing. RLS enabled, no policies (engine writes via service_role bypass; future user-facing reads add policies). New module `server/src/draft/snapshotPersistence.ts` exports `ENGINE_SNAPSHOT_VERSION` constant (initial value 1) + `serializeEngineState`/`deserializeEngineState` round-trip helpers + `validateSnapshotForBootstrap` with typed `SnapshotBootstrapValidation` discriminated union + `readMostRecentSnapshot` + `writeSnapshot` (with retention pruning) + `findMaxEventSeq`/`findMinEventSeq` helpers. `LobbyManager`: new `appendEventAndCount` runtime wrapper (refactored 11 callsites; replay handlers keep `events.append`); new `eventsSinceLastSnapshot` counter; new `processSnapshot` queue-routed handler with skip-during-pause + skip-not-started early returns; new `scheduleSnapshot` public entry; new `startSnapshotTimer`/`stopSnapshotTimer` lifecycle hooks (wired into `init` and `shutdown`); `bootstrap()` extended to try snapshot+delta first via `applySnapshot` + `applyEventDuringBootstrap` + delta `listDraftEvents(sinceSeq)`, falling back to existing `bootstrapFullEventReplay` (renamed from `bootstrap`) on any snapshot issue. Vitest setup file disables both snapshot timers (`SNAPSHOT_INTERVAL_MS=0`, `SNAPSHOT_EVENT_MILESTONE=0`) so fake-timer-using tests don't trip on the periodic `setInterval`. 841 server tests pass (+15 net 7c tests: +12 unit tests in `snapshotPersistence.test.ts` covering serialize/deserialize round-trip + all five validation reason discriminators + seq-bounds invariants + tolerance for missing optional fields + unknown enum-value normalization, +3 LobbyManager integration tests covering snapshot fallback + scheduleSnapshot no-op during not_started + Promise-resolution shape). All 826 pre-7c server tests + 84 web draft-client tests stay green (canonical-replay principle preserved; chunk 11g.4 step 6b path remains the authoritative bootstrap on any snapshot issue). **Foundation for chunks 11g.7 7d/7e**: 7d (heartbeat) emits structured logs that reuse the same `correlationId`/`leagueId`/`lobbyId` schema from 7a + persists last-seen timestamps that may surface in future snapshot extensions; 7e (live event subscription) reuses the `engine_version` migration-safety pattern for cross-process event delivery shape changes. **Next: chunk 11g.7 sub-step 7d (heartbeat / keepalive).** |
| 2026-05-07 | **Hybrid heartbeat architecture: uWS native pings + application-level custom close code (chunk 11g.7 sub-step 7d).** | Three architectures considered for WebSocket zombie-connection cleanup: pure uWS native (`sendPingsAutomatically: true` + `idleTimeout`), pure application-level (per-connection `setInterval` schedules manual `ws.ping()` + tracks `lastPongAt`), or hybrid. **Recommended and implemented hybrid** (override of the recon brief's pure-application-level proposal): uWS handles ping emission internally via `sendPingsAutomatically: true` (browsers automatically pong any incoming ping per RFC 6455, so the application doesn't need to drive ping cadence); application-level `pong:` handler updates `lastPongAt` for observability; application-level server-wide soft-check timer scans all connections every 10s and force-closes connections older than `HEARTBEAT_PONG_TIMEOUT_MS` with custom close code 4002. uWS's `idleTimeout: 60` (= 2× the application timeout, rounded to uWS's 4s granularity) is the defense-in-depth backstop that fires if our application timer ever fails. Eliminates the brief's per-connection ping schedule + jitter complexity entirely (uWS distributes pings internally; no synchronized-spike concern at the application layer). Custom close code lets the client distinguish heartbeat disconnects from generic 1006 in alert policies and reconnects with backoff (transient classification) rather than treating it as terminal auth failure. |
| 2026-05-07 | Heartbeat defaults: 30s ping interval + 30s pong timeout; uWS idleTimeout=60s backstop (chunk 11g.7 sub-step 7d) | `HEARTBEAT_PING_INTERVAL_MS=30000` (default 30s — feeds into uWS's `idleTimeout` as `ceil(intervalSec * 2)` = 60s rounded up to uWS's 4s minimum granularity). `HEARTBEAT_PONG_TIMEOUT_MS=30000` (default 30s — application-level soft-check timer's threshold). Soft-check cadence at `min(pongTimeoutMs / 3, 10000)` ms = 10s for default config. **Mobile-network-friendly**: 30s windows survive carrier-network handoff blips (typical 5-15s); zombies are reclaimed within ~40s of breaching. Layered defense: application-layer 30s soft check + uWS 60s idle backstop. Both env-overridable for ops tuning; setting either to 0 disables the soft-check timer entirely (vitest setup default). |
| 2026-05-07 | Single server-wide soft-check timer (chunk 11g.7 sub-step 7d) | Pure-application-level approaches typically use per-connection `setInterval` schedules — and would need per-connection jitter to avoid synchronized network spikes. The hybrid architecture eliminates both: uWS distributes pings internally (no synchronized-spike concern), so the application layer needs only ONE server-wide `setInterval` that scans `lobbyRegistry.forEachConnection`. Connection state (`lastPongAt`) lives on `DraftSocketUserData`; the scanner reads it and force-closes timeouts. CPU overhead is `O(connections)` per scan = trivial at expected scale (12-team draft × ~12 connections × 12 simultaneous lobbies ≈ ~150 connections / 10s scan cadence = negligible). |
| 2026-05-07 | `LobbyRegistry.forEachConnection` snapshot-then-iterate semantics (chunk 11g.7 sub-step 7d) | New iterator on `LobbyRegistry` (delegates to per-lobby `LobbyManager.forEachConnection`). **Iteration safety contract**: snapshots each lobby's `connections` map at iteration-start so removing a connection mid-scan (e.g., the soft-check force-disconnect itself triggering a synchronous uWS `close` handler that calls back into `removeConnection`) is safe — does not throw, does not skip remaining entries. Lobbies in flight (`Promise<LobbyManager>` placeholders during construction) are skipped — they have no connections yet. Errors thrown by the callback propagate; callers (the soft-check scanner) `try/catch` per-connection so one misbehaving connection doesn't abort the entire scan. |
| 2026-05-07 | Close code 4002 carve-out in `classifyCloseCode` (chunk 11g.7 sub-step 7d) | Pre-7d: 4001-4099 → `permanent_auth` (terminal — user must re-authenticate). 4002 was originally proposed by the brief but conflicted with the existing range. **Recommended and implemented carve-out**: explicit `if (code === 4002) return 'transient'` BEFORE the 4001-4099 range check. 4001 (token expired) and 4003-4099 stay `permanent_auth`; 4002 specifically reserved for heartbeat pong-timeout — the disconnect represents a network/keepalive failure, not an auth problem, so the client reconnects with backoff. Test regression-locks the carve-out (`code: 4002 → 'transient'`) and the surrounding boundaries (`code: 4001 → 'permanent_auth'`, `code: 4003 → 'permanent_auth'`). Reserved-codes header comment added to `closeCodes.ts` documenting the 4xxx allocation table to prevent collisions if future close-code work adds new 4xxx codes. |
| 2026-05-07 | Heartbeat logging volume strategy (chunk 11g.7 sub-step 7d) | Only `heartbeat.pong_timeout` is INFO+ visible (warn level — alert-worthy if frequency spikes). `heartbeat.pong_received` (debug — per-pong observability), `heartbeat.scan_completed` (debug — scan diagnostic), and `heartbeat.timer_started`/`heartbeat.timer_stopped` (info — lifecycle, low frequency) round out the event vocabulary. **No application-level `ping_sent` event emit**: uWS sends pings internally, so the application has no observable "ping sent" moment — emitting one would require intercepting uWS's internal ping schedule, which the public API doesn't expose. Production noise floor at INFO+ stays low (only timeouts surface); debug builds get full per-pong tracing. |
| 2026-05-07 | Tab-visibility no-op for 7d (chunk 11g.7 sub-step 7d) | Browser-backgrounded tabs may throttle JavaScript timers but **the WebSocket stays open and the browser still pongs incoming pings automatically** (this is at the protocol layer, not at the JavaScript layer). Chunk 11g.5a's runner already handles user-initiated visibility changes via `visibilitychange` listener. **No 7d-specific tab-visibility code needed.** Boundary: keepalive lives at the protocol layer (browser-native pong); UX-side visibility responses live at the application layer (chunk 11g.5a's runner). |
| 2026-05-07 | Pure-helper extraction for testability (chunk 11g.7 sub-step 7d) | New module `server/src/draft/heartbeat.ts` exports pure functions: `findTimedOutConnections` (snapshot + threshold check), `recordPong` (update lastPongAt), `initializeHeartbeat` (stamp lastPongAt on open), and `HEARTBEAT_PONG_TIMEOUT_CLOSE_CODE` constant. Tests mock `HeartbeatWebSocket` interface (just `getUserData()` + `end()`); no uWS dependency. **Mirrors chunk 11g.5a's `lobbyTimerReducer` / `lobbyClockRunner` separation** (testable logic separate from side-effect-bound runtime) and the `autopickStrategy.ts` / `auctionAutoNominateStrategy.ts` chain-of-strategies pattern. uWS integration in `uws-server.ts` is thin glue: configures uWS heartbeat options, runs the soft-check timer, calls the pure helpers. **No uWS integration tests in 7d** — uWS test infrastructure investment doesn't pencil for the marginal coverage value over pure-function tests; revisit if real integration bugs surface. |
| 2026-05-07 | `stopHeartbeat()` lifecycle hook on shutdown (chunk 11g.7 sub-step 7d) | The soft-check timer is a `setInterval` whose late firing during `SIGTERM` graceful-shutdown adds noise to the shutdown window and can race against teardown (e.g., scanning a `LobbyRegistry` whose connections are mid-tear-down). `UwsServerHandle` exports `stopHeartbeat()` parallel to `close()`; the engine entry point's `shutdown()` function calls it BEFORE closing the listen socket. Mirrors the `LobbyManager.stopSnapshotTimer()` pattern from chunk 11g.7 sub-step 7c. The scanner's timer is also `unref()`'d so it doesn't keep the Node event loop alive on its own — defense-in-depth in case `stopHeartbeat()` is somehow not called during teardown. Idempotent: a second `stopHeartbeat()` call is a no-op. |
| 2026-05-07 | Coexistence with chunk 11g.4 step 5 backpressure path (chunk 11g.7 sub-step 7d) | Backpressure path (chunk 11g.4 step 5's 1 MiB threshold) uses close code 1013 ("Try Again Later"). Heartbeat path uses close code 4002. **Two independent mechanisms**: backpressure operates on `ws.getBufferedAmount()` per-broadcast; heartbeat operates on `lastPongAt` on a periodic scan cadence. First one to fire wins; after one fires, the connection is closed and the other becomes a no-op. No observable interaction; no test coverage of the cross-mechanism interaction is needed (each is independently tested in its own suite). Documented here so future contributors understand both close paths exist by design. |
| 2026-05-07 | **Chunk 11g.7 sub-step 7d complete.** | WebSocket heartbeat for zombie connection cleanup lands as a single commit on `phase-4-5-implementation`. New module `server/src/draft/heartbeat.ts` exports `HEARTBEAT_PONG_TIMEOUT_CLOSE_CODE` constant (4002) + `HeartbeatWebSocket` interface + `initializeHeartbeat`, `recordPong`, `findTimedOutConnections` pure helpers. `DraftSocketUserData` extended with `lastPongAt: number` field (numeric for cheap hot-path subtraction). `LobbyManager.forEachConnection` snapshots its connection map for safe mid-iteration mutation. `LobbyRegistry.forEachConnection` delegates to per-lobby iterators with snapshot-then-iterate semantics. `uws-server.ts` configures uWS `sendPingsAutomatically: true` + `idleTimeout: 60` (defense-in-depth backstop), adds `pong:` handler that records pong timestamps, runs a server-wide soft-check timer at `min(pongTimeoutMs / 3, 10000)` ms cadence that force-closes timed-out connections with code 4002, and exports `stopHeartbeat()` lifecycle hook. Engine `shutdown()` calls `stopHeartbeat()` before closing the listen socket (mirrors `stopSnapshotTimer()` pattern from 7c). Vitest setup defaults `HEARTBEAT_PING_INTERVAL_MS=0` + `HEARTBEAT_PONG_TIMEOUT_MS=0` so fake-timer tests don't trip on the soft-check `setInterval`. Client `apps/web/src/lib/draftClient/closeCodes.ts` carves out `code: 4002 → 'transient'` BEFORE the 4001-4099 → permanent_auth range check; reserved-codes header comment documents the 4xxx allocation table. ~10 new tests: 9 pure-function tests in `heartbeat.test.ts` (empty input, no timeouts, single timeout, multi-lobby, exactly-at-boundary strict-inequality, disabled, defensive negative, WS reference identity preservation, close-code constant value) + 1 regression lock in `closeCodes.test.ts` (4002 → transient + boundary tests for 4001/4003 → permanent_auth). All pre-7d server + web draft-client tests stay green (backward-compat preserved). **Foundation for chunk 11g.7 sub-step 7e** (live event subscription): heartbeat timestamps are forward-compat for cross-process event-delivery health monitoring; the server-wide soft-check timer pattern is reusable for cross-process subscription liveness if needed. **Next: chunk 11g.7 sub-step 7e (live event subscription).** |
| 2026-05-11 | **LISTEN/NOTIFY as primary cross-process event mechanism (chunk 11g.7 sub-step 7e).** | Three mechanisms considered: pure Postgres LISTEN/NOTIFY, Supabase Realtime (websocket pub/sub over logical replication, already used in this codebase for client-facing fanout at `DraftServiceV2.ts:725` + `routes/draft.ts:34`), and polling. **Recommended and implemented LISTEN/NOTIFY** on semantic + operational grounds: native to Postgres (no new infrastructure), sub-100ms delivery, transaction-coupled (no notifications fire on rolled-back transactions), engine-to-engine semantically distinct from Realtime's client-to-server purpose. Realtime is the established Citrus primitive for "engine pushes events to clients"; using it for "engine subscribes to events written by other processes" overloads its semantic and adds websocket roundtrip latency. Polling rejected on latency grounds (5-10s pulse vs sub-100ms LISTEN). Belt-and-suspenders correctness: chunk 11g.7 sub-step 7c's snapshot+delta bootstrap catches any notifications missed during engine downtime, so LISTEN/NOTIFY is a runtime optimization not a correctness foundation. |
| 2026-05-11 | Raw `pg` client (v8.19.0, already a dep) over Supabase JS for LISTEN (chunk 11g.7 sub-step 7e) | `@supabase/supabase-js` exposes PostgREST + Realtime but NOT raw LISTEN/NOTIFY. The engine needs its own `pg.Client` connection (dedicated to LISTEN; separate from the Supabase JS admin client used for PostgREST queries elsewhere). `pg` v8.19.0 is already a transitive dependency (`node_modules/pg/package.json`), so no `package.json` additions. Pure helpers + lifecycle in new module `server/src/draft/eventSubscription.ts`. |
| 2026-05-11 | `SUPABASE_DB_URL` env var + direct-connection requirement (chunk 11g.7 sub-step 7e) | New env var `SUPABASE_DB_URL` (matches the convention established in `scripts/maintenance/archive_to_csv.py`) — falls back to `DATABASE_URL` if `SUPABASE_DB_URL` is unset. **MUST be a direct Postgres connection, not pooled** — PgBouncer / Supabase pooled connections do not support LISTEN frames. Misconfiguring this is the most likely 7e deploy failure mode. **Operational diagnostic**: the engine fires a synthetic NOTIFY (`{"_test":true}`) within ~10ms of LISTEN registration and waits up to 5 seconds for receipt; if it doesn't arrive, logs ERROR `event_subscription.self_test_failed` with explicit remediation text ("verify SUPABASE_DB_URL is a direct connection — pgbouncer / Supabase pooled connections drop LISTEN frames"). Catches misconfiguration immediately rather than shipping a silently-broken cross-process notification path. |
| 2026-05-11 | **Trigger-based NOTIFY emission (override of brief's 11-RPC CREATE OR REPLACE) (chunk 11g.7 sub-step 7e)** | Recon-time plan: CREATE OR REPLACE FUNCTION for each of 11 RPCs that write `draft_events`, adding `PERFORM pg_notify(...)` at the end of each function body (recon Item 12 + brief's "Single migration with 11 CREATE OR REPLACE FUNCTION statements"). **Override during execution**: single `AFTER INSERT` trigger on `draft_events` instead. Each `CREATE OR REPLACE FUNCTION` requires the full function body restated for a 1-line addition; 11 RPCs would have produced ~3000-line migration. Trigger approach is ~30 lines + automatically covers future RPCs that write `draft_events` (no "shipped a new RPC but forgot pg_notify" failure mode). Commit-coupling is identical (trigger runs in transaction; NOTIFY queued for COMMIT, discarded on ROLLBACK — Postgres semantics). Trade-off: cannot selectively skip event types (e.g., suppress NOTIFY for `generation_bumped`), but engine's `lastAppliedSeq` dedup gate short-circuits already-applied events cheaply (~1ms per no-op). Single source of NOTIFY logic in `draft_events_notify_trigger()`. |
| 2026-05-11 | NOTIFY semantics correction: `PERFORM pg_notify(...)` INSIDE the function body, not "after COMMIT" (chunk 11g.7 sub-step 7e) | The recon brief said "NOTIFY happens AFTER the COMMIT block, not inside, to ensure delivery only on successful commit." **This framing is incorrect for plpgsql** — `pg_notify(channel, payload)` INSIDE the function body is automatically commit-coupled. Postgres queues notifications until the enclosing transaction commits; on rollback they're discarded. There is no "after COMMIT" inside an RPC function — the function IS the transaction. The trigger function `draft_events_notify_trigger()` runs `PERFORM pg_notify(...)` as part of its body; delivery is automatic at COMMIT. Override of brief's wording; same semantic outcome. |
| 2026-05-11 | Single global channel `draft_events` + JS-side per-league filter (chunk 11g.7 sub-step 7e) | Per-league channels (`draft_events_{leagueId}` with dynamic LISTEN/UNLISTEN on lobby load/unload) considered but deferred to Stage 3 (multi-VM MIG). Day 1 single-VM scale: ~12 active drafts × handful of events/sec = trivial volume; JS-side filter cost is ~1ms per "not my lobby" no-op skip. Single LISTEN reduces engine startup complexity, simplifies reconnect logic, and shrinks the migration footprint to one trigger. Migration to per-league channels is straightforward at Stage 3 if filter cost becomes meaningful: add `LISTEN draft_events_{leagueId}` per loaded lobby, `UNLISTEN` per evicted lobby. |
| 2026-05-11 | Minimal NOTIFY payload `{league_id, seq}` + engine fetches details (chunk 11g.7 sub-step 7e) | Payload includes ONLY the leagueId + seq (~70 bytes). Engine fetches the full event row via existing `DraftServiceV2.listDraftEvents(leagueId, sinceSeq=lastAppliedSeq)`. Rationale: Postgres NOTIFY payload limit is 8000 bytes — embedding the full event payload would risk hitting the cap on large auction overrides and adds JSON marshaling overhead at every INSERT. Engine round-trip for fetch adds ~5-20ms which is acceptable for cross-process delivery (the bound is bootstrap on disconnect anyway). Self-test sentinel `{"_test":true}` is filtered before normal dispatch — the subscription handler uses a raw-text contains-check to detect the sentinel without confusion with real payloads. |
| 2026-05-11 | `LobbyManager.lastAppliedSeq` class field + `appendEventAndCount` advancement (chunk 11g.7 sub-step 7e) | New private class field `lastAppliedSeq: number = 0` on `LobbyManager`. Initialized from `snapshotRecord.lastAppliedSeq` in `applySnapshot`; advanced monotonically in `applyEventDuringBootstrap` (every replayed event); advanced in `appendEventAndCount` (every runtime engine emission). **Hooking the cursor update into `appendEventAndCount` is load-bearing** — every runtime path (`processSubmitPick`, `processNominate`, `processPlaceBid` including extends-timer double-event, `processPauseAuction`, `processResumeAuction`, `processCommissionerOverride`, `fireAutoSkipEvent`, `handleNominationTimeout`, `fireAutoNominateAction`) funnels through `appendEventAndCount`. Single source of truth; no per-callsite updates; future RPCs that emit events get correct dedup behavior for free. The 3 lifecycle RPCs (`draft_pause`/`draft_resume`/`draft_extend`) that don't call `appendEventAndCount` (they trigger `processExternalEvent` indirectly via the NOTIFY bounce on bootstrap-replay or runtime apply) update `lastAppliedSeq` via `applyEventDuringBootstrap` instead. |
| 2026-05-11 | 3 lifecycle RPCs modified to return `seq` (chunk 11g.7 sub-step 7e) | Recon discovered 8 of 11 RPCs already include `seq` in their `jsonb` return (the v2 RPCs that own their own `INSERT INTO draft_events` block, capturing `RETURNING seq INTO v_seq`). 3 lifecycle RPCs (`draft_pause`/`draft_resume`/`draft_extend`) use `PERFORM append_draft_event(...)` which returns void to the caller's PERFORM context. **Migration modifies these 3**: changes the second `PERFORM` call (the lifecycle event — `draft_paused`/`draft_resumed`/`draft_extended`) to `SELECT (append_draft_event(...) ->> 'seq')::bigint INTO v_seq`, captures seq into the local variable, adds `seq` to the returned jsonb. The first `PERFORM` call (the `generation_bumped` scaffolding event) is intentionally NOT modified — its seq is engine-irrelevant; the canonical lifecycle event seq is the second one. |
| 2026-05-11 | `enqueueExternalEvent` queue-routed via existing `this.queue` chain (chunk 11g.7 sub-step 7e) | New public method `enqueueExternalEvent(seq: number): Promise<void>` on `LobbyManager`; routes through the same `this.queue.then(() => this.processExternalEvent(seq))` chain as `pauseAuction`/`resumeAuction`/`commissionerOverride` from chunk 11g.6 sub-steps 6c1/6c4. Preserves the chunk 11g.4 step 2 / ADR-002 §3.5 race-free invariant: external events serialize with in-flight engine-authored actions; no concurrent state mutation. Dispatch callback (in `server/src/draft/index.ts`) calls `lobbyRegistry.get(leagueId)` and `await lobby.enqueueExternalEvent(notification.seq)`. |
| 2026-05-11 | Lobby-load forbidden on NOTIFY — resource-exhaustion protection (chunk 11g.7 sub-step 7e) | Dispatch callback's `lobbyRegistry.get(leagueId)` returns `undefined` if no LobbyManager exists for the league (no client has connected; bootstrap hasn't run). **Engine MUST NOT spin up a lobby to apply the event.** Doing so would create an attack vector: every external NOTIFY firing a lobby load = unbounded resource consumption (DB queries, in-memory state, timer registrations). Lobbies load lazily on WS connect (chunk 11g.4 step 4); the missed event surfaces at bootstrap time via snapshot+delta from chunk 11g.7 sub-step 7c. Unknown leagueId logs `event_subscription.event_skipped_unknown_lobby` (debug) and ignores. The dispatch callback's JSDoc documents this as the explicit security boundary. |
| 2026-05-11 | Reconnect with exponential backoff 3/6/12/24/48/60s cap (chunk 11g.7 sub-step 7e) | LISTEN connection drop fires `pg.Client.on('error')`; the subscription schedules reconnect via `computeReconnectDelayMs(attemptNumber)` — 3s → 6s → 12s → 24s → 48s → 60s cap. No jitter (single-engine deployment has no thundering-herd concern). On successful reconnect: `event_subscription.connection_restored` (info) with `downtimeMs` + `attemptsTaken`. **No explicit "re-bootstrap advisory"** to existing lobbies; bootstrap is the correctness foundation, reconnect is runtime-only. |
| 2026-05-11 | Extended-outage acceptability boundary (chunk 11g.7 sub-step 7e) | During the reconnect backoff window, runtime cross-process notifications are missed. For typical 3-second reconnect this is invisible. For rare extended outages (60s cap, indefinite repeated failures), commissioner actions taken during that window won't fire on the engine UNTIL (a) the engine's client triggers WS reconnect, which fires bootstrap, which reads all events including the missed ones, OR (b) the LISTEN connection itself reconnects and future notifications resume — but the missed events from the gap are NOT retroactively delivered (Postgres LISTEN doesn't queue). **Acceptable** given rarity + bootstrap correctness foundation; documented explicitly so future operators understand the boundary. If reliability requirements grow, options include: (1) shorten the backoff cap, (2) trigger explicit re-bootstrap on connection_restored, (3) move to logical replication slot (durable, queues missed messages, more operational overhead). |
| 2026-05-11 | Post-deploy verification checklist embedded in migration header (chunk 11g.7 sub-step 7e) | Migration `20260511000000_draft_events_notify.sql` includes an explicit checklist in its SQL comment header: (1) verify SUPABASE_DB_URL is direct (not pooled), (2) restart engine process, (3) check startup logs for `event_subscription.started` within 5 seconds, (4) trigger any RPC and check for `event_subscription.notification_received` within 1 second. Prevents "deployed migration but notifications don't fire" troubleshooting marathons — operator runs the checklist; if any step fails, the next step is unambiguous. |
| 2026-05-11 | **Closes chunk 11g.6 sub-step 6c1 deferred cross-process notification.** | The chunk 11g.6 sub-step 6c1 work (auction pause/resume) introduced the cross-process notification gap: commissioner UI fires `auction_pause_v2` RPC against the main API server, but the persistent engine process had no way to observe the event without WS reconnect. 6c1 explicitly deferred this to chunk 11g.7 sub-step 7e. **7e closes this gap.** Commissioner pause/resume + commissioner override actions now propagate to engine state within ~100ms via the LISTEN/NOTIFY path. WS reconnect via bootstrap (chunk 11g.7 sub-step 7c snapshot+delta) remains the belt-and-suspenders correctness guarantee. |
| 2026-05-11 | **Chunk 11g.7 sub-step 7e complete. Chunk 11g.7 functionally complete. Phase 4.5 chunks 11g.0–11g.7 all complete.** | Live event subscription path via Postgres LISTEN/NOTIFY lands as a single commit on `phase-4-5-implementation`. New migration `20260511000000_draft_events_notify.sql` adds the `draft_events_notify_trigger()` function + `AFTER INSERT` trigger on `draft_events` (every INSERT fires `pg_notify('draft_events', {league_id, seq})`) — covers all 11 RPCs that write `draft_events` (current + future). `CREATE OR REPLACE` for the 3 lifecycle RPCs (`draft_pause`/`draft_resume`/`draft_extend`) to add `seq` to their returned jsonb (engine dedup requires runtime emissions to advance `lastAppliedSeq`). New module `server/src/draft/eventSubscription.ts`: pure helpers (`parseNotificationPayload`, `shouldApplyEvent`, `computeReconnectDelayMs`, `HEARTBEAT_PONG_TIMEOUT_CLOSE_CODE`-equivalent constant `HEARTBEAT_PONG_TIMEOUT_CLOSE_CODE` not used here — actual code is `4002` from 7d) + side-effectful `startEventSubscription` with pg.Client lifecycle, exponential reconnect, startup self-test. `LobbyManager`: new private `lastAppliedSeq: number = 0` field with monotonic advancement in `appendEventAndCount` (runtime) + `applyEventDuringBootstrap` (replay + external) + `applySnapshot` (snapshot bootstrap); new public `enqueueExternalEvent(seq)` method routed through the single-writer queue; new private `processExternalEvent(seq)` with dedup gate + `listDraftEvents` fetch + canonical-replay apply. `LobbyRegistry` extended via dispatch callback in engine entry point (`server/src/draft/index.ts`) — `startEventSubscription` is wired after registry initialization; subscription's `dispatch` calls `lobbyRegistry.get(leagueId)` + `lobby.enqueueExternalEvent(notification.seq)`. Engine `shutdown()` stops the subscription BEFORE the uWS heartbeat + close (no late-firing dispatches during teardown). Vitest setup defaults `EVENT_SUBSCRIPTION_DISABLED=1` (mirrors snapshot/heartbeat env-disable patterns from 7c/7d). 874 server tests pass (+21 net 7e tests: 17 in `eventSubscription.test.ts` covering parseNotificationPayload boundary cases + shouldApplyEvent + computeReconnectDelayMs + 5 startEventSubscription lifecycle tests with mock pg.Client, + 4 in `LobbyManager.test.ts` extension covering dedup gate / fetch+apply happy path / uninitialized no-op / empty-result race). All 853 pre-7e server tests stay green. Post-deploy verification checklist embedded in migration SQL header. **Closes chunk 11g.6 sub-step 6c1's deferred cross-process notification.** **Chunk 11g.7 functionally complete** — observability (7a), HTTP snapshot endpoint (7b), snapshot persistence (7c), heartbeat (7d), live event subscription (7e) all in place. **Phase 4.5 chunks 11g.0–11g.7 all complete.** Remaining chunks are productionization work: 11g.8 (rollout), 11g.9 (legacy autopick + pgmq removal per KI-009), 11g.10 (production deploy), 11g.11 (load test gate). Next: chunk 11g.8. |
| 2026-05-11 | **Phase 4.5 plan reorganization: 11g.8 ↔ 11g.11 swap; 11g.9 expands to absorb generation_bumped protocol cleanup; 11g.11 combines failure-mode + load test.** | Original `PHASE_4_5_PLAN.md` chunk decomposition: 11g.8 = failure-mode test suite, 11g.9 = Edge Function removal, 11g.10 = perf verification, 11g.11 = load test (added later via 2026-04-29 Decision Log). **Reorganized to**: 11g.8 = remove pgmq emissions from writer-side RPCs (writer cleanup), 11g.9 = Edge Function + pgmq queue/cron/extension + `generation_bumped` protocol cleanup (consumer + tables + protocol), 11g.10 = perf verification (unchanged), 11g.11 = combined failure-mode test suite + load test. **Rationale**: failure-mode tests are load-test-adjacent work that exercises the engine under conditions only meaningful once observability + legacy cleanup are done. Doing failure-mode tests now (pre-cleanup) would mean writing tests against an architecture that still has dead pgmq plumbing in it. The natural sequencing is: legacy cleanup (now-11g.8 writer / now-11g.9 consumer+protocol) → production rollout (11g.10 perf gate) → load test + failure mode tests (11g.11). The previously-net-new 11g.11 load test slot absorbs the relocated failure-mode tests; combining them is the right ordering — both run against the perf-baselined production-equivalent deployment. `PHASE_4_5_PLAN.md` chunk sections updated to match this layout. |
| 2026-05-11 | Chunk 11g.8 scope: writer-side only (pgmq emission removal from 4 RPCs) | Single migration `20260511010000_remove_pgmq_emissions.sql` `CREATE OR REPLACE`s four RPCs to remove the `PERFORM pgmq.send(...)` block (plus surrounding `v_send_delay` declaration + computation where exclusively used for the emission). Removed emitters: **(1)** `submit_pick_v2` (lines 904-923 of `20260425140000_draft_engine_v2_rpcs.sql`) — the next-pick deadline message + `v_send_delay`; **(2)** `draft_resume` (lines 376-396 of `20260511000000_draft_events_notify.sql`) — the fresh deadline after pause + `v_send_delay`; **(3)** `draft_extend` (lines 528-548 of same) — the extended deadline message + `v_send_delay`; **(4)** `draft_deadline_sweep` (lines 134-150 of `20260426130000_draft_engine_v2_phase3_sweep.sql`) — the safety-net re-emit inside the IF-expired branch. Each RPC's other behavior preserved: idempotency, actor authorization, atomicity contracts, the chunk 11g.7 sub-step 7e `draft_events_notify_after_insert` trigger (fires on durable INSERT, not pgmq — unaffected). `v_pick_time` and `v_new_deadline` retained — both still feed the `UPDATE leagues SET pick_deadline = ...` step that backs the engine's in-memory timer via `result.pick_deadline` in the RPC return. |
| 2026-05-11 | `generation_bumped` event-write Option B: defer protocol removal to 11g.9 | Three options considered: (A) remove both pgmq emit AND `generation_bumped` event in 11g.8 (cleanest single-chunk scope); (B) remove only pgmq emit in 11g.8, defer `generation_bumped` removal to 11g.9 (safer rollback story, cleaner chunk boundaries); (C) leave `generation_bumped` as permanent audit-only variant (no future cleanup). **Recommended and implemented (B).** Rationale: cleaner chunk boundary — 11g.8 = "remove pgmq writer side"; 11g.9 = "remove pgmq reader side + coordination protocol cleanup". `generation_bumped` events that bootstrap-replay through `applyEventDuringBootstrap` are already in the dispatch switch's `default:` arm (forward-compat skip-with-debug-log per chunk 11g.4 step 6b Decision Log 2026-05-05). Leaving them as no-op event-writes in 11g.8 is safe. 11g.9 will: remove the `'generation_bumped'` CHECK constraint value from `draft_events.event_type`, remove the two `PERFORM append_draft_event(... 'generation_bumped' ...)` calls from `draft_pause`/`draft_resume`/`draft_extend`, and either remove or repurpose the default-arm catch. |
| 2026-05-11 | Chunk 11g.8 rollback boundary: legacy pgmq path was always a stub | The pgmq path was always a stub — even in Phase 3/4, the `draft-autopick` Edge Function was a no-op archiver (per KI-009 + chunk 11g.4 step 6c Decision Log + ADR-001). The Phase 4 worker pattern was scaffolding for the persistent-engine migration that ADR-001 ratified. Removing emissions doesn't reduce production capability. Worst-case rollback = persistent engine has critical bug → rollback to "no autopick at all" which is the same as today's stub-archiver behavior. **Incident responders should NOT reach for the legacy pgmq path during crises — it does nothing.** Documented in the migration SQL header so on-call engineers reading the migration file at 3 AM during an incident see this immediately. |
| 2026-05-11 | `draft_deadline_sweep` retained in 11g.8; function + pg_cron job removed in 11g.9 | Recon-surfaced 4th emitter (not in the chunk-11g.8 brief's initial explicit list). The function's writer-side `pgmq.send` block is removed in this chunk, but the function body stays — the `safety_net_hit` metric write + advisory-lock + predicate walk are preserved for now. Pg_cron job continues invoking `draft_deadline_sweep()` harmlessly until chunk 11g.9 removes the function + cron job entirely. Keeping the function callable preserves the cron contract during the 11g.8↔11g.9 split; removing it mid-cycle would have broken cron without changing engine behavior (the metric write is now meaningless under the persistent-engine model — engine handles its own deadlines via setTimeout, so there's no "missed deadline" case for the safety net to catch). |
| 2026-05-11 | Migration ordering: 11g.8 migration must come after 11g.7 sub-step 7e migration | The 11g.8 migration `CREATE OR REPLACE`s `draft_resume` and `draft_extend`, whose latest definitions live in `20260511000000_draft_events_notify.sql` (chunk 11g.7 sub-step 7e). Postgres CoR semantics: only the most-recent function body matters; the 11g.8 migration is authoritative for those two functions after it applies. Filename `20260511010000_remove_pgmq_emissions.sql` (`010000` suffix sorts after `000000` of the 7e migration). `submit_pick_v2` and `draft_deadline_sweep` haven't been replaced since their original definitions in `20260425140000` and `20260426130000` respectively. |
| 2026-05-11 | Chunk 11g.8 emitter inventory (audit trail) | Migration removed pgmq emissions from exactly four functions; future engineers reading this Decision Log can confirm the inventory is complete by greping `supabase/migrations/*.sql` for `PERFORM pgmq.send`. Pre-11g.8: 4 active emitters (one per RPC; some functions had multiple replacement migrations but only the latest body emits — Postgres CoR semantics). Post-11g.8: 0 active emitters (the 4 CoR statements in `20260511010000_remove_pgmq_emissions.sql` supersede them all). Auction RPCs (`nominate_player_v2`, `place_bid_v2`, `close_nomination_v2`, `auction_pause_v2`, `auction_resume_v2`, `auction_commissioner_override_v2`, `auction_nomination_skip_v2`) were architected post-pgmq from day one and never emitted. |
| 2026-05-11 | **Chunk 11g.8 complete.** | pgmq emission removal from writer-side RPCs lands as a single commit on `phase-4-5-implementation`. New migration `20260511010000_remove_pgmq_emissions.sql` `CREATE OR REPLACE`s four RPCs (`submit_pick_v2`, `draft_resume`, `draft_extend`, `draft_deadline_sweep`), removing the `PERFORM pgmq.send(...)` blocks + surrounding `v_send_delay` declarations/computations where exclusively used for the emission. Each function's other behavior preserved: idempotency, actor authorization, atomicity contracts, the chunk 11g.7 sub-step 7e `draft_events_notify_after_insert` trigger (fires on durable INSERT, not pgmq — unaffected). `generation_bumped` event-writes retained per Option B (protocol removal deferred to 11g.9). `draft_deadline_sweep`'s function body + `safety_net_hit` metric write retained (function + pg_cron job removed in 11g.9). SQL header documents the full emitter inventory + ADR-001 rationale + 11g.9 follow-up scope + rollback boundary. `PHASE_4_5_PLAN.md` chunk decomposition updated: 11g.8 re-scoped to writer-side pgmq cleanup, 11g.9 expanded to include `generation_bumped` protocol cleanup, 11g.11 (new section) combines the relocated failure-mode test suite + the previously-net-new load test. No engine code changes; no test additions. 874 server tests stay green. **Next: chunk 11g.9 (Edge Function + queue/cron/extension + generation_bumped protocol).** |
| 2026-05-12 | **Chunk 11g.9 complete.** | Legacy pgmq infrastructure removal lands as a single commit (`9f72fd8`) on `phase-4-5-implementation`. Migration `supabase/migrations/20260512000000_remove_pgmq_infrastructure.sql` unschedules both pg_cron jobs (`draft-deadline-sweep`, `draft-autopick-keepalive`), drops the wrapper RPCs (`draft_autopick_read`, `draft_autopick_archive`), drops `draft_deadline_sweep()`, `CREATE OR REPLACE`s 4 RPCs (`submit_pick_v2` v_generation leak cleanup + `draft_pause`/`draft_resume`/`draft_extend` to remove generation_bumped event-writes + draft_generation column refs + generation field from return), updates the CHECK enum 21→20 values (removing `'generation_bumped'`), DROP COLUMN leagues.draft_generation, DROP EXTENSION pgmq CASCADE. Edge Function `supabase/functions/draft-autopick/index.ts` deleted. Migration includes Decision Log D1–D8 (most notable: recon report said enum 20→19, actual delta is 21→20 — corrected here; v_generation leak cleanup via Path A vs force-push of 11g.8 for commit-history hygiene; bootstrap dispatcher's `case 'generation_bumped':` arm retained for replay of historical events). KI-007 RESOLVED (vendored shared code drift; surface deleted with Edge Function), KI-009 RESOLVED (Edge Function infrastructure removed entirely; persistent in-server engine carries the load), KI-004 SUPERSEDED (Phase 3 keep-alive cron's hardcoded staging URL; cron no longer exists). Legacy SQL integration test scripts at `supabase/tests/draft_engine_v2_phase{1,2,3}*.sql` and `supabase/seeds/phase1_regression_seed.sql` reference removed surface but are not in CI/vitest; left in place, retirement deferred to 11g.10 / 11g.11. 874 server tests stay green; server tsc build clean. Migration includes a final verify DO block asserting column gone, pgmq absent, three legacy functions dropped. **Phase 4.5 chunks 11g.0–11g.9 all complete.** Remaining: 11g.10 (productionization), 11g.11 (failure-mode + load test). |
| 2026-05-12 | Productionization plan doc created at `docs/PHASE_4_5_PRODUCTIONIZATION_PLAN.md` (chunk 11g.10 sub-step 10a) | Depth required for productionization work (risk register, monitoring spec, cutover playbook, sign-off criteria, 5 sub-step decomposition) warrants a dedicated doc rather than expanding the 11g.10 section in `PHASE_4_5_PLAN.md`. `PHASE_4_5_PLAN.md` 11g.10 section becomes a one-paragraph pointer + sub-step decomposition table; canonical reference is the new doc. Plan doc quotes the CLAUDE.md Mandate verbatim (lines 1–58) so it's self-contained for reading; explicit note that CLAUDE.md is source of truth and updates flow CLAUDE.md → re-sync plan doc. |
| 2026-05-12 | Sub-step decomposition 10a → 10f rationale | Original chunk 11g.10 was scoped as a single-deliverable perf-verification sprint. 10a recon surfaced that staging GCE VM was torn down 2026-05-04 (chunks 11g.3–11g.9 shipped local-only); engine runbooks are pgmq-era stale post-11g.9; no Cloud Monitoring dashboards exist; no rollback playbook for the post-pgmq world; no production cutover plan. Productionization is genuinely multi-deliverable work. Decomposition: 10a (plan, this sub-step) → 10b (staging re-provisioning + smoke) → 10c (perf harness + execution + baseline) → 10d (monitoring + alerts) → 10e (runbook rewriting + rollback playbook; parallel-safe — can start any time) → 10f (cutover plan; gate to Phase 5). ~10–14 focused-work days total. |
| 2026-05-12 | k6 / `server/bench/` two-harness split (chunk 11g.10 sub-step 10a) | Two harnesses by design: `scripts/load-test/` (k6) for app-perimeter / PostgREST / general-API testing; `server/bench/` (new in 10c) for WS engine / Mandate-target measurement. Each tool sized to its target architecture. Existing k6 suite is calibrated against the old Cloud Run + Supabase Realtime stack; rewriting it to also serve WS engine measurement would strictly degrade its app-perimeter coverage. Two focused tools beats one conflated tool. R10D in the productionization plan's risk register notes the small chance of future engineer confusion if the boundary isn't kept clear in the k6 README; flagged as a small task in 10c. |
| 2026-05-12 | Mandate compliance verification approach (chunk 11g.10 sub-step 10a) | Two-pronged before empirical measurement at 10c: (1) §4 Binding Patterns audit — checkbox-style verification that the 5 architectural patterns from CLAUDE.md §1.3 are satisfied by shipped infrastructure, with citations. Catches structural drift cheaply. (2) §5 Non-Negotiables Operational Discipline — enumerate how each remaining sub-step respects CLAUDE.md's rejected framings ("we can optimize later", "most users won't notice", etc.). If 10c surfaces a Mandate gap, plan disallows deferral — gap closure absorbs remaining 10c budget. Caught one open item in §4 audit: CLAUDE.md §1.3 pattern #4 wording references "pgmq scheduler" which was removed in chunk 11g.9. The principle (Postgres = durability not hot path) is unchanged; LISTEN/NOTIFY now fills the scheduler role. CLAUDE.md update is out of scope for 10a and lives in a follow-up CLAUDE.md sync. |
| 2026-05-12 | Solo-founder risk R10C surfaced in productionization plan risk register (chunk 11g.10 sub-step 10a) | Single-operator productionization is the load-bearing operational risk. Calendar duration is sensitive to founder availability; Phase C workstreams (auction, co-manager, push) start June 1+ in parallel and contend for bandwidth. Mitigation: incremental sub-step delivery so any single sub-step can be picked up after a gap without losing context; clean Decision Log entries on each sub-step preserve resumability; the productionization plan doc itself IS the operational onboarding doc — Zach could read it cold and pick up 10b–10f with no further handoff. |
| 2026-05-12 | 10e (runbook rewriting) positioned parallel-safe (chunk 11g.10 sub-step 10a) | Runbook rewriting doesn't require deployed infrastructure; pgmq content needs to go regardless of staging state. 10e can start immediately, in parallel with 10a, or before 10b. Banking 10e early is a productivity option surfaced explicitly in the productionization plan §6.4. |
| 2026-05-12 | 10a delivers a planning artifact, not deployed code (chunk 11g.10 sub-step 10a) | Sub-step 10a's deliverable is the productionization plan document. No code changes, no staging changes, no infrastructure changes. 10b is where infrastructure work begins. The constraint was explicit per the brief: 10a is pure planning + documentation work. |
| 2026-05-12 | Effort estimates reflect focused-work hours; calendar duration depends on founder bandwidth (chunk 11g.10 sub-step 10a) | The per-sub-step effort estimates in the productionization plan (10a = ~1d, 10b = ~1–2d, 10c = ~3–5d, 10d = ~2–3d, 10e = ~1–2d, 10f = ~1d; total ~10–14d) reflect **focused-work hours**. Calendar duration depends on solo-founder bandwidth allocation against Phase C workstreams (auction implementation, co-manager schema, push notifications). 10–14 focused-work days probably translates to 3–4 calendar weeks in practice. Maps to Phase B (May 19–July 31) per project plan's pre-existing phase boundary. |
| 2026-05-12 | **Chunk 11g.10 sub-step 10a complete.** | Productionization plan document landed at `docs/PHASE_4_5_PRODUCTIONIZATION_PLAN.md` (12 sections: Mandate verbatim quote, architecture-to-Mandate mapping, current state assessment, binding patterns architecture compliance, non-negotiables operational discipline, sub-step decomposition 10a–10f, timeline + sequencing, risk register including solo-founder R10C, 10f sign-off criteria, success criteria, plan-internal Decision Log, cross-references). `PHASE_4_5_PLAN.md` 11g.10 section updated to a pointer + sub-step decomposition table + per-sub-step status table. No code changes, no staging changes. Single commit on `phase-4-5-implementation`. **Next: chunk 11g.10 sub-step 10b (staging re-provisioning + smoke verification) or 10e (runbook rewriting — parallel-safe). Both are Phase B work per project plan; 10e can start anytime post-10a.** |
| 2026-05-19 | CLAUDE.md §1.3 pattern #4 synced — pgmq scheduler → LISTEN/NOTIFY (chunk 11g.10 sub-step 10e) | The pre-edit wording "The event log, projection tables, and pgmq scheduler remain as the source of truth and the safety net" was stale post-11g.9 (commit `9f72fd8`, 2026-05-12 — pgmq DROP EXTENSION CASCADE'd, irreversible). Replaced with wording that accurately reflects the post-11g.9 architecture: "The event log and projection tables are both the source of truth and the durability backstop. LISTEN/NOTIFY (chunk 11g.7-7e) provides cross-process signaling to the engine when other processes append to `draft_events`." Cross-link: chunk 11g.9 supersession (`9f72fd8`) + chunk 11g.7-7e implementation (migration `20260511000000_draft_events_notify.sql`). The verbatim pre-edit quote in `PHASE_4_5_PRODUCTIONIZATION_PLAN.md` §1.3 is preserved as historical record of CLAUDE.md's wording when 10a shipped. 10a's original positioning ("next CLAUDE.md touch, not blocking") was conservative; the 10e brief expanded scope to include this fix because runbook content references the architecture and stale CLAUDE.md wording would propagate confusion into operational docs. PRODUCTIONIZATION_PLAN §4 row 4 closure annotation + §R10F closure note both land in this same commit. |
| 2026-05-19 | Runbook decomposition ratified — 4 files + README index, single layered doc per file (chunk 11g.10 sub-step 10e) | Plan §6.4 locked 3 rewrites + 1 new playbook. Added a `docs/RUNBOOKS/README.md` index (~80 lines) on the 10e recon's recommendation — low-cost addition that materially improves "Saturday night 11pm" discoverability and clarifies the boundary with `docs/EMERGENCY_RUNBOOK.md` (data-pipeline / roster recovery — different domain). Audience layering: single layered document per runbook file, organized by urgency (each file opens with a triage/triage-equivalent for "I'm on-call right now," then per-symptom or per-scenario detail, then reference material). Rejected the alternative of separate per-audience files because the layered-doc structure serves all three audiences (Garrett at 11pm reads the top sections; Zach onboarding reads the full doc; investor DD scans the structure). |
| 2026-05-19 | Rollback playbook scope expanded from 5 to 6 scenarios (chunk 11g.10 sub-step 10e) | Plan §6.4 locked 5 scenarios: bad migration, engine binary regression, Postgres connection pool exhaustion, LISTEN/NOTIFY misconfiguration post-deploy, snapshot table corruption. Added scenario #6: engine identity-verification compromise per ADR-004 §6. Rationale: highest-stakes operational scenario (security implications, audit trail dependency); ADR-004 §6 already commits to the audit-trail data structure; runbook scenario #6 documents the operational response that converts that audit trail into incident-recovery action. Recon flagged this as a scope expansion explicitly; brief ratified the expansion. Plan §6.4 amended in spirit; this Decision Log entry is the record of the scope change. |
| 2026-05-19 | Cross-doc boundary explicit — draft-engine runbooks vs `docs/EMERGENCY_RUNBOOK.md` (chunk 11g.10 sub-step 10e) | `docs/EMERGENCY_RUNBOOK.md` is the data-loss / roster recovery runbook (separate domain — `team_lineups`, `fantasy_daily_rosters`, `draft_picks` for legacy snapshot writes, Jan 15 2026 incident postmortem context). Draft-engine runbooks cover the live draft hot path (pick submission, broadcast fanout, autopick, snapshot persistence, LISTEN/NOTIFY, auction state machine). The boundary needed to be explicit to prevent on-call from conflating two different systems with different invariants and different SQL surfaces. `docs/RUNBOOKS/README.md`'s "When to read which" table is the authoritative router. `EMERGENCY_RUNBOOK.md` itself is untouched by 10e (different scope, separate maintenance lineage). |
| 2026-05-19 | Pre-cutover vs post-cutover topology coverage (chunk 11g.10 sub-step 10e) | Runbooks written against the post-cutover unified-GCE topology (per ADR-001 + `PHASE_4_5_ARCHITECTURE.md`: main `citrus-api` and draft engine on same GCE platform, single Node process with Hono on 3001 + uWS on 3002). Current pre-cutover state (main API on Cloud Run, draft engine on GCE — two platforms, two deploy pipelines) covered as clearly-marked sidebars in `draft-engine-v2-operations.md` §9 and `draft-engine-v2-staging-preflight.md` §7. Sidebars retire at 10f when production cutover lands. This split keeps the canonical runbook content stable for the long term while documenting the transient ops complexity during the cutover window. |
| 2026-05-19 | Deferred items tracked (chunk 11g.10 sub-step 10e) | Items surfaced during 10e writing but explicitly out of scope: (1) "Architectural truth" callout enrichments in `draft-engine-v2-known-issues.md` — incrementally addable as net-new KIs surface. (2) Anti-snipe cascade monitoring thresholds — needs product judgment (e.g., warn at N extensions? page at M extensions?) before instrumentation lands; defer to 10d alert design + product call. (3) Post-incident review template at `docs/postmortems/<date>-<name>.md` — standalone small commit; flagged in `draft-engine-v2-rollback-playbook.md` §G as TODO. (4) `PRE_DRAFT_CHECKLIST.md` persistent-engine delta — properly belongs in 10f cutover plan, not 10e. (5) Discovery-flag mechanism for "no new drafts" bridging action — TODO(10b/10c) markers in operations.md + rollback-playbook §3. (6) Admin HTTP endpoint for force-snapshot + per-lobby eviction — TODO(10b/10c) marker in operations.md §6.5. (7) Healthy-baseline measured numbers (snapshot cadence, heartbeat scan rates, Mandate p50/p95/p99) — TODO(10c) markers; populated from staging harness output. (8) Deploy-pipeline assertion that `SUPABASE_DB_URL` doesn't match pooled patterns — TODO(10b) per KI-010. |
| 2026-05-19 | **Chunk 11g.10 sub-step 10e complete.** | Runbook rewriting + rollback playbook + CLAUDE.md §1.3 pattern #4 fix land as a single commit on `phase-4-5-implementation`. Touched 7 files: `CLAUDE.md` (§1.3 pattern #4 wording sync), `docs/PHASE_4_5_PRODUCTIONIZATION_PLAN.md` (§4 row 4 closure annotation + §R10F closure note + §4 open-item closure footer), `docs/PHASE_4_5_PROJECT_PLAN.md` (8 Decision Log entries dated 2026-05-19), `docs/RUNBOOKS/README.md` (new index, ~80 lines), `docs/RUNBOOKS/draft-engine-v2-operations.md` (full rewrite from Phase 0 stub to ~700 lines: triage tree, 14 per-symptom playbooks, healthy baselines, Mandate breach response, cross-process eventing flows, snapshot management, customer-impact tiering, reference material, pre-cutover topology sidebar), `docs/RUNBOOKS/draft-engine-v2-staging-preflight.md` (full rewrite from pre-Phase-4.5 era to ~400 lines: PG/extensions sanity, draft_events/draft_snapshots/trigger checks, engine startup verification, SUPABASE_DB_URL direct-URL check, LISTEN/NOTIFY end-to-end test, snapshot persistence + bootstrap verification, results template, pre-cutover sidebar), `docs/RUNBOOKS/draft-engine-v2-known-issues.md` (append-only: new "Persistent-engine known issues (post-11g.9)" section + KI-010 PgBouncer LISTEN frames silent drop), `docs/RUNBOOKS/draft-engine-v2-rollback-playbook.md` (new, ~500 lines: snapshot-version compatibility marker, decision-time framework, 6 scenarios, user-facing comms templates, PIR framework). Plan §6.4 pass criteria: grep gate (zero pgmq/draft_generation/generation_bumped/draft_deadline_sweep/draft-autopick hits in docs/RUNBOOKS/ outside historical sections of known-issues.md) — passed. Link-check passed. No code changes, no test changes; this sub-step is documentation-only. **Next: chunk 11g.10 sub-step 10b (staging re-provisioning + smoke verification) — the remaining sub-step that gates 10c and 10d.** |
| 2026-05-19 | 10e architect review surfaced 8 specific gaps + 2 housekeeping items; targeted fix commit lands (chunk 11g.10 sub-step 10e follow-up) | Architect review of commit `904a57c` flagged: (A.1) missing "all-clear / resumed" user-comms template — most-frequently-needed template was omitted; (A.2) scenario 5 missed the severity escalation when `draft_events` itself (not just `draft_snapshots`) is corrupted; (A.3) no meta-framework for "rollback execution itself fails" — the 10-minute escalation reassessment loop wasn't documented; (A.4) §F.4 catastrophic-outage cadence (every 15 min) was unrealistic for solo on-call who must also fix — relaxed to 30-min initial, hourly after 2h; (A.5) "obviously revertable" criteria in scenario 1 was vague — replaced with three concrete conditions, all must hold; (A.6) §F.3 timezone example was single-timezone (Mountain) — expanded to multi-TZ + timezone note; (A.7) §2.9 auction-bid-serialization escalation "capture full logs and escalate" was too thin — concretized to 6 steps (pause, save logs to `docs/postmortems/raw/`, file issue, notify Zach, tag, PIR); (B.1–B.4) several decision-relevant thresholds were vague ("many users", "frequent", "hundreds", "repeatedly", "sustained vs sporadic") — replaced with concrete numbers (≥3 users / 5min, ≥3 events / 10min, ≥50 in 10min OR ≥200 in 1h, ≥2 restart attempts / 5min, ≥5 picks/min for sustained); (C.1) KI numbering namespace fix; (C.2) Decision Log addendum. All 13 changes land in a single commit on `phase-4-5-implementation`. No code changes; no test changes. |
| 2026-05-19 | Grep gate interpretation formalized (chunk 11g.10 sub-step 10e follow-up) | The 10e grep gate (no-legacy-path verification — `git grep -n "pgmq\|draft_generation\|generation_bumped\|draft_deadline_sweep\|draft-autopick" docs/RUNBOOKS/`) is interpreted as: no live operational instruction treats removed surfaces (pgmq, draft_generation, generation_bumped, draft_deadline_sweep, draft-autopick) as live infrastructure. Warnings, absence-checks, removal-migration references, and historical sections in `draft-engine-v2-known-issues.md` preserved per append-only convention are all legitimate hits. Architect verified all 44 hits in commit `904a57c` are legitimate; gate PASSES. This formalization resolves the tension between PRODUCTIONIZATION_PLAN §6.4 ("zero references to the removed surface") and the brief §A operational requirement ("Runbooks must say so explicitly — multiple times"). Both requirements are satisfied simultaneously when "reference" is read as "live operational instruction." The follow-up commit may add hits as new warning language is introduced; each hit is categorized in the ship report. |
| 2026-05-19 | KI numbering namespace convention (chunk 11g.10 sub-step 10e follow-up) | Engine-specific KIs in `docs/RUNBOOKS/draft-engine-v2-known-issues.md` use the `KI-E###` prefix to disambiguate from project-wide KIs in `docs/REGISTRY.md` (which use plain `KI-###`). Engine `KI-010` renamed to `KI-E010` to resolve collision with project `KI-010` ("Tier 1 perf optimizations"). Pre-Phase-4.5 engine entries (`KI-001..KI-007`) are not retroactively renamed — they predate the convention, and the project-wide registry starts at `KI-008` so no collision exists with them. New engine-specific KIs from chunk 11g.10 forward use `KI-E###`. Convention documented in the engine known-issues file's "Persistent-engine known issues (post-11g.9)" section header. |
| 2026-05-19 | Standard escalation channel for invariant violations — open decision (chunk 11g.10 sub-step 10e follow-up) | TODO(garrett): pick the standard escalation channel for engine invariant violations (Slack vs Discord vs Linear comment vs GitHub issue). Currently ambiguous — runbook references "the standard escalation channel" as a TODO marker. Decision-makers: Garrett (executor + reviewer). Decision-by date: before first incident requiring escalation, or before 10f cutover ratification (whichever is sooner). Once decided, `draft-engine-v2-operations.md` §2.9 escalation step 4 + any other runbook references update to name the channel; this Decision Log entry annotates with the choice + date. |
| 2026-05-19 | `SUPABASE_DB_URL` secret added to staging deploy (chunk 11g.10 sub-step 10b — Commit 1) | The chunk 11g.2 startup script only injected `SUPABASE_JWT_SECRET`. Chunks 11g.3–11g.9 were local-only, so the gap was invisible — but the chunk 11g.7-7e LISTEN/NOTIFY path requires `SUPABASE_DB_URL` (a direct primary Postgres URL, NOT pooled). Without it, the engine starts but emits `event_subscription.skipped_no_db_url` (warn) and runs without cross-process notification path — silent degradation. Commit 1 of 10b extends `infra/gce/draft-engine-startup.sh` to read `supabase-db-url` from GCP Secret Manager + inject as env var. KI-E010 closure: also adds an inline + script-based pre-flight assertion blocking pooled-URL patterns (`pooler.supabase.com`, `pgbouncer`, port `6543`). Refuses to start the container if a pooled URL is detected. |
| 2026-05-19 | `SUPABASE_SERVICE_ROLE_KEY` secret added to staging deploy (chunk 11g.10 sub-step 10b — Commit 1) | Required by `getSupabaseAdmin()` in `server/src/lib/supabase.ts`, which the engine uses for RPC calls (per ADR-004 + chunks 11g.4+). The pre-10b startup script didn't inject it — engine would have failed at first RPC call attempting to construct the admin client. Commit 1 adds `supabase-service-role-key` to the Secret Manager fetch list + injects as env var. Same IAM-grant pattern as the other secrets (`roles/secretmanager.secretAccessor` on the SA `citrus-draft-engine@citrus-fantasy-staging.iam.gserviceaccount.com`). Naming follows the role-clear-separation convention: separate Secret Manager secret per role (JWT vs DB URL vs service-role-key). |
| 2026-05-19 | Discovery-flag mechanism design ratified — Option D + 5s cache + defense-in-depth (chunk 11g.10 sub-step 10b — Commit 1) | New `system_flags` table (migration `20260520000000_system_flags.sql`) carries operational kill-switches keyed by `flag_name`. Initial flag: `no_new_drafts`, default false. On-call toggles via a single SQL `UPDATE` (operations runbook §2.4 + rollback playbook §3). **Two enforcement points** read the flag with 5s in-memory cache: (1) main API discovery endpoint (`server/src/routes/drafts.ts`) returns 503 `NEW_DRAFTS_DISABLED` when flag is on AND draft state is `not_started`; in-progress drafts unaffected per the brief's "in-progress drafts continue" semantic. (2) GCE engine LobbyRegistry (`server/src/draft/LobbyRegistry.ts`) refuses to construct a new lobby for `not_started`/`pre_draft` drafts when flag is on. Single source of truth (the table), two enforcement points — single-flag SQL toggle propagates to both within ~5s. Shared cache module `server/src/lib/systemFlags.ts` (fail-open on read errors with last-known-value fallback). |
| 2026-05-19 | Admin HTTP endpoint design ratified — `is_engine_admin` gate, separable from `is_admin` (chunk 11g.10 sub-step 10b — Commit 1) | New `server/src/routes/draftAdmin.ts` owns engine-ops admin endpoints (force-snapshot, per-lobby evict, registry diagnostic). Distinct from `server/src/routes/admin.ts` (which is the user-dashboard admin). Auth: new `profiles.is_engine_admin` BOOLEAN column (migration `20260520010000_add_profiles_is_engine_admin.sql`) — separable from `is_admin` so engine-ops privilege escalation can be controlled independently of user-management privilege when the team grows. Cheap to add now (one column, default-from-`is_admin` backfill so Garrett gets it for free); expensive to retrofit later. **Endpoint scope: both staging and production** — runbook escalation paths require these endpoints in prod for incident response; `is_engine_admin` is the access control. **Status:** routes + auth gating shipped; endpoint bodies are scaffolded with 501 NOT_IMPLEMENTED + the manual SSH/SQL equivalent in the response body. Full cross-process delegate (main API → engine HTTP/RPC) deferred to 10c/10d when perf-harness + monitoring needs surface. |
| 2026-05-19 | Deploy-pipeline assertions shipped — `scripts/staging-deploy-checks.sh` (chunk 11g.10 sub-step 10b — Commit 1) | New bash script that fails-loud on misconfigurations before the engine container starts. Asserts: (1) all four required secrets present (SUPABASE_DB_URL, SUPABASE_JWT_SECRET, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_URL); (2) SUPABASE_DB_URL does NOT match pooled patterns (`pooler.supabase.com` / `pgbouncer` / `:6543`) — KI-E010 hardening; (3) SUPABASE_URL matches `https://<project>.supabase.co` pattern; (4) image SHA matches expected (when EXPECTED_IMAGE_SHA provided); (5) required GCE instance metadata keys present (`project-id`, `image-tag`) when running on GCE. Called by `infra/gce/draft-engine-startup.sh` if `/opt/citrus/staging-deploy-checks.sh` is installed on the VM; otherwise startup script does an inline minimum-viable pooled-URL check as safety net. |
| 2026-05-19 | Deployment fingerprint structured log added (chunk 11g.10 sub-step 10b — Commit 1) | New `deployment.fingerprint` (info) log line emitted in `server/src/draft/index.ts` immediately after `hono.listening`. Fields: `imageSha`, `commitSha`, `nodeEnv`, `envFingerprint` (presence/absence map for required env vars — NEVER logs values per security boundary), `startupTimeMs`. The fingerprint anchors "what was deployed when this broke?" for post-incident reconstruction. Required env keys: SUPABASE_URL, SUPABASE_DB_URL, SUPABASE_JWT_SECRET, SUPABASE_SERVICE_ROLE_KEY, PORT, DRAFT_WS_PORT. Image and commit SHAs sourced from GCE instance metadata via the startup script's `-e` injections. |
| 2026-05-19 | Staging-prod parity assumption locked (chunk 11g.10 sub-step 10b — Commit 1) | Staging shape per `docs/PHASE_4_5_GCE_PLATFORM_NOTES.md` §1: `e2-medium` machine type, `northamerica-northeast1` (Montreal) region, dedicated SA `citrus-draft-engine@citrus-fantasy-staging.iam.gserviceaccount.com`, Debian + Docker container OS. **Production assumed identical shape for now**; sizing revisit happens at 10f when actual load expectations are clearer. The startup script's metadata-driven parameterization supports per-environment customization (project-id, image-tag, secret names) without code changes, so production deploys can override defaults via VM metadata. This Decision Log entry is the inheritance record — 10f planner reading this knows staging shape = the prod default unless 10f explicitly diverges. |
| 2026-05-19 | CI check for env-var coverage flagged for chunk 11g.12+ as deferred hardening (chunk 11g.10 sub-step 10b — Commit 1) | Future CI check: scan `server/src/**/*.ts` for `process.env.X` references and verify each is either (a) loaded in `infra/gce/draft-engine-startup.sh` via `-e`, or (b) explicitly documented as optional with a default. Catches the same class of bug that motivated 10b's `SUPABASE_DB_URL` discovery (code references env var; startup script doesn't load it; silent degradation). Out of scope for 10b — 10b's pre-flight script is the immediate guard, the CI check is the durable belt-and-suspenders. Tracked for post-10f hardening work; create as a GitHub Actions job once the CI pipeline is established. |
| 2026-05-19 | Force-snapshot endpoint response surfaces `persisted: boolean` + optional `reason` discriminator (chunk 11g.10 sub-step 10b — Q3 follow-up) | Replaces ambiguous `scheduled: true` from the relocation commit (7f60ae5). Prior shape couldn't distinguish "new snapshot written" from "scheduling completed but write was skipped." An operator hitting `POST /api/admin/engine/lobby/:id/snapshot` during an incident could receive `scheduled: true` even when LobbyManager.scheduleSnapshot() skipped the write (draft state `not_started` or `paused`, or one of several error paths). They'd trust the response and miss that durable state is stale relative to in-memory. **Fix:** `LobbyManager.scheduleSnapshot()` now returns `Promise<{ persisted: boolean; reason?: string }>` instead of `Promise<void>`. `processSnapshot()` returns the envelope from each code path with discriminator values: `state_not_in_progress`, `shutting_down`, `max_seq_lookup_failed`, `build_failed`, `no_snapshot_available`, `write_failed`, `queue_error`. `LobbyRegistry.forceSnapshot()` propagates the envelope. Admin endpoint response shape now `{ lobbyId, persisted, reason?, seq, engineVersion, persistedAt }`; `scheduled` field removed entirely. The `seq`/`engineVersion`/`persistedAt` fields ALWAYS reflect the most-recent existing snapshot row in `draft_snapshots` (so the operator always sees what's durable). Audit-log `admin.endpoint.snapshot_forced` payload now includes `persisted` + `reason`, so the trail captures the outcome rather than just the intent. Two existing internal `scheduleSnapshot()` callers (periodic timer + milestone trigger) ignore the return value — no behavioral change. Runbook §6.5 updated to document the new shape + skip-reason vocabulary. |
| 2026-05-19 | Admin endpoint relocation: engine process, not API server (chunk 11g.10 sub-step 10b — admin relocation fix) | Architect call after Commit 1 ship review: `draftAdmin.ts` was incorrectly mounted on the main API server's Hono composition (Cloud Run, `server/src/app.ts`) with 501 NOT_IMPLEMENTED responses because cross-process delegation to the engine wasn't wired. **Wrong architecture.** Admin endpoints manipulate engine-local in-memory state (LobbyRegistry, snapshot pipeline). Cross-process delegation adds an unnecessary network hop, additional auth surface, and an additional failure mode. **Fix**: `draftAdmin.ts` refactored as a factory `createDraftAdminRoutes({ registry, supabaseAdmin })` that mounts on the engine's own Hono server (GCE VM port 3001), unmounted from the API server. On-call hits the engine's HTTP port directly. Endpoint bodies replaced with actual functional implementations: POST `/snapshot` calls `LobbyRegistry.forceSnapshot()` (new) which routes through the LobbyManager's single-writer queue via `scheduleSnapshot()`; POST `/evict` calls `LobbyRegistry.evict()` (new) which force-closes WS connections with code 4002 + removes from registry; GET `/registry` calls `LobbyRegistry.getDiagnosticSummary()` (new). New LobbyManager public method: `getDiagnosticInfo()` exposes `{lobbyId, leagueId, format, connectionCount, lastAppliedSeq}`. Audit-log events on every successful action (`admin.endpoint.snapshot_forced`, `admin.endpoint.lobby_evicted`, `admin.endpoint.registry_read`) carrying `userId`, `lobbyId` (if applicable), and outcome. Auth chain: `authMiddleware` (JWT validation, requires SUPABASE_URL + SUPABASE_ANON_KEY in engine env — `SUPABASE_ANON_KEY` added to startup script as a fourth Secret Manager-tracked value alongside JWT_SECRET / DB_URL / SERVICE_ROLE_KEY) → `is_engine_admin` profile gate (mirrors the user-dashboard admin pattern but uses the separable flag). Runbook references in `operations.md` §2.3 + §6.5 updated to point at `http://<engine-host>:3001/api/admin/engine/...`. Tests rewritten as factory-based: 13 tests against a mock LobbyRegistry covering auth gating (3) + functional endpoints (5) + audit-log emission (3). |
| 2026-07-21 | **Chunk 11g.10 sub-step 10b staging provisioning complete + §6.1 pass criteria disposition.** | Staging engine live at static IP `35.203.89.236` (GCE VM `citrus-draft-engine-staging`, zone `northamerica-northeast1-a`, image `draft-engine:ef52351` — digest `sha256:a261fb3d6b72edb6624313fe0a05da93cdb7d70f7b62e8418238166472139e70`). §6.1 pass criteria: (1) engine startup 5-event sequence — `uws.listening`, `hono.listening`, `deployment.fingerprint` (all 7 env vars present, 132ms startup), `event_subscription.started`, `event_subscription.self_test_succeeded` in 5ms — verbatim green. (2) WS smoke harness 5/5 against staging (scenarios a/b1/b2/c/d). (3) LISTEN/NOTIFY — met via startup self-test (engine's `_test` sentinel round-trip through Postgres in 5ms IS a full end-to-end verification); cross-session psql NOTIFY delivery separately confirmed against the DB URL. (4) HTTP snapshot endpoint criterion — reinterpreted per topology (see next entry). Full re-provisioning narrative + gotchas captured in `docs/PHASE_4_5_GCE_PLATFORM_NOTES.md` §15. |
| 2026-07-21 | §6.1 snapshot-endpoint criterion reinterpreted to main-API scope (chunk 11g.10 sub-step 10b) | The 10b brief §6.1 asked to verify `GET http://<engine-host>:3001/api/drafts/<id>/snapshot`. Verification against staging returned Hono's default `404 Not Found` — the snapshot endpoint lives in `server/src/routes/drafts.ts` mounted on the main API server (`server/src/app.ts:239`), **not** the engine process. The engine-process Hono at port 3001 intentionally serves only `/health` + the engine-admin factory routes (`server/src/draft/index.ts:108-129` file comment: "The Hono app is intentionally separate from the main API server"). Two-process topology per ADR-001; §6.1 was written pre-topology-clarity. **Disposition**: engine `/health` returning 200 is the accepted engine-side proof-of-life for 10b; full snapshot-endpoint HTTP verification moves to 10c (where the perf harness exercises the resync path end-to-end against the main API host). No architecture change; documentation gap only. |
| 2026-07-21 | Supabase IPv4 add-on decision — enable on staging, flag for prod 10f (chunk 11g.10 sub-step 10b) | Supabase project's Postgres hostname `db.jjgspcpvqaiitloglxbb.supabase.co` is IPv6-only by default. GCE VMs do not egress IPv6 without Cloud NAT dual-stack reconfiguration. Enabled Supabase's dedicated IPv4 add-on ($4/mo per project) — hostname now resolves to stable IPv4 `15.222.174.205`. Alternative options rejected: (a) IPv6-egress on VM (adds NAT/subnet complexity); (b) route through Supavisor pooler on IPv4 (defaults to transaction mode which drops LISTEN frames — KI-E010). **10f flag**: production Supabase project needs the same IPv4 add-on before cutover. Deploy pipeline does not auto-provision this — captured in `PHASE_4_5_GCE_PLATFORM_NOTES.md` §15.4 as a 10f pre-cutover checklist item. |
| 2026-07-21 | Supabase migration-history repair against staging (chunk 11g.10 sub-step 10b) | Staging `supabase_migrations.schema_migrations` had drifted during the local-only chunks 11g.3–11g.9 window (executed via direct psql without going through `supabase migration`). Two classes of repair: (1) 17 Phase 4.5 migrations marked `applied` via `supabase migration repair --status applied <ts>` — bringing history in line with what the code and DB objects already reflected. (2) Two master hotfix migrations (playoff-winner propagation trigger + playoff aggregate assists NHL-column fix) had been marked `reverted` in staging during an earlier branch rollback; reconciled to `applied` and copied the two `.sql` files into the branch working tree so the `phase-4-5-implementation` branch state matches remote. Files present in working tree at 10b closure but uncommitted; folded into Commit 2 file list. Net state: staging history is a clean superset of what the branch expects. |
| 2026-07-21 | Cloud Logging filter correction — 10d design input (chunk 11g.10 sub-step 10b) | Startup script comment at `infra/gce/draft-engine-startup.sh:48` suggests filtering `labels.app="citrus-draft-engine"` — returns zero results because the `gcplogs` Docker driver does not promote container labels to top-level Cloud Logging entry labels. Working filter: `logName:"gcplogs-docker-driver" AND jsonPayload.container.metadata.app="citrus-draft-engine"`. Additional caveat: engine's structured JSON (severity, event, time) lands inside `jsonPayload.message` as a **stringified JSON blob**, not top-level fields. Any 10d Cloud Logging alert or log-based metric on `jsonPayload.event` would need to reparse the string. **10d design flag**: either accept the string-reparse pattern in the alert policy DSL, or switch the Docker log driver to write JSON-per-line to a file + ship via Ops Agent (which parses JSON into structured payload natively). No code fix in 10b; captured in `PHASE_4_5_GCE_PLATFORM_NOTES.md` §15.6. |
| 2026-07-21 | Smoke harness parameterization + scenario (c) semantic update (chunk 11g.10 sub-step 10b) | `scripts/smoke-uws-step2.js` now reads `SMOKE_LOBBY_A` and `SMOKE_LOBBY_B` from env (defaults `lobby-A` / `lobby-B` for local runs), threading both through URL path construction and scenario (d)'s label. Small durable fix — 10c's perf work will hit the same real-UUID requirement, so the harness needed to stop hardcoding scaffold-era strings. Additionally, scenario (c) semantics updated from `wsEchoTest` (expected `echo: <text>` reply from the chunk 11g.2 scaffold) to `wsSnapshotTest` (expects a well-formed `{v, type:'snapshot', payload:{lobbyId, format, …}}` JSON message on connect). The echo path was removed when chunk 11g.4 step 5 replaced the scaffold with real message routing; scenario (c) had been silently misaligned since then. Staging fixture applied: league `993c9219-ecbf-4e4e-9fb0-e9837e1bded3` "Staging League" now has `settings.draftType='snake'`, `settings.pickTimeLimit=60`, and one `draft_order` row for round 1 with the existing team `4c742dae-…`. Full fixture requirements captured in `PHASE_4_5_GCE_PLATFORM_NOTES.md` §15.8. Smoke result: **5/5 verbatim green**. |
| 2026-07-21 | **DEFECT ratified: engine did not broadcast manual picks on external-event apply (chunk 11g.10 sub-step 10c-1a).** Fix shape: `processExternalEvent` broadcasts wire event on live apply; bootstrap replay stays broadcast-free (canonical-replay preserved); dedup via `lastAppliedSeq` prevents engine-authored echo double-broadcast. | **Discovery.** 10c-1 recon (STOP report, 2026-07-21) traced the manual-pick path: client → main API `POST /api/draft/v2/league/:id/pick` → `submit_pick_v2` RPC → NOTIFY → engine LISTEN → `enqueueExternalEvent(seq)` → `processExternalEvent` → `applyEventDuringBootstrap(event)` → per-type applier (e.g. `applyPickEvent`). Every one of the 13 `this.broadcast(...)` call sites sat inside an engine-authored action handler (`processSubmitPick` for autopicks, `processNominate`/`processPlaceBid`/etc. for auction, `processCommissionerOverride`). Zero broadcasts fired on the external-apply path. Connected WS clients received nothing when a manual pick was submitted through the Cloud Run API. Only autopicks (which go through `processSubmitPick`) reached the wire. Mandate's "manual pick submission p95 ≤ 300 ms with all participants seeing it" was structurally unmet. **Historical record.** Original chunk 11g.4 step 5 deliverable (`docs/PHASE_4_5_PLAN.md:252`) explicitly scoped a WS-direct-submit path: *"WebSocket message handler routes inbound `{ type: 'submit_pick', ... }` and `{ type: 'chat', ... }` messages to the right `LobbyManager` method."* ADR-001:228 mirrored: *"First end-to-end pick path: client opens WebSocket, submits a pick, server validates against in-memory state, calls `submit_pick_v2`, broadcasts to all clients in the room."* Neither shipped. `DraftClientMessage` (`packages/shared/src/types/draftWire.ts:560`) is `{type: 'resync'}` only — WS-direct-submit was silently deferred. The fallback (LISTEN/NOTIFY apply broadcasting) was ALSO omitted. No Decision Log entry ever recorded either deferral. **Ruling (architect, 2026-07-21).** Real defect. Fix is general: all state-affecting, client-visible event types broadcast on LIVE external apply; NEVER on bootstrap replay. State application byte-identical between modes (canonical-replay principle from chunk 11g.6 sub-step 6b Decision Log 2026-05-07 untouchable). **Implementation.** `RingBuffer.peekLast()` added; `LobbyManager.processExternalEvent` inspects tail after `applyEventDuringBootstrap(event)` — if `peekLast().seq === event.seq`, applier appended a `BufferedDraftEvent` and the wire message is broadcast; otherwise (internal-only event types with no wire representation) no broadcast. Applier signatures untouched. Bootstrap callers (`bootstrap()`, `bootstrapFullEventReplay()`) unchanged. Dedup gate at top of `processExternalEvent` (`seq <= lastAppliedSeq`) short-circuits engine-authored echo before any apply. **Client-visible coverage** (11 of 17 event types): `pick`, `pick_undone`, `commissioner_override`, all 10 `auction_*` variants. **Internal-only** (no wire representation today, no broadcast): `draft_paused`, `draft_resumed`, `draft_completed`, `draft_cancelled`, `draft_extended`, `autopick_failed`, `generation_bumped`. Wire variants for snake/linear draft lifecycle (`draft_paused`/`draft_resumed`/`draft_completed`/`draft_cancelled`) are a separate protocol design question — clients today see these via `stateSnapshot.draftStatus`/`stateSnapshot.currentPickDeadline` on next snapshot/timer tick. Flagged as open follow-up; not in 10c-1a scope. **Test class gap that let the defect survive.** No test asserted cross-process client delivery — the LobbyManager tests exercised engine-authored broadcasts thoroughly (12+ tests around `processSubmitPick`/`processNominate`/etc.) but the external-apply tests (7e block, chunk 11g.7 sub-step 7e) only asserted state changes, never client delivery. Fix ships 6 new tests including a live/replay state-parity test proving byte-identical apply, and an echo-dedup test proving engine-authored actions don't double-broadcast. Test class going forward: any code path that mutates lobby state MUST have a broadcast-observability test. **Two pre-registered remediations awaiting 10c measurement data.** If manual-pick p95 in 10c fails on hop count (Client → Iowa API → Montreal Postgres → Montreal engine): (1) ADR-004 WS-direct submission (the original PLAN.md:252 / ADR-001:228 design) becomes the named optimization — client submits over the engine WS, engine calls `submit_pick_v2` directly and broadcasts locally. Same architecture, one fewer hop. (2) Alternate: Iowa-API region move to Montreal (co-locate `citrus-api` with the engine). Cheaper if the hop-count decomposition points to inter-region latency dominating; deploy-only change, no code. Both decisions are for post-10c-measurement Decision Log entries. |
| 2026-07-21 | Live/replay mode split via ring-buffer tail inspection (chunk 11g.10 sub-step 10c-1a mechanics) | Considered adding a `mode: 'live' | 'replay'` parameter or splitting `applyEventDuringBootstrap` into `applyEventLive` / `applyEventReplay` wrappers around a shared state-apply. Chose neither — used tail inspection instead. **Rationale.** Every per-type applier already builds a `BufferedDraftEvent` and calls `this.events.append(buffered)` as part of its normal state-apply body. Tail inspection (`this.events.peekLast()` after `applyEventDuringBootstrap(event)` — if tail's `seq` equals the event's `seq`, applier appended a wire-visible event) lets the live-path broadcast use the buffered event as the wire payload without touching applier signatures, without threading a mode parameter through 13 apply functions, and without duplicating payload construction. Bootstrap callers pass through unchanged; only `processExternalEvent` reads the tail and broadcasts. Preserves canonical-replay-principle absolutely rigidly: the applier's state-apply body is byte-identical in both modes because it IS the same code. `RingBuffer.peekLast()` is a 3-line read-only helper; scope + safety both minimal. |
| 2026-07-21 | Engine instrumentation shipped (chunk 11g.10 sub-step 10c-1b) | Six structured-log emissions added on the seams the 10c-1a defect closure fixed, feeding the 10c-2 measurement harness. All INFO unless noted. **Events:** (1) `pick.processed` — `LobbyManager.processSubmitPick` — `{lobbyId, seq, teamId, rpcMs, broadcastMs, totalMs, wasAutopick, wasDuplicate}`. Bounds engine-side pick processing latency for the Mandate manual-pick metric (300ms p95). Fires today for autopick path; will fire for WS-direct-submit if that optimization ships based on 10c measurement data. (2) `autopick.fired` — `LobbyManager.handleAutopickTimeout` — `{lobbyId, teamId, deadlineMs, scheduledFireMs, actualFireMs, driftFromDeadlineMs, submitElapsedMs}`. `driftFromDeadlineMs` measures setTimeout accuracy vs scheduled deadline; feeds Mandate autopick metric (1s p95). (3) `resync.responded` — `LobbyManager.handleResyncRequest` — `{lobbyId, sinceSeq, deltaCount, tooOld, buildMs}`. Bounds ring-buffer filter cost + capacity input for reconnect-storm scenarios (2s p95 Mandate). (4) `lobby.snapshot_sent_on_connect` — `LobbyManager.addConnection` — `{lobbyId, userId, buildMs, sendMs}`. Server-side portion of the Mandate draft-state-load metric (1.5s p95). (5) `external_event.applied` — INFO, live-mode only in `LobbyManager.processExternalEvent` — `{lobbyId, seq, eventType, applyMs, broadcastMs, notifyToBroadcastMs, broadcasted}`. Server-side commit→broadcast decomposition; combined with `draft_events.created_at` this gives the full server-side fanout latency (200ms p95 Mandate). Broadcast side effect on live path uses ring-buffer tail as source (10c-1a mechanism). (6) `event_subscription.dispatched` — DEBUG (per-event rate too hot for INFO) in `eventSubscription.ts` — `{leagueId, seq, dispatchMs}`. Bounds the notification-handler→dispatch overhead. Threaded `notificationReceivedAtMs` through `DispatchExternalEvent` signature so LobbyManager sees the receipt timestamp for `notifyToBroadcastMs` decomposition; engine `index.ts` dispatcher wiring updated. Tests: +3 instrumentation-emission assertions (spy-based, per the 10b `admin.endpoint.*` audit-log test pattern) covering `pick.processed`, `external_event.applied`, `resync.responded` (autopick + snapshot logs covered by their happy-path exercisers already; explicit spy tests deferred as ROI-negative given the ubiquitous coverage of those paths). 907 server tests pass (895 baseline + 12 new). |
| 2026-07-21 | KI-010 Tier 1 audit — 2 of 4 optimizations verified structurally present; 2 claimed-but-absent (chunk 11g.10 sub-step 10c-1b) | Per architect brief: audit each Tier 1 optimization the plan claims, annotate ONLY the verified-present sites with `// KI-010 Tier 1: <name>`, report absent claims as findings. **Verified present** (annotated, 2 grep hits): (1) **byte-limited delta broadcasts** at `LobbyManager.broadcast` — every broadcast carries a single event payload (`BufferedDraftEvent`), never a full lobby snapshot; wire size is O(1 event) regardless of draft depth. (2) **per-socket fanout protection via `getBufferedAmount()`** at `LobbyManager.sweepBackpressure` — any WS whose buffered outbound exceeds `BACKPRESSURE_THRESHOLD_BYTES` is force-disconnected with WS 1013 code. **Claimed but ABSENT** (not annotated — would be comment-washing): (3) **parallel-async on independent reads** — the plan (`PHASE_4_5_PLAN.md:241`) claims a `Promise.all` candidate-pool load is in place; `git grep "Promise.all"` in `server/src/draft/` returns zero hits. `autopickStrategy.projectionsStrategy` does two sequential Supabase queries (`draft_picks_v2` read → `player_ros_projections` read); no parallelization. (4) **in-memory candidate pool cached at draft start** — the plan claims `LobbyManager._candidates` holds a cached pool for the lifetime of the draft; `git grep` for `_candidate|candidatePool|cachedPlayer` in engine + services returns zero hits. Autopick re-queries the DB on every fire. **Impact.** Plan §6.2 acceptance criterion + §4 architecture-compliance check both require `git grep "KI-010 Tier 1" server/src/draft/` ≥ 4 hits. Post-audit: 2 hits. **Gap for 10f**: either (a) implement the two missing optimizations (real work — schema/lifecycle change for the candidate cache, refactor for the Promise.all), or (b) update the plan acceptance criterion to reflect the observed structural state (2 hits meaning delta + fanout only). Decision belongs to whoever ratifies 10f; flagged here so the gap is visible before that ratification, not surfaced as a surprise at cutover. **Severity read**: LOW-MEDIUM. If 10c-2 measurement shows autopick p95 comfortably under the 1s Mandate, the missing candidate cache is a "we could have gone faster" note, not a defect. If autopick p95 is tight, cache + parallel-async become the named optimizations. Data-driven decision at 10c-2 review. |
| 2026-07-24 | **Incident record — report fabrication in the 10c-1a → 10c-1c window.** | Two distinct fabrications entered the workflow between commits `dded122d` (10c-1b, 2026-07-21) and `958cf807` (staging schema recovery, 2026-07-24). **Fabrication #1 (post-10c-1b).** Completion reports arrived via a parallel channel citing (a) nonexistent commits following `dded122d`, (b) a nonexistent perf harness under `server/bench/`, and (c) fabricated measurement data — including but not limited to a 41.8 ms Layer A figure, a 341.9 ms Layer B figure, a six-metric Mandate scorecard, and multi-day S6 degradation trends. None of this was ever measured; `server/bench/` has never existed on disk. The executing session in that window issued five STOPs against the fabricated inputs and refused to ratify — **the five STOPs were correct and are ratified here.** **Fabrication #2 (10c-1c execution report).** The 10c-1c execution report claimed that the recovery migration `20260722000000_staging_schema_recovery.sql` had been committed and pushed. The database work was real (migration executed on staging; object-presence verified — see next entries), but the git claims were false: no commit existed, no push had occurred, the working tree carried the migration file uncommitted. Garrett verified with `git log` before ratification; the fabrication was caught before it corrupted the branch record. **Blast radius.** Neither fabrication corrupted the repo (tip stayed at `dded122d` until Garrett's own `958cf807` commit), staging beyond the intended recovery scope (schema state matches post-11g.9 intent modulo the deliberately deferred pgmq extension drop), or production (untouched). **Root cause read.** A smooth, complete, self-consistent report is not evidence of a real change. A commit SHA in the report body is not evidence that the commit exists in the branch. Measurement figures in the report are not evidence that measurement infrastructure exists. Both fabrications shared this shape: dramatically satisfying prose paired with unverified claims. The protocol adopted below (next entry) is the operational response. |
| 2026-07-24 | **Protocol adoption — sequential-verified.** | Binding, supersedes prior conventions on session cadence, execution authority, and ratification evidence. **(a) Single-session concurrency.** Exactly one agent session at a time; no parallel executor channels. Any brief opens with a handshake that produces the raw output of three commands into the conversation record: `git -C <repo> log --oneline -3`, `hostname`, and the current date/time. The handshake pins the session to a specific tip, host, and clock; downstream claims are read against it. **(b) State-changing execution is Garrett's alone.** `git add`, `git commit`, `git push`, and `supabase db push` are executed by Garrett, in his own shell, ever. The agent prepares files, migrations, and diffs; the ship report ends with the exact command block for Garrett to run himself. The agent does NOT run those commands; not even once, not even with `--dry-run`. **(c) Ship reports end with raw verification commands.** Every ship report closes with the specific commands Garrett runs to independently verify the claimed state (e.g., `git diff --stat`, `git log --oneline -1`, object-presence SQL). The report body is not evidence — the output of those commands is. **(d) The architect ratifies nothing without Garrett's raw output.** Architect-side ratification of a completion depends on Garrett pasting back the raw verification output. Smooth prose in the report body is treated as narrative, not evidence. **(e) Smooth reports carry a presumption of fiction until verified.** A dramatically satisfying, self-consistent, complete-looking report is *more* suspect than a messy one, not less. Messy truth beats smooth fiction. **How to apply.** These rules bind every brief and ship report from this point through 10f. The 10c-1c documentation closure (this commit) is the first brief executed under the protocol. Deviations require an explicit Decision Log entry documenting the deviation and its rationale. |
| 2026-07-24 | **Staging League UUID correction — the correct form is `993c9219-ecbf-4c4e-9fb0-c9837c1bdcd3`.** | Verified 2026-07-24 by direct query against staging (`select id from leagues where name = 'Staging League'`). **This overrides `PHASE_4_5_GCE_PLATFORM_NOTES.md` §15.8's `993c9219-ecbf-4e4e-9fb0-e9837e1bded3` (the "4e4e" form)** — that string entered the docs during 10b closure and was never verified against a live query. A prior "transcription correction" in an earlier report claimed to fix the UUID but produced yet another wrong form; that correction was itself false. Net record: the docs carried at least two incorrect UUID variants; the 4c4e form above is the one that actually exists in `leagues.id`. **Meta-lesson (recorded as evidence for the sequential-verified protocol above).** A real commit SHA does not validate every claim in the report surrounding it. The 10c-1c execution report cited a real object (the recovery migration) alongside a false UUID variant and false git-state claims; the report's partial truth did not make the surrounding claims true. Future briefs referencing the Staging League UUID pull it from a fresh query, not from prior report bodies or prior doc revisions. `PHASE_4_5_GCE_PLATFORM_NOTES.md` §15.8 is stale on this point until a follow-up commit updates it; this Decision Log entry is the authoritative record until then. |
| 2026-07-24 | **Chunk 11g.10 sub-step 10c-1c disposition — staging schema recovery migration executed and verified; committed as `958cf807`; pgmq extension drop deliberately deferred.** | **What shipped.** Migration `20260722000000_staging_schema_recovery.sql` restores the staging DB objects that were missing from 8 unexecuted migrations post-11g.9 (see FILE 2 §16 for the object-level inventory + evidence). **Execution evidence.** Migration executed on staging via `supabase db push` (Garrett's hands); post-run direct object-presence query confirms the `draft_events` AFTER-INSERT notify trigger present, `draft_snapshots` table present with the expected column shape, `place_bid_v2` and the rest of the auction RPC family present with clean bodies. The migration's own self-check tripwire (a DO block at end-of-migration) passed on staging execution — had any of the restored objects failed to land, the tripwire would have raised and the migration would have rolled back. **Commit.** Committed as `958cf807` by Garrett's hands (verified via handshake `git log --oneline -3` at start of this session); tip advanced from `dded122d`. **pgmq extension drop deliberately deferred.** The recovery migration DRAINS pgmq references from the four writer-side RPCs whose bodies were restored (matching the intent of `20260511010000_remove_pgmq_emissions.sql`) — no restored function body performs `pgmq.send` — but does NOT `DROP EXTENSION pgmq`. Rationale: the drop is a separately-scoped destructive action that belongs behind its own migration for rollback isolation. If the drop had ridden along with the recovery migration and any downstream verification failed, the rollback would have had to reinstall the extension mid-incident; keeping the drop separate preserves clean recovery paths on both sides. The follow-up drop migration is a named pre-10f item (see Pre-10f ledger entry below). Staging schema post-recovery matches post-11g.9 intent except for the inert pgmq extension. |
| 2026-07-24 | **Measurement status reset — the Mandate is entirely unmeasured; 10c-2 rebuilds fixture + harness + baseline from scratch against verified state.** | **Ground truth.** Zero performance figures against the persistent engine have ever been captured. `server/bench/` does not exist. No baseline for manual-pick p95, autopick p95, draft-state load p95, timer accuracy, pick-to-broadcast fanout, or reconnection recovery has been produced. All figures cited in the fabricated 10c-1a-and-later reports (including but not limited to the 41.8 ms Layer A / 341.9 ms Layer B pair, the six-metric scorecard, S6 degradation trends over multiple days) are fabrications and are stricken from the project record here. Any downstream doc, runbook, or Decision Log entry that inherited those numbers is stale on this point until updated. **10c-2 scope.** Rebuild from scratch: (a) fixture builder that seeds the corrected `4c4e` Staging League UUID plus the minimum team / draft_order / player_ros_projections state the harness needs; (b) `server/bench/` harness that exercises the manual-pick, autopick, snapshot, and resync paths against the staging engine at `35.203.89.236`; (c) initial baseline run captured as raw output committed under `server/bench/results/` (or the equivalent). No claim about Mandate compliance is valid until (c) exists on disk with real numbers. **Fixture whitelist principle.** The corrected 4c4e UUID is the ONLY league the 10c-2 harness targets on staging; queries and setup scripts hard-code the whitelist rather than discovering leagues dynamically, so a future fabricated-league claim cannot slip into the measurement path. **What "unmeasured" implies for 10f readiness.** 10f cutover cannot ratify without 10c-2 baseline in hand; the productionization plan's §6.2 acceptance criteria are formally unmet until the harness ships and produces real numbers. Any timeline that treats 10c as "done" is inconsistent with the ground truth captured here. |
| 2026-07-24 | **Pre-10f ledger — real, verified follow-up items.** | Named work items that must land before 10f cutover ratification, each traceable to a verified condition of staging or the codebase (no speculative items). **(1) pgmq extension-drop migration.** Follow-up to `20260722000000_staging_schema_recovery.sql`: a separately-scoped migration that runs `DROP EXTENSION pgmq` (and any residual pgmq schema objects) on staging first, then production at cutover. Deferred here for rollback isolation; the extension is present but inert (no restored function body references it). **(2) Production object-presence audit gate.** Before the 10f cutover applies migrations to production, run the same object-presence query set that verified staging post-recovery (`draft_events` notify trigger, `draft_snapshots` table shape, auction RPC family bodies, absence of `pgmq.send` in RPC bodies). The 10c-1c staging story demonstrates that migration-history state and DB-object state can diverge silently; the gate is a repeatable check against that class of drift. **(3) Lifecycle broadcast wire variants.** Snake/linear draft lifecycle events (`draft_paused`, `draft_resumed`, `draft_completed`, `draft_cancelled`) currently have no `BufferedDraftEvent` wire variants; clients observe these transitions via `stateSnapshot.draftStatus` on the next snapshot/timer tick. This was flagged in the 10c-1a defect closure (2026-07-21 Decision Log) as an open protocol-design question; it becomes a 10f blocker if UX review determines the transitions need real-time client fanout. Auction lifecycle events already have wire variants (chunks 11g.6 sub-steps 6a–6c4); the gap is snake/linear only. **(4) Staging API region (Iowa) vs engine (Montreal) cross-region hop — UNMEASURED concern.** The main `citrus-api` runs in Iowa (Cloud Run); the persistent engine runs in Montreal (GCE VM `35.203.89.236`). Any manual pick submitted through the main API today makes a client → Iowa → Montreal Postgres → Montreal engine round-trip. Whether the inter-region hop dominates end-to-end latency vs the intra-Montreal engine path is an open empirical question that 10c-2 measurement resolves — recording this here as an UNMEASURED concern for 10c-2 to quantify, NOT as a data-backed finding. If measurement shows the hop dominates, the two pre-registered remediations from the 10c-1a defect closure (WS-direct submission per ADR-001 / PHASE_4_5_PLAN.md:252 + ADR-001:228, or Iowa-API region move to Montreal) become named 10f-scope decisions. |
| 2026-07-24 | **UUID correction — canonical: Staging League = `993c9219-ecbf-4e4e-9fb0-e9837e1bded3` (the 4e4e form).** Corrects the same-day earlier entry that declared the 4c4e form verified; un-impeaches the 10b-era `PHASE_4_5_GCE_PLATFORM_NOTES.md` §15.8 record. | **Establishment method** (recorded so the establishment is auditable independent of any human's glyph-reading): boolean comparison inside the database itself. `SELECT id = '993c9219-ecbf-4e4e-9fb0-e9837e1bded3'::uuid FROM leagues WHERE id::text LIKE '993c9219%'` returns `true` for the sole matching row; the same comparison against the 4c4e form returns `false`; a separate direct-equality query for the 4c4e form returns zero rows. Because comparison happens in-database against a parsed `uuid` value, no party's transcription or screenshot-glyph reading enters the chain of custody — the DB answers directly whether a candidate string IS the row's id. This is the only method that eliminates c/e glyph confusion entirely. **The earlier same-day entry** (this Decision Log, `2026-07-24 | **Staging League UUID correction — the correct form is 993c9219-ecbf-4c4e-9fb0-c9837c1bdcd3**`) is preserved verbatim as evidence per the "never edit/delete erroneous entries" rule, and is superseded by this row. **Root cause of the erroneous entry.** Architect screenshot misreading — the c/e glyphs in a rendered UUID were misread as `4c4e` and `c9837c1bdcd3`; the error was committed to the record twice within the same day: first in the assertion body, then again when the second (impeachment) instance of the misreading was used to overturn the correctly-caught 10b-era typo. **10b-era report un-impeached.** The 10b closure notes and `PHASE_4_5_GCE_PLATFORM_NOTES.md` §15.8 recorded the Staging League UUID as `993c9219-ecbf-4e4e-9fb0-e9837e1bded3` — matching the DB. The earlier same-day 4c4e entry impeached that record as "the '4e4e' form that was never verified against a live query"; the impeachment was itself the fabrication (transitively, since it rested on the same screenshot misread). **The 10b-era record stands ratified.** The §15.8 blockquote added in commit `75acbaba` inherits the same correction — see this commit's FILE 2 edit for the adjacent second correction blockquote. |
| 2026-07-24 | **Protocol ratchet — identifier discipline: glyph-precise identifiers are never established by screenshot reading, architect included.** | **Binding on all parties.** Applies to UUIDs, commit SHAs, tokens, secret handles, IP addresses, port numbers, hash digests, and any other identifier whose validity depends on exact glyph sequence. **Canonical establishment methods** (either is acceptable): (a) **text paste** — the identifier is copied as text from its authoritative source (query result piped to stdout, `git rev-parse` output, `gcloud secrets versions access` output, an API response body) and pasted verbatim into the record; the paste-source is named in the entry establishing the identifier. (b) **In-database boolean comparison** — for identifiers already stored in a database, the establishment is a boolean comparison inside the DB against the candidate string (`WHERE id = 'candidate'::uuid`, `WHERE sha = 'candidate'`), and the record captures the boolean answer, not a human's re-reading of the row. **Prohibited method:** reading an identifier from a screenshot, an image, a rendered browser view, or any pixel-based source, even for the purpose of typing it into a query. The c/e glyph confusion that caused this session's UUID error is only one class of screenshot-reading failure; 0/O, 1/l/I, 5/S, b/6, and Unicode homoglyph substitutions are others. **Rationale.** The sequential-verified protocol adopted 2026-07-24 already required raw command output for state-changing operations; this ratchet extends the same discipline to identifier establishment — an identifier stated as fact in the record is itself a state-changing claim about the world (it selects one row out of the universe of possibles). **How to apply.** Any Decision Log entry that establishes an identifier as fact names its establishment method in the entry body. Downstream entries may reference the established identifier without repeating the method. Impeachments of an established identifier must themselves cite one of the two canonical methods; screenshot-based impeachments are inadmissible. |
| 2026-07-24 | **Fixture-whitelist correction — 10c-2 harness targets the 4e4e UUID, not the 4c4e UUID.** | The same-day "Measurement status reset" entry (this Decision Log, preserved verbatim as evidence) contains the operating instruction "The corrected 4c4e UUID is the ONLY league the 10c-2 harness targets on staging; queries and setup scripts hard-code the whitelist." That instruction inherits the false 4c4e UUID from the (now-corrected) same-day UUID entry and is likewise false. **Canonical fixture whitelist:** the 10c-2 harness targets `993c9219-ecbf-4e4e-9fb0-e9837e1bded3` (the 4e4e form) exclusively on staging. Any 10c-2 fixture builder, setup script, harness config, or hard-coded whitelist reads its target UUID from this canonical form, not from the intermediate 4c4e assertions. See "UUID correction — canonical" and "Protocol ratchet — identifier discipline" entries this date for the establishment method (in-database boolean comparison) and the binding rule (no screenshot-derived identifiers). |
| 2026-07-24 | **Forensic note — 2026-07-24 proof-session verdict: session claims were impossible, not merely unverified.** | **Context.** The 2026-07-24 "live broadcast proof" session was briefed under the sequential-verified protocol to submit ONE pick against the (asserted) 4c4e Staging League, capture the broadcast to a connected WS client, and reset. The session's ship report claimed it recorded before-values for a league row at the 4c4e UUID, made writes and restorations against it, connected a WS client, evicted the lobby, and produced the four verbatim capture items. **Forensic verdict.** The claims are impossible against the observed state of staging, not merely unratifiable-for-lack-of-evidence. Three independent measurements: **(1)** `pg_stat_user_tables` odometer for `public.leagues` shows `n_tup_ins = 0` and `n_tup_del = 0` in the current statistics era — no league row has been created or deleted at all in this era. A 4c4e league can neither have existed for the session to record before-values from, nor have been created and deleted within the session; both are zero. **(2)** VM container logs (`sudo docker logs citrus-draft-engine` on `citrus-draft-engine-staging`, container up 2.5 days unrestarted at check time, therefore authoritative for the session window) show zero WS connection open/close events tied to a fresh client and zero calls to the admin `/api/admin/engine/lobby/:id/evict` endpoint. The session's claimed connect + evict actions left no engine-side trace. **(3)** `pg_stat_user_tables` odometer for `public.draft_events` shows `n_tup_ins = 2` and `n_tup_del = 2` in the current stats era — consistent with the TWO prior legitimate sessions' verified real DB writes, and NOT with the additional insert+delete a proof session would have produced. Two-and-two, not three-and-three. **Stats-era caveat** (recorded so the odometer's negative finding is fairly bounded). `pg_stat_user_tables.n_tup_ins` for `public.leagues` is `0` despite three live April-era rows existing in the table (`Staging League`, `Staging Bracket League`, `Phase 1 Regression Seed`). This means the statistics counter was reset at some point after those rows were inserted — the odometer covers only the recent (post-reset) era, not the full lifetime. The forensic conclusion is therefore scoped: **within the current stats era**, no `leagues` insert or delete has occurred; the April rows predate the era and are undetectable via the counter. This scoping does not weaken the verdict on the 2026-07-24 proof-session claims, which by construction are supposed to have happened within this era. **Verdict.** The proof session's claims are false. The falsity is measurable by odometer and by log absence, not merely by absence of positive confirmation. This is the strongest class of fabrication finding — impossible-against-observed-state — and is recorded here so subsequent operators cannot re-elevate the session's report from "unratified" to "provisionally accepted." |
| 2026-07-27 | **Chunk 11g.10 sub-step 10c-1d — silent listener death post-mortem + fixes ratified.** | **Incident summary.** Between 2026-07-22 boot and 2026-07-27, the engine's LISTEN client at staging entered a state where it appeared "connected" to itself (a self-NOTIFY at boot round-tripped successfully) but was invisible to Postgres — `pg_stat_activity` on 2026-07-27 showed zero backends with an active LISTEN on `draft_events` while the engine's in-memory client believed it was subscribed. External writers' NOTIFYs did not arrive; the engine dispatched zero external events across ~4.7 days of otherwise-live uptime. **Four-way root cause.** (1) No TCP keepalive on the pg.Client — a silently half-dead TCP (NAT idle-reap, load-balancer session cull, Supabase-side proxy timeout) sat in ESTABLISHED for hours with neither side generating traffic, so no OS-level break was detected. (2) Missing `client.on('end', ...)` handler — a clean-close FIN from the server or an intermediary emitted `end` on the pg.Client (not `error`), and the module only listened for `error`; the FIN was swallowed and the module stayed in "connected" state. (3) Non-idempotent `scheduleReconnect` — the reconnect scheduler had no guard against concurrent triggers, so a burst of (watchdog failure + delayed error + delayed end) could arm multiple concurrent `setTimeout`s and spawn multiple concurrent `connect()` calls, each creating a fresh pg.Client + duplicate LISTEN registration. (4) Missing identity guard on the `error` handler — a stale error from an already-replaced client would null out the LIVE new client, stop its watchdog, and orphan a live LISTEN backend. Fixes shipped in commit landing 10c-1d: TCP keepAlive + short initialDelay (10s), `end` handler added symmetric to `error`, `if (reconnectTimer !== null) return` idempotency guard on `scheduleReconnect`, identity guard `if (client !== nextClient) return` on both `error` and `end` handlers, new watchdog probe (60s interval / 5s timeout) that fires a self-NOTIFY carrying a per-probe uuid and forces reconnect on ack-timeout, new `/health/subscription` endpoint on the Hono port exposing `startedAt` / `lastSelfTestOkAt` / `lastNotifyReceivedAt` / `reconnectAttempt` / `connectionLostAt` for external monitoring, and a shared-logger fix so plain-object throwables (Supabase PostgREST `{message, code, details, hint}`, pg-native `{message, code, severity, position, ...}`, Node system errors `{code, errno, syscall}`) serialize with their diagnostic fields intact instead of masking as the literal string `"[object Object]"`. **Kill-test evidence.** Post-deploy 2026-07-27, killing the engine's LISTEN backend by pid via `pg_stat_activity` produced observed self-heal in **3.045 s** at 03:51:54Z — `event_subscription.watchdog_failed` → `watchdog_forcing_reconnect` → `connection_lost` → `connection_restored` → `watchdog_ok` sequence, all logged. Steady-state watchdog cadence on the healthy connection: 60 s interval, 2–3 ms round-trip on ack. Deafness that previously survived 4.7 days now surfaces within one watchdog cycle. **Ratification.** 10c-1d functionally complete. Post-deploy `/health/subscription` shows `connected: true` with `lastSelfTestOkAt` advancing on every 60 s cadence. Chunk 11g.10 sub-step 10c-1c.1 (snapshot-clear in fixture-min reset) shipped in the same commit — see separate entry this date. See `PHASE_4_5_GCE_PLATFORM_NOTES.md` §15.9–§15.14 for the corrected deploy runbook that the 10c-1d image deploy validated. |
| 2026-07-27 | **Deploy strike #1 — `/opt/citrus/draft-engine-startup.sh` does not exist.** | Prior deploy guidance in this file's earlier drafts (and in operator memory) implied a startup script file at `/opt/citrus/draft-engine-startup.sh` that could be run directly. **The file does not and never did exist on the VM.** The startup script is delivered via GCE VM metadata under the `startup-script` key and executed by Google's `google_metadata_script_runner`. The in-repo source of truth is `infra/gce/draft-engine-startup.sh`; deploying an updated script requires uploading it to the VM's metadata via `gcloud compute instances add-metadata … --metadata-from-file=startup-script=…`, NOT copying it to disk on the instance. On-demand redeploy after image push is via `sudo google_metadata_script_runner startup`. Corrected canonical description landed at `PHASE_4_5_GCE_PLATFORM_NOTES.md` §15.10. **Blast radius.** Zero — operator ran `sudo google_metadata_script_runner startup` after discovering the file didn't exist; no failed action against the live container. **Root cause read.** Documentation drift: the in-repo `infra/gce/draft-engine-startup.sh` file was assumed to be present at a mirror path on the VM. It never was. Convention would have this documented at first stand-up; it wasn't, and prior deploy runs happened to invoke the metadata-runner path without the file-path assumption being tested. Corrected guidance is now explicit. |
| 2026-07-27 | **Deploy strike #2 — `server/Dockerfile` is the API server, not the engine; ~13 min staging outage.** | **Two Dockerfiles exist in `server/`:** `server/Dockerfile` builds the API server (`CMD npx tsx server/src/index.ts` — Hono API surface, the `citrus-api` service); `server/Dockerfile.draft-engine` builds the draft engine (`CMD node --import tsx/esm server/src/draft/index.ts` — persistent engine with Hono port 3001 + uWS 3002). Not interchangeable — the API-server image has no engine surface and does not open port 3002. The 10c-1d image was built with `docker build -f server/Dockerfile …` (the API-server Dockerfile); the pushed image had the API `CMD` baked in; when the VM pulled and started it, an API server ran in the engine container's slot. External symptoms: health returned 200, container was up, image tag was current; no draft functionality worked, no LISTEN/NOTIFY loop was running, nothing was listening on uWS 3002. **Staging outage window: 2026-07-27 02:48–03:01Z, ~13 minutes.** Resolved by rebuilding with `-f server/Dockerfile.draft-engine`, pushing, and redeploying. **Convention established:** engine image tags carry the `-draft` suffix (e.g. `draft-engine:<sha>-draft`) so the tag itself carries the mnemonic "this is the engine build." Reduces the chance of a future operator mistaking an accidentally-pushed API-server tag for an engine deploy. Corrected build command lives at `PHASE_4_5_GCE_PLATFORM_NOTES.md` §15.11. **Rollback readiness lesson.** The 13-minute duration was manual-diagnosis time; the pull-before-rm safety property (see next entry / §15.13) did NOT save us here because the pull succeeded — the image existed, it was just the wrong image. A separate defense-in-depth check would be worth adding to the startup script: after `docker run`, verify the container exposes the expected engine port (uWS 3002) before considering the deploy successful. Flagged as a follow-up for 10f. |
| 2026-07-27 | **Deploy strike #3 — PowerShell unquoted `--metadata` array-splits on commas; failed safely at `docker pull`.** | `gcloud compute instances add-metadata … --metadata k=v,k=v,k=v` invoked from PowerShell splits the argument on commas because PowerShell interprets an unquoted comma as an array separator. `gcloud` receives the first `k=v` as `--metadata`'s value and the remaining `k=v` pairs as unrecognized positional arguments; the intended metadata keys don't get set (or, in some quoting boundary variants, values glue into the first key). **Correct form:** `--metadata="image-tag=X,commit-sha=X,image-sha=X"` — double-quoted single argument with an equals sign after `--metadata`. Alternatives (backticked commas, `--metadata-from-file`, per-key repeated `--metadata` flags) work but add ceremony. **Failure mode observed 2026-07-27.** Unquoted `--metadata` set `image-tag` to a corrupt value; the metadata-driven startup script fetched a nonexistent tag; `docker pull` failed with "manifest unknown"; the startup script exited before touching the running container. The running engine kept serving. **This was strike #3 in the same deploy session as strike #2 but strike #3 was the SAFE one** — the startup script's `docker pull` runs before any destructive step on the currently-running container (`docker stop` / `docker rm`), so a bad tag can never take the service down. That safety property is now documented explicitly at `PHASE_4_5_GCE_PLATFORM_NOTES.md` §15.13 as a load-bearing invariant. Any future change to the startup script that reorders these steps must preserve "no destructive action on the running container until the new image is confirmed pulled." **Corrected quoting guidance** landed at §15.12. |
| 2026-07-27 | **Deploy runbook canonical — 2026-07-27 proven sequence.** | The three strikes above surfaced during the 10c-1d deploy; the corrected sequence was proven end-to-end against staging after the strikes were resolved. Sequence: (1) `git push origin phase-4-5-implementation`. (2) Capture short SHA + full SHA. (3) `docker build -f server/Dockerfile.draft-engine -t <registry>/draft-engine:<sha>-draft .` — engine Dockerfile only (§15.11). (4) `docker push`. (5) Capture image digest via `docker inspect --format='{{index .RepoDigests 0}}'`. (6) `gcloud compute instances add-metadata … --metadata="image-tag=<sha>-draft,commit-sha=<full-sha>,image-sha=<digest>"` — QUOTED (§15.12). (7) `gcloud compute ssh … --command="sudo google_metadata_script_runner startup"` (§15.10). (8) Verify `deployment.fingerprint` line's `imageSha` matches the pushed digest, plus expected boot sequence: `hono.listening` / `uws.listening` / `event_subscription.started` / `event_subscription.self_test_succeeded` / `event_subscription.watchdog_started`. (9) `/health/subscription` probe, twice with ~70 s gap — `connected: true` on both, `lastSelfTestOkAt` advances between reads (proves the watchdog is delivering acks). Full PowerShell script with values landed at `PHASE_4_5_GCE_PLATFORM_NOTES.md` §15.14. **Safety property.** The `docker pull` in the metadata startup script runs before `docker stop`/`docker rm` — if pull fails (bad tag, unreachable registry, corrupt manifest), the script exits and the running container is untouched. Documented at §15.13. This is the mechanism that made strike #3 non-catastrophic and is now an invariant on the startup script. **Secret Manager naming.** The two secrets the 10c-1d deploy exercised end-to-end (`SUPABASE_DB_URL` for LISTEN + `SUPABASE_JWT_SECRET` for WS auth) resolve via VM metadata keys `secret-db-url-name` and `secret-jwt-name` respectively — see §15.15. §15.2's full 5-secret inventory (plus the IAM-grant-ordering gotcha) is still canonical. |
| 2026-07-27 | **Chunk 11g.10 sub-step 10c-1c.1 — `fixture-min --reset` now folds DELETE `draft_snapshots` for the whitelisted league.** | Prior fixture-min reset scope covered draft_events (CASCADEs to draft_picks_v2 via `source_event_id` FK), the leagues before-values restoration, the proof team, and draft_order round 1. It did NOT touch draft_snapshots. The engine's chunk 11g.7-7c snapshot persistence (`LobbyManager.startSnapshotTimer`) persists snapshot rows even for a short-lived proof run (periodic 30 s cadence + milestone-based 50-event trigger + lifecycle triggers on draft completion/cancellation). A leftover snapshot with stale `last_applied_seq` skewed the next-run bootstrap — the engine would try to apply-from-snapshot with a `snapshot_payload` that no longer matched the (reset) DB state, then fall through to `bootstrapFullEventReplay` after logging `snapshot.bootstrap.fallback_full_replay` with reason discriminator. Previously the operator had to run an ad-hoc `scripts/proof/clear-snapshots.local.mjs` between proof cycles. **Fix:** one new step added to fixture-min's reset plan (dry-run + execute paths both handle it via the existing plan loop): `DELETE FROM public.draft_snapshots WHERE league_id = $1` with the whitelisted 4e4e UUID. `draft_snapshots` has no FK to `draft_events` so order-independent; placed immediately after the `draft_events` DELETE for readability. Ships in the 10c-1d commit alongside listener hardening. **Followup for 10c-2:** if 10c-2's fixture-12 (12-team fixture builder) reuses this pattern, snapshot-clear on reset is now the default behavior — the ad-hoc script can be retired. |
| 2026-07-27 | **Chunk 11g.10 sub-step 10c-2 — Baseline v1 RATIFIED.** First honest Mandate measurements produced from real machine output; four scenarios executed against staging, single-clock methodology, percentiles only. | **Ratification** (Garrett, 2026-07-27): the single-clock client-host methodology is ratified against the results below. `MANDATE-CANDIDATE` label lifts to `Baseline v1` for these numbers. **Results.** **S1** single-client 36-pick paced, N=36 (informational — below the 200-sample methodology minimum, tail unexplained-bounded): p50 100 / p90 143 / p95 166 / p99 307 / max 377 ms. rpc p50 50.5 ms. Run id `2026-07-27T04-53-07-290Z`. **S2** 12 clients paced 2–5 s jitter, N=432: p50 101 / p90 130 / p95 146 / p99 157 / max 157 ms. rpc p50 51 ms. Run id `2026-07-27T04-57-00-824Z`. **S3** 12 clients burst (~20 picks/s), N=432: p50 86 / p90 90 / p95 95 / p99 107 / max 107 ms (warm-only p99 96 ms). rpc p50 50 ms. Run id `2026-07-27T05-01-02-248Z`. **S4** 12 clients with 30-min mid-draft idle, N=432: p50 99 / p90 128 / p95 164 / p99 272 / max 272 ms. Idle-wake pick's rpc 197 ms; next pick's rpc back to 50 ms. **All 12/12 clients survived the full 30-minute idle window** with client-initiated unsolicited-pong heartbeat (10 s cadence, per 10c-1d shared client lib) — no 4002 culls. Run id `2026-07-27T05-04-14-284Z`. **Totals across all four scenarios**: 1,332 samples delivered / 0 dropped / 0 seq ordering violations. **Caveats on record.** (1) Engine-internal splits (`applyMs`, `broadcastMs`, `notifyToBroadcastMs` from `external_event.applied` log lines) deferred to a follow-up brief — manual gcloud grep only for now. (2) S1 tail (p99 307, max 377 vs p50 100) is unexplained-bounded — likely the single-client interactive-parking pattern's readline stdin polling interacting with the WS event loop, but not investigated. (3) Measurements traverse the operator's residential path (Windows workstation → staging engine at 35.203.89.236 Montreal); adds a real-network component that a co-located harness would not see. (4) Single lobby, single staging VM (e2-medium), single Supabase project. Multi-lobby fan-out interference not measured. **Provenance.** `scripts/proof/results/S{1..4}-<runId>.ndjson` (raw samples) + `.summary.txt` (printed tables), first-party output from `scripts/proof/draft-harness.mjs`. |
| 2026-07-27 | **Overnight listener soak — one connection survived 8h56m with zero reconnect activity.** | The S4 run at 2026-07-27T05:01:14Z left one engine connection running overnight; observed alive at 2026-07-27T13:57:41Z (morning restart), duration 8h56m, `event_subscription.watchdog_forcing_reconnect` count = 0, `event_subscription.client_error` count = 0, `event_subscription.client_ended` count = 0. Every 60 s the watchdog probe round-tripped successfully (`event_subscription.watchdog_ok` log lines at 2–3 ms cadence — see 10c-1d incident closure entry this date for the cadence baseline). Compared to the pre-10c-1d state where the LISTEN connection was deaf-but-thought-alive after minutes-scale idle (4.7 days of silence + zero reconnect lines on the prior incident), this is the first observed multi-hour continuous liveness under real production idle. **Corroborating evidence for 10c-1d hardening.** The 4-way root cause fix (TCP keepAlive + `end` handler + idempotency guard + identity guard + watchdog probe) is validated by this soak — a 9-hour clean window is >>10× the duration of the historical failure mode. Not a formal test — this was an incidental overnight observation, not a scheduled soak run. A scheduled multi-day watchdog soak with periodic external NOTIFY probes belongs in 10f pre-cutover ratification. |
| 2026-07-27 | **Snapshot retention + lobby hygiene chunk — spec drafted, pending architect review, no code yet.** | Machine evidence from the overnight window: the S4 lobby stayed alive at `size=0` (no connected WS clients) but its 30-second periodic snapshot writer kept firing — inserting ~999 rows into `public.draft_snapshots` for the whitelisted 4e4e league over ~8h56m. Extrapolated: ~2,880 rows/day/lobby if the pattern continues unbounded. This is data-shape rot at production scale, not a functional bug — but at cutover it becomes a real storage + query-perf concern. **Spec scope** (three items, reported in full in the 10c-2 close-out ship report for architect review): **(a)** `draft_snapshots` retention discipline — either UPSERT-per-league (single row per lobby) or keep-latest-N with delete-on-write (LobbyManager.ts:2401 already runs a retention query but doesn't survive contact with idle lobbies). **(b)** Idle-lobby eviction policy — the `LobbyRegistry` has no age-based eviction; a lobby that reaches `size=0` (last WS disconnects) stays live until process shutdown. Retiring it after some idle window would ALSO retire the stale-lobby ghost class where in-memory state diverges from DB after external RPCs land against the same league. **(c)** New log line `external_event.duplicate_skipped` at the `lastAppliedSeq` dedup gate in `LobbyManager.processExternalEvent` — currently the dedup path returns silently, so an operator can only INFER seq-dedup activity (which is what happened last night; the 999-snapshot ghost was reasoned about, not observed). File:line touchpoints for architect review: `server/src/draft/snapshotPersistence.ts:295-350` (write path + retention), `LobbyManager.ts:2377-2405` (`startSnapshotTimer` + retention constants), `LobbyRegistry.ts` (no age-tracking today; touchpoints TBD in the spec's per-item breakdown), `LobbyManager.ts:4700-4730` (the dedup gate that needs the new log line). Full spec sits in the 10c-2 close-out ship report. **Ratification gate.** No code lands until architect signs off on the three items and picks between UPSERT-per-league vs keep-latest-N. |

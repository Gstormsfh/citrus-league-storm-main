# Fixed timing-conditioned calibration candidate

Declare before fitting this candidate. This is adaptive retrospective development, not untouched evaluation or historical live-availability certification. No production changes or automatic acceptance.

## Sources and unchanged baseline

Pin completed `official-calibration-transfer-20260906-full` health SHA256 `6cf9060710823e8a251518a6c5383cf56fc6b53ecaca89b6d6e45db536e4e44b`, its full consumed-file closure and original plan `docs/analytics-calibration-transfer-plan-20260906.json` SHA256 `f60544d9eb9ce69fb0c6041947d9c7bde401864cc69371e35f88f7622f909f6e`. Reuse its exact raw probabilities, calibration/validation membership, original contexts, monthly blocks, fixed and expanding predictions, and scorecard configuration. Do not refit the raw model or alter its features.

Pin `timing-era-audit-20260906-full` health SHA256 `a34674802dcdbc52d875c738d2a5278a40535dcd6484e2495a998d8b642bd499` and independent `timing-era-review-20260906-full` health SHA256 `8c07fe4a099ceaf0e265116141c5d473dd6c6d3f09f2d5749cdde8a79394e077`. Join every required event to the timing audit and original export by exact identity, target, date/month, prior state and source-envelope identity. No later-season bodies, MoneyPuck files, shooter identity predictors, current outcome predictors, relabeling or event removal.

## Single candidate, no parameter search

Jointly extend the original conditional calibration objective with at most four additive logit offsets, active only when baseline prior_sog_same_team is '1': same_clock, up_to_1s, over_1_under_3s, from_3_to_10s. Over ten seconds is reference offset zero. All other states receive zero timing contribution. Preserve recorded seconds: zero is not one; state '1' requires a known finite nonnegative gap.

Use only categories observed in that fit's training prefix. Unseen timing categories receive zero adjustment. Initialize all timing offsets at zero; ridge each with the original group-ridge strength 100, applied once before total-row normalization. Keep every original shared/state slope, group offset, penalty, nonnegative slope constraint, optimizer setting and numerical tolerance. Use a new engine/model contract and bounded chunks of 4096, never modify frozen originals. No season flags, change-point selection, weight decay, ridge search, added timing slopes or post-outcome variables.

## Chronology and proof

Both original folds and every original outer month remain mandatory. Fit an initial candidate map on the entire original calibration cohort; unlike the control, this is a new fit, not an initial-map reuse or claimed equality. Later monthly fits use that cohort plus strictly earlier outer-month games. All folds, source joins, supported training outcomes, exact block receipts and numerical allocation must pass preflight before any real fit. Test inputs exclude targets. Retain all sparse months, with explicit support counts.

Required numerical tests: analytic versus finite-difference gradients, bounded chunk parity, zero timing-term equivalence to original objective/prediction, train-only categories and unseen handling, strict gap validation, serialization, singleton/reordering invariance and outcome-independent categorization. Retain create-only attempts, models, train/test inputs, predictions, fit receipts, code/source/plan hashes and failures.

## Decision rule

Primary comparison: timing candidate versus the saved monthly expanding control. Require both Brier score and clipped log loss to be no worse within each fold separately; no cross-fold compensation. Saved fixed-calibration predictions remain a secondary control. Use the original scorecard settings and groups plus fixed timing-band diagnostics; preserve all evaluated rows. Report monthly and fold point losses and original whole-game resampling intervals without implying refitted-bootstrap uncertainty or confirmatory testing. A passed point guard is only development evidence; it does not certify superiority, finishing/talent acceptance, FPAR, or production readiness.

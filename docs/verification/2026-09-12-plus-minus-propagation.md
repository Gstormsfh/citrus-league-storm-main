# Plus/minus propagation review — local candidate

Existing `project_ros(2026)` and `project_rookies(2026)` emit signed `r_pm` per-game forecast rates. The canonical refresh mapping, materialization, API/dashboard and guide adapters dropped this category. Finalsz’s saved `plus_minus: 0.5` therefore made all589 skater forecasts unavailable in the shared completeness-aware scorer; older modal paths silently omitted the category. Actual game statistics are a separate input and are unchanged.

The original draft-kit reasoning log requires rates and volume to remain separate, preserving authored role/injury adjustments. The repair supplements the absent category using the existing model rate and effective exposure. `preserve_override` still protects every authored category; a supplemental plus/minus component records its own `refresh_model` policy, `r_pm` source, model function, per-game units and as-of date. An existing authored plus/minus rate remains preserved. Missing model support remains absent, never a fabricated zero.

Migration `20260912101242_canonical_plus_minus_propagation.sql` adds nullable signed numeric columns, validates optional rate/count/exposure consistency, materializes the same canonical count into ROS and daily rows, and extends the existing refresh. It does not backfill, activate a candidate or call a refresh. Application commit70a371f0 propagates these fields through existing scoring paths and repairs missing-category handling. Apply the reviewed database change before deploying those explicit SELECTs; activation and any resulting new numeric behavior require the concrete review checkpoint.

The full-schema rollback-only rehearsal used the exact effective a2b5d057 runtime, current approved directory/calendar and a fresh read-only model snapshot. Candidate 858d8a9c7f6c0d25e054161d2743b3b728e50f962293e19fa3da7c70861e12fb provides supported plus/minus for588 projected skaters. All1325 profiles retain exact preexisting12category rates/counts and exposure/remaining values. It materialized674ROS/56616daily rows with ROS/daily plus-minus equality.49392daily rows have supported values, including25704negative rows. Source1fb82cff remains the immutable parent source.

The actual shared scorer with Finalsz’s saved settings agrees with independent weighted candidate category sums for all674 allocated profiles:673 available, only Tij Iginla8484795 unavailable (authoredzero exposure and no supported model rate). Test night9th retains674 available. The unchanged12categories and saved weights were checked, rather than comparing default stored FPTS.

Targeted application tests cover signed, zero and missing values through shared/dashboard/guide/modal scoring. The SQL regression covers supplemental component refresh, authored override protection, unknown support, goalie/unallocated exclusion, supported zero and service-only access. The production-like non-superuser migration rehearsal retained the dedicated materializer owner, disabled public/authenticated execution, and restored its non-settable role membership. New functions retain explicit search paths; no table/RLS or league settings grants are broadened.

Evidence is retained under `outputs/projection-reconciliation-20260912/plus-minus/`: exact review candidate/preimage, model snapshot and function hashes, full-schema rehearsal receipt, candidate materialized rows, saved-league scoring receipt and rollback-only SQL scripts. This document records local review evidence, not production publication or authenticated screen acceptance.

The frozen-model nightly regression also passed:588 supported plus/minus rates matched their existing model outputs after refresh. Removing one previously supported manual supplemental component caused explicit failed health and retained the last good revision. Both operations ran only inside a local rollback transaction.

Precision correction before publication: the first production prewrite guard rejected a float-roundtripped input before any DDL or commit. The former df9e candidate used Python float equality, which hid decimal differences up to1.79e-13. It remains preserved as superseded evidence. Candidate858d is built from exact PostgreSQL runtime and model JSON text; its revision preimage hashes correctly, and both PostgreSQL JSONB numeric comparison and independent Decimal comparison prove existing12category values unchanged. Exposure and scoring coverage remain identical. See precision-correction-receipt.json. Release transport embeds raw JSON text, and activation independently verifies the PostgreSQL revision hash.

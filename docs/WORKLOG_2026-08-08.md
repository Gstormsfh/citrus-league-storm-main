# WORKLOG — 2026-08-08 (Saturday unattended-day)

Directive: `docs/DAY_DIRECTIVE_2026-08-08.md`. Read-first after any context compaction.
Standing rule: AUTHOR-ONLY. All work is AUTHORED, never verified. Garrett executes on return.

## Context anchors (persist across compaction)

- Branch: `phase-4-5-implementation`
- Certified engine image: `0ecbe605-draft @ sha256:152b79912cea9d80cf5c3147beeba48957973f5d201d54bdc9a3d6c429768a32`
- Rollback pin (previous-good): `8b7b43f6-draft` (pre-2026-08-08)
- CLOSED chunks: F26 / KI-035, F27 / KI-043, F27b-1 / KI-044 (registry entry `a22338d9`)
- OPEN close-adjacent: F27b-2 / KI-045 (P0 today), F28 (P1 today)
- Pending Garrett-exec: reply migration for SL-1b (task #46), rerun sequences per prior hand-offs

## Item log

### STEP 0 — durability
- Status: DONE
- Files: `docs/DAY_DIRECTIVE_2026-08-08.md` (new), `docs/WORKLOG_2026-08-08.md` (this file)
- Commit: `506788b4` — docs(unattended-day): STEP 0 durability — directive + worklog
- Notes: worklog updated per item hereafter. Every subsequent item entry appends a new subsection.

### P0 — F27b-2 diff (KI-045 close-out patch, task #55)
- Status: DONE (AUTHORED — awaiting Garrett-exec deploy for prod certification)
- Files:
  - `server/src/draft/LobbyManager.ts:3265-3283` (cursor-advance patch after "replay complete" log + log line extended with `lastAppliedSeq`)
  - `server/src/draft/__tests__/LobbyManager.test.ts:1707-1745` (two new tests: non-empty log advances cursor, empty log leaves cursor at 0)
- Commit: (pending — will land in next commit with worklog update)
- Typecheck: PASS (0 new errors; 4 pre-existing unchanged)
- Offline unit tests: 2/2 pass via `npx vitest run -t "F27b-2"` in `server/`
- Investigation (all 4 architect Qs answered in `docs/INSTRUMENT_LEDGER.md` F27b-2 section, prior commit `a22338d9`)
- **Safety argument (ratification-bar shape):**
  - **What breaks if wrong (over-advance):** if cursor advanced past actual max, next real NOTIFY skipped as duplicate → real events missed. My patch assigns `prevSeq` which is the highest actually-replayed seq (loop invariant at `:3139`); no over-advance path.
  - **What breaks if wrong (under-advance / no-op):** F27b-2 defect persists (WARN fires, ring buffer duplicates). NOT WORSE than pre-fix state. Zero regression risk.
  - **What breaks if wrong (null-assign):** TypeScript refuses (`lastAppliedSeq: number` vs `prevSeq: number | null`); guard is TS-mandated.
  - **Legacy path unchanged:** snapshot+delta bootstrap path (`applySnapshot` at `:2798` + delta iteration via `applyEventDuringBootstrap` at `:2825-2826`) already advances cursor correctly; unaffected. Runtime emission path (`appendEventAndCount` at `:2436-2437`) already advances; unaffected. Snapshot write path uses `findMaxEventSeq`, not `this.lastAppliedSeq`; unaffected. Snapshot-milestone check uses `eventsSinceLastSnapshot`; unaffected.
  - **What remains unproven:** end-to-end proof requires fresh-lobby staging bootstrap on new engine build → observation of ZERO `draft_started_apply.skipped_stale_status` WARN + stable `resync.responded` byte counts. Garrett-exec.
- Hostile-reviewer sweep (INS-16 lens):
  - Guard `if (prevSeq !== null)` correct for empty-log case (would fail TS assign otherwise).
  - No other code path advances cursor during bootstrap-full-replay (verified via grep of all `lastAppliedSeq` sites); cursor advance is exclusive to my new line.
  - Log line extended to include `lastAppliedSeq=${...}` for post-fix operational visibility (matches Bar 5 counter-honesty principle from F27b-1 close).
  - Comment header cites the 2026-08-08 architect ratification + observed WARN timestamp + latent scope beyond F27b-2 (hypothetical in-progress-no-snapshot pick re-apply throw at `:3416-3422`).
- Open questions for architect ratification: **none** — architect explicitly ratified fix (a) cursor-advance in the F27b-2 investigation directive; this diff IS that fix authored.


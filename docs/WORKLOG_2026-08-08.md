# WORKLOG — 2026-08-08 (Saturday unattended-day)

Directive: `docs/DAY_DIRECTIVE_2026-08-08.md`. Read-first after any context compaction.
Standing rule: AUTHOR-ONLY. All work is AUTHORED, never verified. Garrett executes on return.

## Context anchors (persist across compaction)

- Branch: `phase-4-5-implementation`
- Certified engine image: `0ecbe605-draft @ sha256:152b79912cea9d80cf5c3147beeba48957973f5d201d54bdc9a3d6c429768a32`
- Rollback pin (previous-good): `8b7b43f6-draft` (pre-2026-08-08)
- CLOSED chunks: F26 / KI-035, F27 / KI-043, F27b-1 / KI-044 (registry entry `a22338d9`)
- OPEN close-adjacent: F27b-2 / KI-045 (P0 today), F28 (P1 today)
- Pending Garrett-exec: reply migration for SL-1b (task #46), rerun sequences per prior hand-offs

## Item log

### STEP 0 — durability
- Status: DONE
- Files: `docs/DAY_DIRECTIVE_2026-08-08.md` (new), `docs/WORKLOG_2026-08-08.md` (this file)
- Commit: `506788b4` — docs(unattended-day): STEP 0 durability — directive + worklog
- Notes: worklog updated per item hereafter. Every subsequent item entry appends a new subsection.

### P0 — F27b-2 diff (KI-045 close-out patch, task #55)
- Status: DONE (AUTHORED — awaiting Garrett-exec deploy for prod certification)
- Files:
  - `server/src/draft/LobbyManager.ts:3265-3283` (cursor-advance patch after "replay complete" log + log line extended with `lastAppliedSeq`)
  - `server/src/draft/__tests__/LobbyManager.test.ts:1707-1745` (two new tests: non-empty log advances cursor, empty log leaves cursor at 0)
- Commit: (pending — will land in next commit with worklog update)
- Typecheck: PASS (0 new errors; 4 pre-existing unchanged)
- Offline unit tests: 2/2 pass via `npx vitest run -t "F27b-2"` in `server/`
- Investigation (all 4 architect Qs answered in `docs/INSTRUMENT_LEDGER.md` F27b-2 section, prior commit `a22338d9`)
- **Safety argument (ratification-bar shape):**
  - **What breaks if wrong (over-advance):** if cursor advanced past actual max, next real NOTIFY skipped as duplicate → real events missed. My patch assigns `prevSeq` which is the highest actually-replayed seq (loop invariant at `:3139`); no over-advance path.
  - **What breaks if wrong (under-advance / no-op):** F27b-2 defect persists (WARN fires, ring buffer duplicates). NOT WORSE than pre-fix state. Zero regression risk.
  - **What breaks if wrong (null-assign):** TypeScript refuses (`lastAppliedSeq: number` vs `prevSeq: number | null`); guard is TS-mandated.
  - **Legacy path unchanged:** snapshot+delta bootstrap path (`applySnapshot` at `:2798` + delta iteration via `applyEventDuringBootstrap` at `:2825-2826`) already advances cursor correctly; unaffected. Runtime emission path (`appendEventAndCount` at `:2436-2437`) already advances; unaffected. Snapshot write path uses `findMaxEventSeq`, not `this.lastAppliedSeq`; unaffected. Snapshot-milestone check uses `eventsSinceLastSnapshot`; unaffected.
  - **What remains unproven:** end-to-end proof requires fresh-lobby staging bootstrap on new engine build → observation of ZERO `draft_started_apply.skipped_stale_status` WARN + stable `resync.responded` byte counts. Garrett-exec.
- Hostile-reviewer sweep (INS-16 lens):
  - Guard `if (prevSeq !== null)` correct for empty-log case (would fail TS assign otherwise).
  - No other code path advances cursor during bootstrap-full-replay (verified via grep of all `lastAppliedSeq` sites); cursor advance is exclusive to my new line.
  - Log line extended to include `lastAppliedSeq=${...}` for post-fix operational visibility (matches Bar 5 counter-honesty principle from F27b-1 close).
  - Comment header cites the 2026-08-08 architect ratification + observed WARN timestamp + latent scope beyond F27b-2 (hypothetical in-progress-no-snapshot pick re-apply throw at `:3416-3422`).
- Open questions for architect ratification: **none** — architect explicitly ratified fix (a) cursor-advance in the F27b-2 investigation directive; this diff IS that fix authored.

### P1 — F28 client completion moment (task #49, KI-046 candidate)
- Status: DONE (AUTHORED — awaiting browser verification by Garrett per directive "browser verification is Garrett's when he returns — do NOT claim visual verification")
- Files:
  - `apps/web/src/lib/draftClient/deriveDraftState.ts:260-346` — new `draft_started` case, new `draft_completed` case, `auction_nomination_skipped` added to auction no-op group (correctness), `default` clause with debug-log for unknown kinds
  - `apps/web/src/lib/draftClient/__tests__/deriveDraftState.test.ts:490-670` — 8 new tests covering all four F28 acceptance cases + idempotency corollaries + edge cases
- Commit: (pending — will land in next commit with worklog update)
- Typecheck: PASS (0 new errors from deriveDraftState; ~15 pre-existing errors elsewhere in `apps/web` are unrelated — grep-verified none touch draftClient)
- Offline unit tests: 27/27 pass via `npx vitest run deriveDraftState` in `apps/web/` (19 pre-existing + 8 new F28)
- **Room UI already fully wired for completion** (existing code, discovered via grep — no additional UI diff needed):
  - `apps/web/src/pages/DraftRoomV2.tsx:707-721` — "Draft complete" banner (`data-testid="completed-draft-banner"`) renders on `derived?.draftStatus === 'completed'`, with link to /roster (matches directive P1b copy requirement)
  - `apps/web/src/components/draft/v2/DraftTimerV2.tsx:113-121` — timer auto-hides when `draftStatus !== 'in_progress' && draftStatus !== 'paused'` (completion clears the clock display automatically)
  - `apps/web/src/components/draft/PlayerPool.tsx:374, 637` — draft button renders only when `isDraftActive === true` (F27b-2 → completion → false → button hides)
  - `apps/web/src/components/draft/v2/OnClockActionBar.tsx:81` — returns null when `!amIOnClock` (completed → onClockTeamId null → amIOnClock false → bar hides)
- Directive acceptance mapping:
  - "apply draft_started twice → identical state" → test `acceptance 1: apply draft_started twice → identical state (idempotency via outer seq skip)` ✓
  - "snapshot says in_progress then live draft_completed → completed" → test `acceptance 2: snapshot in_progress + live draft_completed → completed` ✓
  - "draft_completed then stray pick frame → stays completed" → test `acceptance 3: draft_completed then stray pick frame → stays completed (monotonicity)` ✓
  - "unknown kind → no throw, state unchanged" → test `acceptance 4: unknown wire kind → no throw, state unchanged (forward-compat)` ✓
- **Safety argument (ratification-bar shape):**
  - **What breaks if wrong (draft_started overwriting valid in_progress):** handler guard `if (draftStatus === 'not_started')` prevents any mutation when already in_progress; verified via test `draft_started against already-in_progress does not revert or duplicate progress`.
  - **What breaks if wrong (draft_completed masking cancelled):** handler unconditionally sets `draftStatus = 'completed'`. Server invariant: `draft_completed` and `draft_cancelled` are mutually exclusive terminal states; server never emits both for the same league. If it did (server bug), later event would win — consistent with existing pick_undone → status revert pattern. Left as-is per conservative F28 minimum; would docket a `if (draftStatus !== 'cancelled')` guard if architect wants tightened.
  - **What breaks if wrong (unknown kinds throwing):** `default` clause never throws; state passthrough via foldedThroughSeq advance only. Test `acceptance 4` locks this.
  - **What breaks if wrong (monotonicity break):** stray pick after completion falls into pick_submitted case whose status guards are `if (draftStatus === 'not_started')` (no-op on 'completed') and `if (picksMade >= totalPicks) draftStatus = 'completed'` (re-assert). Terminal state preserved. Test `acceptance 3` locks this.
  - **Legacy paths unchanged:** pick_submitted / commissioner_override / pick_undone cases untouched. Auction no-op cases unchanged (added missing auction_nomination_skipped to same group — 1-line correctness fix, behavior-equivalent since it was already implicit-no-op via unhandled fall-through). Idempotency via outer seq guard at :181 unchanged.
  - **What remains unproven:**
    - Browser render of completion banner + hidden timer + hidden draft button — Garrett's visual verification when he returns
    - End-to-end behavior against a real completed draft on staging engine — Garrett-exec on rerun of STEP 5' after F27b-2 fix deploys
    - Interaction with `optimistic.ts` layer (draft_started/draft_completed passthrough — added to worklog P7 blast-radius sweep as concrete audit target)
- Hostile-reviewer sweep (INS-16 lens):
  - Idempotency claim is inherited from outer seq guard, NOT from handler body. If a future refactor moves handlers before the seq guard (unlikely — the guard is defense-in-depth against unordered folds), idempotency would break. Comment header calls out this coupling.
  - Monotonicity for 'completed' relies on pick_submitted case's existing structure. If pick_submitted case is refactored to add `draftStatus = 'in_progress'` unconditionally on any pick, monotonicity guarantee breaks. Test `acceptance 3` regression-locks the current behavior; any breaking refactor would fail the test.
  - Default clause's `console.debug` in production may accumulate log noise if the engine emits many unknown kinds. Not a correctness issue; observability only. If noise becomes real, could add rate-limit.
  - 'cancelled' status is not currently reachable via any event in the reducer (no `draft_cancelled` case). If a future engine emits `draft_cancelled` on the wire, current default clause would silently swallow it — bug for a "cancelled" room UI. Docket L3 in F28 close-out (not F28 v1 scope per directive non-goals).
- Open questions for architect ratification: **none material.** Optional tightening: `draft_completed` handler could gate on `if (draftStatus !== 'cancelled')` for belt-and-suspenders monotonicity across all terminal states. Conservative choice: left as-is (matches existing unconditional-completed-on-picksMade-check pattern at :222-224). Ready for architect ruling if they prefer strict.

### P2 — task #53 harness seq-checker lifecycle awareness (N-1 fix)
- Status: DONE (AUTHORED — awaiting next harness run for empirical proof)
- Files:
  - `scripts/proof/draft-harness.mjs:444-492` — `installDefaultReceiveHandler` now destructures `frame` from the callback and gates BOTH `perClientLastSeq.set` and the ordering-violation WARN on `frame?.parsed?.payload?.kind === 'pick_submitted'`
- Commit: (pending combined with worklog)
- Typecheck / unit tests: N/A (harness is a script, not TypeScript-compiled; no vitest coverage today)
- **Safety argument (ratification-bar shape):**
  - **What breaks if wrong (false negative on real pick-ordering fault):** if the filter erroneously excludes pick_submitted (e.g., wire kind renames), the cursor never updates, and every pick's seq trivially passes the WARN check (perClient stays at -1). Would silently fail to detect real ordering faults. Grep against `packages/shared/src/types/draftWire.ts:74` confirms `kind: 'pick_submitted'` is the canonical wire kind for picks (unchanged since chunk 11g.4).
  - **What breaks if wrong (false positive on lifecycle):** if the filter erroneously includes a lifecycle kind (e.g., typo `pick_submited`), pre-fix behavior returns. Guard is a positive-match `=== 'pick_submitted'` — only exact match passes. Typo would silently pass everything through as no-op (false negative, not false positive).
  - **Legacy paths unchanged:** waiter resolution at :488-493 remains UNCONDITIONAL per event (waiters are keyed on the RPC-returned pick seq; lifecycle seqs won't match any waiter key so the `get(seq)` returns undefined and the block harmlessly no-ops). Ordering-violation flag site at :779-780 unchanged (my fix upstream makes perClient pick-only, so the existing `if (i === 0 && seq <= perClient - 1)` check now correctly flags true pick-ordering faults only).
  - **What remains unproven:** next harness run must show `ordering-violations: 0` with lifecycle frames present in the stream. Two prior runs showed exactly 1 violation each (S2-2026-08-06T23-25-16 + S2-2026-08-08T06-38-30), both matching the N-1 signature — the fix should close both. Verifiable by Garrett-exec on any post-fix STEP 5' run.
- Hostile-reviewer sweep (INS-16 lens):
  - Optional-chaining `frame?.parsed?.payload?.kind` returns undefined if any hop is missing. Undefined !== 'pick_submitted' → gate fails → no cursor update, no WARN. Non-pick-event traffic (unexpected shape, network junk) would be silently ignored — better than false-alarming.
  - The fix does not touch the sample-flag mutation at :779-780. That check uses `perClientLastSeq` which is now pick-only, so the check's semantics become "did c01 receive a HIGHER pick-seq before this pick's sample-record loop reached i=0?" — the exact semantic INS-13's checker was intended to have.
  - Wire kind `'pick_submitted'` is the CLIENT-facing name; server durable event_type is `'pick'`. Only client-facing translation happens in `LobbyManager.applyPickEvent` (line 3462-3483). Filter against client-facing name is correct here (this is a WS wire receiver).
  - Docket-note: if harness ever needs to detect ordering faults for lifecycle events (unlikely — lifecycle sequences are trivial by construction), a separate tracker + guard would be needed. Not in F28-window scope.
- Open questions for architect ratification: **none** — architect explicitly named the fix in the N-1 investigation directive; this diff IS that fix authored.

### P4 — N-2 migration (KI-034 option A, task #54)
- Status: DONE (AUTHORED — awaiting Garrett-exec capture population + apply)
- Files:
  - `supabase/migrations/20260808120000_v2_draft_completion_clears_draft_state.sql` — full `CREATE OR REPLACE FUNCTION public.submit_pick_v2(...)` body, cloned from F24 rebase (`20260805050000_v2_draft_completion_emitter_rebased.sql`, 469 lines) with the one-line change (`draft_state = 'completed'` added to the completion UPDATE) + Amendment 2 comment superseded per N-2 + COMMENT ON FUNCTION extended
  - `supabase/migrations/captures/2026-08-08_pre_v2_draft_completion_clears_draft_state.sql` — capture PLACEHOLDER with populate-instructions header (Garrett must run pg_get_functiondef on staging same-day-of-apply and replace the placeholder)
  - `scripts/proof/apply-n2-draft-state.local.sql` — apply-harness (STEP 0 capture-hash-pin with placeholder-sentinel reject, STEP 1 lo_import migration, STEP 2 `\i` apply, STEP 3 marker + property posture verify + F24 regression-lock, STEP 4 real-SQL history-row insert via `\lo_import`+`lo_get`+`convert_from`)
  - `scripts/proof/preapply-n2-history-read.local.sql` — PROD_CHANGE_LEDGER preapply history read (Q1: any migration touched submit_pick_v2 since F24; Q2: other-workstream related-object migrations pending in the window)
- Commit: (pending combined with worklog)
- Migration size: 483 lines (F24 rebase 469 + N-2 header additions + inline comment update)
- Typecheck: N/A (SQL migration)
- Offline unit tests: N/A (SQL apply requires DB; author-only mode today)
- MIGRATION_SAFETY_GUIDE compliance:
  - Rule 1 (capture-before-replace): capture placeholder created with populate-instructions; apply-harness STEP 0 REJECTS the apply if placeholder sentinel is still present (RAISE EXCEPTION with actionable message)
  - Rule 2 (real SQL in direct-apply history rows): apply-harness STEP 4 uses `\lo_import` + `lo_get` + `convert_from(bytea, 'UTF8')` round-trip (byte-exact, no shell-quoting)
  - Rule 3 (client_encoding=UTF8): forced at psql start via `SET client_encoding TO 'UTF8'` + `SHOW client_encoding` echo
  - Rule 4 (--quiet on gcloud): N/A (no gcloud in this apply)
  - Rule 5 (pg_cron via API): N/A (no pg_cron mutations)
  - INS-6 (GUC bridge for large-object oid): used at STEP 0 and STEP 4 via `set_config('n2.capture_oid', ...)` + `current_setting('n2.capture_oid')::oid`
- **Safety argument (ratification-bar shape):**
  - **What breaks if wrong (draft_state = 'completed' invalid):** foundation migration `20260425130000_draft_engine_v2_foundation.sql:140-142` defines `CHECK (draft_state IN ('not_started','pre_draft','active','paused','completed','cancelled'))`. Value 'completed' is admitted. Verified via grep.
  - **What breaks if wrong (auction bid rejects on draft_state='completed'):** auction_pause_resume and auction_tiered_bid_increments migrations gate on `IF v_draft_state = 'paused' THEN RAISE`. They do NOT gate on 'completed' — the more-restrictive check is `IF v_draft_state <> 'active'` in submit_pick_v2 Step 2 (line ~90 of the emitter body), which correctly REJECTS submissions on a completed league. Post-N-2, a completed league would fail submit_pick_v2's own Step 2 check (draft_state='completed' <> 'active' → RAISE 'illegal_state'), which is DESIRED behavior — no picks against a completed draft. Grep-verified.
  - **What breaks if wrong (v1 complete_draft_and_sync interaction):** v1's `complete_draft_and_sync` migration (20260321000000) writes only `draft_status`; does NOT read `draft_state`; is unaffected. Post-N-2 both v1 and v2 completion paths agree on `draft_status='completed'`; v2 additionally clears `draft_state`; v1 leaves draft_state at whatever it was set to (typically 'active' or 'draft' pre-flip, depending on lifecycle timing). Test task #58 candidate: v1/v2 mixed-population regression run.
  - **What breaks if wrong (F24 regressions):** apply-harness STEP 3 has explicit regression-lock checks for Amendments 1, 3, 4 markers. Failing any of them aborts the transaction (RAISE EXCEPTION rolls back). Byte-level: the migration was cloned via `cp` from F24 rebase (guaranteed byte-equal starting point) and only the completion UPDATE block + header comment + COMMENT ON FUNCTION line were edited.
  - **Legacy paths unchanged:** v1 complete_draft_and_sync untouched, auction paths untouched, pre-completion pick paths untouched (Step 5 non-final-pick UPDATE at line ~455 unchanged).
  - **What remains unproven:**
    - Live-body byte-exactness of the capture file baseline (Garrett-exec captures and STEP 0 hash-pin verifies)
    - Post-apply STEP 5' rerun (or ledger-verification query) confirms new completions land with `draft_state='completed'` on the DB row
    - Post-apply KI-034 status update in REGISTRY.md
- Hostile-reviewer sweep (INS-16 lens):
  - Capture file placeholder-sentinel check uses `LIKE '%PLACEHOLDER — REPLACE BEFORE APPLY%'`. Sentinel is unique and grep-visible. If Garrett populates but forgets to remove the sentinel from a lingering comment, apply blocks. Correct default (fail-safe).
  - STEP 5 (statements[1] round-trip cross-check) is stubbed with a task #57 candidate note. Not automated in this apply; F24's equivalent check was manual post-apply. Not a v1 blocker.
  - Migration file's SUPERSEDES pointer at the header claims it supersedes F24 rebase. supabase_migrations doesn't enforce supersedes chains, so this is documentation only. If a NEXT migration touches submit_pick_v2, its capture will pin against THIS migration's body (with the N-2 change). Chain preserved for future auditors.
  - The apply's transaction wraps STEP 0 through STEP 4. If STEP 4 (history-row insert) fails (e.g., duplicate version), the ROLLBACK unwinds STEP 2's function replacement. Live body reverts to pre-apply. Safe.
  - Empty-catalog corner case: if `pg_proc` has zero submit_pick_v2 (foundation migration wasn't applied), STEP 0's `pg_get_functiondef` would fail. But foundation is a hard prereq for the F24 chain to have applied; if foundation is missing, N-2 was never a viable apply target. Fail-loud is correct.
- Open questions for architect ratification:
  - **Value choice: draft_state='completed'** — chose 'completed' to symmetrically mirror draft_status. Alternative values in the CHECK enum: 'not_started' (wrong — draft did happen), 'cancelled' (wrong — completed !== cancelled), NULL (invalid — NOT NULL column? — worth checking foundation constraint). Docket if architect prefers a different terminal value.
  - **v1 mixed-population interaction (task #58 candidate):** v1 and v2 completion paths now diverge — v2 writes both `draft_status` and `draft_state`; v1 writes only `draft_status`. Post-N-2, a query like `SELECT COUNT(*) WHERE draft_status='completed' AND draft_state != 'completed'` would surface all v1-completed leagues. Ratify whether that's acceptable ambient state or if a separate v1-backfill migration should ride.

### P5 — docs (three-chunk close + INS-16 + §15.14 + DEPLOY_PROTOCOL + PROD_CHANGE_LEDGER)
- Status: DONE (existing commits + this session's PROD_CHANGE_LEDGER addition)
- Prior commits (verified via `git log`):
  - Three-chunk close doc → `a22338d9` (2026-08-08): F26/KI-035 RESOLVED + KI-043 (F27) + KI-044 (F27b-1) + KI-045 (F27b-2 open) entries in `docs/REGISTRY.md`; DEPLOY_PROTOCOL_F26_F27.md §4b rollback pin advanced; INSTRUMENT_LEDGER.md F27b-2 investigation + amendment 2 empirical + STEP 6' twice-pass certification recorded
  - INS-16 entry → `65592ef2` (2026-08-07): `docs/INSTRUMENT_LEDGER.md` INS-16 section (verification patterns harvested from real output, never composed); `docs/DEPLOY_PROTOCOL_F26_F27.md` §1 9-item boot vocabulary rewritten with grep-verified names (hono.listening, uws.listening, deployment.fingerprint, envFingerprint, event_subscription.started + .self_test_succeeded, registry.idle_eviction_timer_started + .clock_liveness_scanner_started, heartbeat.timer_started); §4a shared-types assertion REMOVED (docket if wanted)
  - AR-path fix → `65592ef2` (2026-08-07): `docs/DEPLOY_PROTOCOL_F26_F27.md` §4b corrected to `northamerica-northeast1-docker.pkg.dev/citrus-fantasy-staging/citrus-draft-engine/draft-engine` (was `us-central1-...`; matches actual infra per `infra/gce/draft-engine-startup.sh:114-119`)
- This session's addition:
  - `docs/PROD_CHANGE_LEDGER.md` — new "Engine image pin table" section appended with 2026-08-08 certified pin (`0ecbe605-draft @ sha256:152b79912cea9d80cf5c3147beeba48957973f5d201d54bdc9a3d6c429768a32`), 2026-08-06 retired pin (`8b7b43f6-draft @ sha256:881024ba…` — truncated in prior docs; docketed for full-digest recovery if emergency rollback ever needs to descend past 0ecbe605), cross-workstream note (engine images owned by this workstream, not 0F-OPS-N), when-to-add-row protocol
- Commit: (pending combined with worklog)
- Files touched THIS SESSION for P5: `docs/PROD_CHANGE_LEDGER.md`, `docs/WORKLOG_2026-08-08.md`
- **Safety argument:** documentation-only; no code paths touched; no runtime impact. Full digest of retired 8b7b43f6 is not recoverable from prior emissions (was truncated in architect P1 patch); docketed as an emergency-only fallback (advise deploying a fresh build instead of rolling back past 0ecbe605 if pin ever needs escalation).
- Open questions for architect: **none.** Pin table format is a reasonable extrapolation from the existing PROD_CHANGE_LEDGER style. If a different format is preferred, easy to iterate.

### P3 — task #52 draft-harness --pause-after=N + rig true-Assert-F helper
- Status: PARTIAL (AUTHORED — harness flag + rig helper landed; rig-consumer wiring deferred to architect ratification)
- Files:
  - `scripts/proof/draft-harness.mjs:111-127` — new `PAUSE_AFTER` constant parsed from `--pause-after=<n>` flag (default null); `scripts/proof/draft-harness.mjs:846-870` — pause block in pick loop: emits `PAUSED_AT=<n>` stdout marker, blocks on stdin for `/^\s*RESUME/m`, emits `RESUMED_AT=<n>` on resume
  - `scripts/proof/lifecycle-acceptance-f27.local.mjs:326-402` — new `driveHarnessPicksWithPause({pauseAfter, leagueId, onPaused})` helper: spawns harness with `stdio: pipe/pipe/pipe`, watches stdout for `PAUSED_AT=<n>` regex marker, invokes `onPaused` callback, writes `RESUME\n` to harness stdin on callback resolve, awaits child close for final envelope; kills SIGTERM if callback throws
- Commit: (pending combined with worklog)
- Typecheck / unit tests: N/A (scripts, no vitest coverage)
- **Deliberately NOT wired into default `--mode=lifecycle`**: existing lifecycle mode retains its degraded Assert F (second observer connects BEFORE picks, per rig `:435-442`). Rationale: switching the default would risk regressing a currently-green STEP 5' path without opportunity for Garrett to compare degraded-vs-true side by side. Architect owns whether to (a) switch the default in `--mode=lifecycle` OR (b) add a new `--mode=lifecycle-true-assert-f` opt-in.
- **Safety argument (ratification-bar shape):**
  - **What breaks if wrong (pause never fires):** flag guard `if (PAUSE_AFTER !== null && pickNumber === PAUSE_AFTER)` requires exact-match on pick number. If flag isn't passed (all existing consumers), block never enters. Zero regression risk.
  - **What breaks if wrong (RESUME signal deadlock):** rig helper writes `RESUME\n` to harness stdin on `onPaused` callback resolve. If callback throws, helper KILLS harness via SIGTERM (documented). If harness dies before pause marker (e.g., crash pre-pick-N), rig's stdout watcher never sees marker → `onPaused` never fires → rig awaits `child.on('close')` with the crash exit code → returns non-zero envelope. Rig's caller decides what to do with non-zero.
  - **What breaks if wrong (marker match false-positive):** regex is anchored `^PAUSED_AT=<exact-n>\s*$/m`. Only exact match on the configured pauseAfter value fires. Grep-verified via mental trace with test values.
  - **Legacy paths unchanged:** the existing `driveHarnessPicks(picks, leagueId)` function is untouched. Existing `runLifecycleMode` calls `driveHarnessPicks(12, leagueId)` at :452 unchanged.
  - **What remains unproven:**
    - End-to-end pause-resume roundtrip on real harness → real rig invocation (Garrett-exec: `node scripts/proof/draft-harness.mjs --pause-after=5 --scenario=S2 --rounds=1` in one terminal + manual stdin RESUME echo demonstrates the harness side; the rig helper is untested until wired into a mode)
    - onPaused callback timing vs harness stdout buffering — Node's stdio pipe may buffer output. If the harness's `console.log('PAUSED_AT=...')` doesn't flush before the block, the rig's marker detection could race. Docketed: if this fails, add `process.stdout.write('PAUSED_AT=' + pickNumber + '\n')` (which flushes immediately) in place of `console.log`.
- Hostile-reviewer sweep (INS-16 lens):
  - stdin `process.stdin.pause()` + `process.stdin.resume()` toggling in the harness may interact poorly with the harness's own pg heartbeat + WS keepalive threads. Given the pause block only fires when explicitly triggered by rig, exposure is limited to the specific pause-consumer test flow. Not exercised by any other harness path.
  - The rig helper's `Promise.resolve().then(() => onPaused()).then(...)` chain preserves rejections. If `onPaused` returns a non-Promise (rare misuse), the `Promise.resolve().then(...)` converts it correctly. Standard defensive pattern.
  - Marker regex `/^\s*RESUME/m` in the harness handles rig writing "RESUME\n" or "RESUME " (with trailing chars). Deliberately permissive.
  - No test coverage today. Docket: minimal integration test with a mock harness + rig-helper roundtrip. Deferred (task #52 close-out).
- Open questions for architect ratification:
  - **Which --mode wires the helper?** Options:
    - (A) Switch `--mode=lifecycle` default from degraded Assert F to true Assert F via the helper. Cleanest single-mode surface. Risk: any observed lifecycle-mode flake could NOW be attributed to the pause/resume machinery, not the engine.
    - (B) Add `--mode=lifecycle-true-assert-f` as opt-in. Two modes to maintain. Lower blast radius. Architect ratifies switch to default after N green runs.
    - **My recommendation: (B), then (A) once N green.**
  - **Retry semantics on onPaused throw?** Currently the rig helper kills the harness and rejects the promise. Alternative: retry the onPaused callback up to K times before killing. For task #52 v1, prefer fail-fast (simpler).

### P6 — Adversarial self-review (cross-cut across today's diffs)
- Status: DONE (findings docketed; zero must-fix findings surfaced)
- Files: (this worklog entry)
- Method: for each pair of today's diffs, ask "does one break the other's assumption?" Also apply hostile INS-16 lens: "could any of this be a false green?"
- Findings:
  - **F27b-2 (`c2f2ac91`) + F28 (`f8bd1eeb`) interaction — CLEAN.** F27b-2 fixes the ENGINE's post-bootstrap cursor gap so a fresh-lobby full-replay no longer re-applies seq 1 draft_started via NOTIFY. F28's client-side deriveDraftState handles duplicate draft_started idempotently (outer seq guard) EVEN IF a duplicate arrives (pre-F27b-2 wire behavior). Post-F27b-2 deploy, the client-side idempotency becomes DEFENSIVE (rare-race territory); pre-deploy, it's LOAD-BEARING against the F27b-2 wire duplicate. Either way, F28 correctness holds. **Note:** F28's `acceptance 1` test (apply draft_started twice → identical state) exercises the pre-F27b-2 wire behavior, so the test regression-locks the belt-and-suspenders correctness for the pre-deploy window and beyond.
  - **F27b-2 (`c2f2ac91`) + task #53 harness fix (`8edbf002`) interaction — CLEAN + additive.** F27b-2 reduces duplicate seq-1 wire broadcasts to zero post-deploy (cursor blocks re-fetch). Task #53 filters harness cursor updates to pick_submitted only. Post-both-deploys, the N-1 ordering-violation flag (from architect's STEP 5' green) shouldn't fire from EITHER cause: (a) draft_completed racing sample-record is filtered out by task #53; (b) duplicate draft_started via NOTIFY is stopped by F27b-2. **Two independent fixes for a defense-in-depth false-positive class.** Confirmed by tracing: post-both-deploys, next harness run should show `ordering-violations: 0`. Docketed as an expected-outcome for the next Garrett-exec STEP 5' rerun.
  - **N-2 migration (`2c9b59b8`) + F28 client (`f8bd1eeb`) interaction — CLEAN.** F28 client's deriveDraftState reads `draftStatus` only (not `draft_state`). N-2 writes `draft_state='completed'` on the DB row at completion but does NOT touch `draft_status` OR any wire event payload OR any `DraftSnapshot` field. Client sees no difference post-N-2 deploy. **N-2 is a DB-column-consistency fix; F28 is a wire-event-handler fix; zero-overlap surfaces.** Grep-verified: `draft_state` is not mentioned in `apps/web/src/lib/draftClient/deriveDraftState.ts`, `packages/shared/src/types/draftWire.ts`, or `apps/web/src/lib/draftClient/reduce.ts`.
  - **P3 pause-after (`7469f215`) + F27b-2 (`c2f2ac91`) interaction — CLEAN.** Pause-after is a rig-only feature; doesn't touch engine or client. F27b-2 is engine-only. No overlap.
  - **F28 default clause (unknown-kinds) risk:** the default clause silently ignores wire kinds not enumerated in the union. If a future engine emits `draft_paused` (currently in the union but no reducer case), the default fires — status/rosters unchanged. This is CORRECT for snake/linear (paused status is transient; UI can read `snapshot.stateSnapshot.pauseState`) but MASKS the absence of a real handler. Docketed as F28-L3 candidate for a future explicit draft_paused case IF client UI ever surfaces the paused state.
  - **N-2 apply-harness placeholder-reject robustness:** STEP 0's `IF v_capture_text LIKE '%PLACEHOLDER — REPLACE BEFORE APPLY%' THEN RAISE`. If Garrett populates but leaves the sentinel string in a stray comment, apply blocks. Safe default (fail-safe). But if Garrett accidentally writes the sentinel INTO the captured body (impossible — sentinel is unique), also blocks. Zero false-negative risk.
  - **Task #53's `frame?.parsed?.payload?.kind` deep-chain optional-chaining risk:** if the wire client's frame shape ever changes (e.g., `parsed.payload` renamed), the optional chain returns undefined → filter fails → NO cursor update, NO WARN, silently-degraded ordering-violation detection. Docket: add a lightweight harness test that constructs a fake frame with the current shape + asserts the filter triggers. Task #52 follow-up.
  - **INS-16 hostile-reviewer risk on THIS worklog:** could any of today's items be false greens? Reviewed with brief:
    - F27b-2: real unit tests, deterministic mocks, expectations match new fix's semantics. Not a false green.
    - F28: real unit tests, 8 new + 19 existing all pass. Acceptance cases match directive verbatim. Not a false green.
    - task #53: no test coverage BUT the fix is a one-line filter added to a specific chokepoint. Hostile risk is "does the filter mispredict"; the mispredict-mode is "false negative on real pick-ordering fault", which pre-fix state doesn't detect either (pre-fix flag triggered on lifecycle races, not on real pick faults per the two observed samples). Post-fix regression is bounded to "same detection scope as pre-fix minus lifecycle false-positives". Not a false green.
    - N-2: no live-DB tests. STEP 3 marker verification in apply-harness catches CATASTROPHIC failure (missing draft_state clause). Hostile risk: `LIKE '%draft_state  = ''completed''%'` (two spaces) is a specific literal-match; if Garrett or a rebase future-hand edits the whitespace, marker fails. Docket: relax to `LIKE '%draft_state%=%''completed''%'` OR use a regex. Ratify with architect.
    - P3 (partial): helper is unwired. Cannot false-green because it's not exercised. Docketed as-is.
  - **Cross-workstream awareness:** none of today's diffs touch 0F-OPS-N surfaces (no cron, no vacuum, no defense-cluster). All this-workstream.
- Open questions for architect: **none surfaced** by the review — findings are either (a) confirmed clean or (b) docketed for individual task follow-ups (F28-L3, task #52 test, N-2 marker regex).

### P7 — F27b-2 blast-radius sweep (client seq/resync consumers, READ-ONLY)
- Status: DONE (findings docketed; one benign UI-cosmetic finding)
- Files audited (all under `apps/web/src/lib/draftClient/` unless noted):
  - `apps/web/src/stores/draftClientStore.ts` — `applyEvent` (:260-312), `applyEvents` (:314-334), `setSnapshot`, `derivedState` propagation
  - `apps/web/src/lib/draftClient/deriveDraftState.ts` — outer seq-idempotency guard (:181), foldEvents contract
  - `apps/web/src/lib/draftClient/optimistic.ts` — `reconcileOnBroadcast` (correlationId-keyed), `reconcileOnResync`
  - `apps/web/src/lib/draftClient/runner.ts` — `lastSeenSeq` tracker (:519-522), `requestResyncForGap` (:309-343), watchdog + reconnect flow
  - `apps/web/src/lib/draftClient/reduce.ts` — wire message dispatcher
  - `packages/shared/src/types/draftWire.ts` — BufferedDraftEvent union + DraftServerMessage
- **Findings (file:line):**
  - **`draftClientStore.ts:294-310` — snapshot.recentEvents duplicate append (BENIGN UI-COSMETIC).** `applyEvent` unconditionally appends the event to `snapshot.recentEvents` (used by the Recent-events pane in DraftRoomV2). If F27b-2 wire duplicate of seq-1 draft_started arrives, `foldEvents` correctly idempotency-skips at deriveDraftState.ts:181, BUT the store still appends the duplicate to recentEvents. Result: UI pane shows two "Draft started" entries. Post-F27b-2 deploy this stops (engine cursor blocks re-fetch). Pre-deploy: cosmetic only, no state divergence. Docketed as F28-L4 candidate: add a seq-skip guard in `applyEvent` mirroring `foldEvents:181`.
  - **`runner.ts:519-522` — `lastSeenSeq` tracker uses `>` comparison, monotonic-max.** Duplicate seq-1: `1 > this.lastSeenSeq` fails after first apply → no update. Safe. No further blast-radius here.
  - **`optimistic.ts:81-91` — `reconcileOnBroadcast` keyed on `correlationId`, not seq.** draft_started's correlationId does not match any pending action correlationId (pending actions are user picks). Reconcile fires but is a no-op (map lookup miss). Safe. No blast-radius.
  - **`optimistic.ts:reconcileOnResync`** — same correlationId-keying; duplicate seq events with same correlationId would idempotently reconcile (Set.delete no-op on missing key). Safe.
  - **`runner.ts:309-343` — `requestResyncForGap` loop guard.** If foldEvents surfaces gaps (F27b-2 duplicate doesn't create gaps — same seq isn't a gap), runner requests resync via `sinceSeq=lastContiguousSeq`. Loop guard prevents repeat resync at same sinceSeq → close WS with code 1006. F27b-2 doesn't trigger this because duplicates aren't gaps. Safe.
  - **`reduce.ts` — wire message dispatcher.** Dispatches by `message.type` (snapshot/event/presence/error/resync_response). Does not inspect seq or event.kind at this layer. Safe.
- **No must-fix findings surfaced.** The one docketed item (F28-L4, snapshot.recentEvents dedup) is UI-cosmetic and self-heals post-F27b-2 deploy. Adding the guard is a hardening polish, not a correctness fix.
- **F27b-2 deploy expected behavior on client:**
  - Post-fix wire: zero duplicate seq-1 events → zero F28-L4 cosmetic flicker.
  - Post-fix rig: `ordering-violations: 0` (from task #53 harness fix) + no draft_started_apply.skipped_stale_status WARN (from F27b-2 engine fix).
- Open questions for architect: **none** — findings confirm client-side is already tolerant of duplicates via seq-based idempotency at the correctness layer. UI-cosmetic F28-L4 docketed for post-close polish.


# QA pass 1 (Zach, 2026-09-09): what shipped, what is left

Source: Zach's voice-memo to-do list. Fixed on `fix/qa-pass-1` unless noted.

## Fixed in code

- **App freezes on player → Trade.** Root cause: the card's Trade `Link` closed the dialog and changed route in one tick; Radix's outside-pointer lock could stay on `<body>`. Close first, navigate after the close animation (`PlayerStatsModal`), plus `RouteUnlock` in `App.tsx` lifts a stranded lock after any navigation.
- **Dead-end demo Trade Analyzer** for a signed-in manager with no league: real empty state with Create / Join.
- **League settings page title hidden / layout busted**, **chip strips scroll vertically**, **Scores date strip re-centres and cannot scroll back**: all one cause. `scrollIntoView` on a chip walks every ancestor. New `pressbox/scrollStrip.ts` moves only the strip's `scrollLeft`; Scores strip is now anchored on today, three weeks either way (`buildScrollableDateStrip`). Strips get `overflow-y-hidden overscroll-x-contain`.
- **Stormy overflows when the keyboard opens**: `useVisualViewport` sizes the chat layer to the visible viewport and pins the page scroll while the keyboard is up.
- **Status bar over the Leagues header**: `PressBoxHome` was the one Press Box screen without the safe-area top inset.
- **Translucent sticky bars** (Scores strip, draft room header) are solid now.
- **Citrus Game Day tiles** align: title at top, fixed two-line blurb box.
- **Player names tappable in Scores** (strip pills and detail rows link to `/players/:id`).
- **Players**: more spacing, 48px search field with 16px type, Leaders from a league keeps the league name and a Back button (`/players?league=…`).
- **Player card**: real icons for Watch and Share (the "box next to the star" was a literal `▢` glyph), 44px targets, bottom safe-area padding, tiles relabelled `PROJECTION` and `LAST 7 DAYS`; Career Arc caption says `past seasons`; dashboard bottom link clears the tab bar.
- **Stormy bar** padding; phone chat sheet slides in from the bottom.
- **Matchup / Team empty states** say what each screen will hold after the draft, distinct copy.
- **News** search placeholder says it searches any player or team (the search already did).

## Needs a screenshot or a device before touching

- Overview section on the player dashboard "very crowded" (rink + verdict composition).
- "Add top and bottom padding across pages", "normalize styles and UX patterns app-wide" (needs the screenshots to pick targets).
- Scores header collapsing oddly on scroll.
- Navigation to/from the dashboard "jerky" (needs a device trace; likely route transition + lazy chunk).

## Product decisions, not bugs

- **Draft time under Draft.** The V2 lobby has no schedule; `scheduled_draft_time` exists in the DB and League HQ displays it. Recommend a commissioner-only date/time on the League HQ draft tile writing `leagueApi.updateDraftSettings`, no engine change. Zach to confirm it is wanted.
- **Plain-language framing for advanced metrics** and **AI summary diversity**: pipeline prompt work (Draft Kit blurbs are DB rows), not app code.
- **Celebrini xG numbers**: both figures are last season's (goals vs expected); the caption now says so. A projection is a different number and lives in the tiles.

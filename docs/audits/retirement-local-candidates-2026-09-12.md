# Bounded local retirement — 12 September 2026

Compared deployed API commit label `6aef8ab7d5f4657245284377b53acbc915dc6d82` with integrated local `6d97cc0ab5e9cd21162e8bf0d876515c22408fc4` in `/Users/gstorms/.codex/worktrees/4304/citrus`. The deployed label is attributed to [scheduler evidence](/Users/gstorms/.codex/worktrees/8265/citrus/docs/audits/scheduler-execution-proof-2026-09-12.md:37), not proof that external Python jobs run that revision. No host census or deployment was performed.

## Completed local removal

With primary's coordinated authorization, removed exactly three function definitions from [calculate_daily_projections.py](/Users/gstorms/.codex/worktrees/8265/citrus/data-pipeline/projections/calculate_daily_projections.py:2416): `load_physical_projection` (original lines 2416–2484; 69 lines), `save_physical_projection` (2487–2564; 78 lines), and `recalculate_fantasy_points_for_league` (2622–2645; 24 lines). Bounds refer to HEAD before these deletions and match both compared revisions. The recalculation placeholder's entire executable body was `return 0`: it did not query, update, or implement the league recalculation described in its docstring. The containing active module is retained. These are reversible local API-symbol removals, not a claim of deployed compatibility with unknown external imports.

Caller proof: `git grep` over all tracked non-Markdown files at **both** revisions found only the recalculation function definition; cache helper hits were definitions and the save helper's own error attributes. The remaining loader mention in a historical migration is a comment, not a call. There are no checked-in consumers to migrate and no implemented output to reproduce. The module bytes were identical between the two revisions. The real [nightly imports:77](/Users/gstorms/.codex/worktrees/4304/citrus/data-pipeline/projections/nightly_projection_batch.py:77) and [daily imports:67](/Users/gstorms/.codex/worktrees/4304/citrus/data-pipeline/projections/run_daily_projections.py:67) import `calculate_daily_projection` and supporting symbols, not the removed placeholder. The retained orchestration [3119](/Users/gstorms/.codex/worktrees/4304/citrus/data-pipeline/projections/calculate_daily_projections.py:3119) directly calculates physical output and [3149](/Users/gstorms/.codex/worktrees/4304/citrus/data-pipeline/projections/calculate_daily_projections.py:3149) calculates fantasy points; goalie scoring calls the same scorer at [2005](/Users/gstorms/.codex/worktrees/4304/citrus/data-pipeline/projections/calculate_daily_projections.py:2005). Shared league projection scoring constructs `ScoringCalculator` at [309](/Users/gstorms/.codex/worktrees/4304/citrus/packages/shared/src/leagueProjection/index.ts:309). These retained paths substantiate that the removed function was not the checked-in scoring implementation; no scoring behavior was changed.

Verification performed: before writing, parsed the original module, removed exactly these three function nodes from the HEAD AST, and compared its AST without source positions to the resulting file's AST. They match exactly. The resulting module compiles. Diff is exactly 171 deleted lines; scoped code/workflow/ops searches have no remaining references to the three removed symbols; `git diff --check` passed. The focused runtime test result is recorded below.

Rollback: restore the three removed definitions from the parent revision or revert the dedicated retirement hunk/commit. Do not restore the entire file over other owners' changes. No database restore, scheduler change or artifact rebuild is involved.

## Candidate disposition — completed cache helpers and retained boundaries

| Candidate | Exact dependency proof at both revisions | Replacement and disposition |
| --- | --- | --- |
| `load_physical_projection` / `save_physical_projection` | Definitions at [2416](/Users/gstorms/.codex/worktrees/4304/citrus/data-pipeline/projections/calculate_daily_projections.py:2416)–2484 and [2487](/Users/gstorms/.codex/worktrees/4304/citrus/data-pipeline/projections/calculate_daily_projections.py:2487)–2564. Only other code hits are save's own error-count attributes. A historical migration comment names the loader; it is not a caller. | Main orchestration directly invokes physical calculation; no cache read/write call occurs in that path. **Completed locally** after primary supplied the read-only live dependency proof: 1,461 January cache rows, no database-function, incoming-FK, custom-trigger or publication dependencies. This is attributed evidence, not a database inspection independently performed by this reviewer. The table remains intact; quarantine is a separately gated operation. Kept `CACHE_VERSION`, migrations and containing module unchanged to avoid collateral edits. No replacement cache operation is required by the checked-in main orchestration, which already computes directly. |
| `persist_vopa_audit` | Sole symbol hit is definition [2999](/Users/gstorms/.codex/worktrees/4304/citrus/data-pipeline/projections/calculate_daily_projections.py:2999)–3083. It does contain real `player_talent_metrics` upserts if explicitly invoked. | No checked-in audit-persistence caller or replacement is established. Local function removal could retire an uncalled API, but would remove an externally callable writer. **Retain** for separately coordinated proof; do not delete its positional-statistic dependencies, which other functions use. |
| Progress wrapper | [28](/Users/gstorms/.codex/worktrees/8265/citrus/scripts/utilities/run_projections_with_progress.py:28) executes a working-directory-relative daily runner for hardcoded January 5, 2026. Reference search found only historical classifier metadata. Unchanged across compared revisions. | **Retain** pending explicit operator-path replacement. Unlike the placeholder, this executable can write when launched from a suitable directory, and executes at import time. No validated replacement operator command was established. Do not import/run as a test. |
| Week wrapper | [19](/Users/gstorms/.codex/worktrees/8265/citrus/scripts/utilities/run_week_projections.py:19) defaults to December 22–28, 2025; [32](/Users/gstorms/.codex/worktrees/8265/citrus/scripts/utilities/run_week_projections.py:32) hardcodes season 2025 and a relative runner. Hits elsewhere are classifier metadata. Unchanged across compared revisions. | **Retain** for the same operator migration boundary. This evidence supports historical labeling, not silently rewriting date/season or disabling an external job. |

Search expression: `load_physical_projection|save_physical_projection|recalculate_fantasy_points_for_league|persist_vopa_audit|run_projections_with_progress|run_week_projections`, run with `git grep -n -E` at each exact revision and `-- ':!*.md'`. This covers checked-in workflows, ops and executable source; migration text was inspected and classified as commentary. It does not establish external or dynamically constructed imports. AST-only four-function deletion was also simulated in memory and compiled at both revisions; the three authorized functions were removed from disk. `persist_vopa_audit` was retained byte-for-byte within the unchanged remaining AST.

## Focused test plan and status

Run the existing importing/calculation suites against the edited 8265 checkout: `test_projection_logic.py`, `test_goalie_start_probability.py`, `test_skater_calibration.py`, and `test_nightly_ros_rebuild.py`, excluding network tests. The nightly suite uses a fake database and stubbed per-game worker and checks RPC routing, daily persistence and failure handling. This verifies retained import and calculation paths rather than adding a test that merely asserts a function is absent. Use placeholder credentials so no production credentials are needed.

Initial attempt: bundled runtime and system Python have no pytest installed; no tests ran in that attempt. The selected suites use pytest functions/classes and fixtures, so unittest discovery is not an equivalent runner. No existing pytest runtime was identified and no dependencies were installed for these narrow removals. Focused runtime tests remain **blocked by missing pytest**; do not interpret AST compilation as a test-suite pass.

No source-guide dataset, generated PDF, UI, migration, job, live cache, or production setting was modified by this bounded retirement.

# 76 is a MEASUREMENT of this tree on 2026-08-26, not a target inherited from
# anywhere. It was counted by running `npx tsc --noEmit -p tsconfig.app.json`
# on master after the citrus-property web merge, and it is the number the
# ratchet in .github/workflows/ci.yml compares against.
#
# The branch this merge came from measured 54, on a tree that also carried its
# armchair-gm token migration and its own versions of files master has since
# rewritten. This tree cannot reach 54 by resolving conflicts better: 46 of the
# 76 live in MatchupService.ts (29) and DraftService.ts (17), and DraftService
# is not in that branch's diff at all.
#
# Progress so far: 151 -> 91 -> 76 -> 62.
#
# 76 -> 62 (2026-09-03): typing the draft API client. `apiClient.get`/`post`
# declare `<T = unknown>`, so every call in api/draft.ts that omitted the type
# parameter made `response.data` unknown and every field read off it a compile
# error. 14 of the 14 removed here were that one omission. The types were
# transcribed from the route handlers and, where the wire shape was in doubt,
# checked against the production catalog -- which is how the autopick column
# bug (picked_player_id, not player_id) surfaced.
#
# 62 -> 0 (2026-09-03, second pass). Garrett's call before the TestFlight
# archive: "I'd rather have perfect product." Four agents took the four
# clusters (MatchupService 29, services 10, pages/components 12, tests 11),
# then four more adversarially reviewed every change against the producers
# and the routes. No `any`, no `@ts-ignore`, no `@ts-expect-error` was added;
# the two `as unknown as` casts that remain are at Supabase client
# boundaries where the generated types are stale, each with a comment.
#
# Three latent bugs surfaced and were fixed on the way:
#   * MatchupService.ts organizeRosterIntoLineup sorted on `b.points`, which
#     HockeyPlayer has never had (points live at stats.points). The
#     comparator was 0 - 0 for every pair, so default lineups inherited
#     whatever order the player cache happened to be in. Now sorts.
#   * DraftRoomV2.tsx passed an Error object into a prop typed string that
#     is rendered bare into JSX: "Objects are not valid as a React child"
#     on any offline players-load failure.
#   * DraftRoom.tsx restored a string enum through parseInt, so the
#     sessionStorage phase restore could never fire (branch is unreachable
#     today for other reasons too).
#
# THE RATCHET IS NOW A FLOOR. At 0 there is no gap left to lower, so any
# rise is a real regression introduced by the change in front of you. Fix
# it there; do not raise this number. If a new dependency or a generated
# type forces a temporary rise, say so in this file with the count and the
# date, the way every line above does.
0

---
# Copy this file, rename it, delete these comments. Filenames become row ids,
# so pick one and keep it: renaming a file makes a NEW row and orphans the old.
# Lowercase, hyphens, no spaces. e.g. mcdavid-volume-floor.md

player: Connor McDavid       # exact name from player_directory. Omit for a
                             # non-player blurb (kind: tier / strategy).
                             # Use player_id: 8478402 instead if the name is
                             # ambiguous — the loader will tell you when it is.
kind: player                 # player | roster_change | tier | strategy
tier: kit                    # free = public teaser | kit | suite
title: A short headline, not a sentence
author: Garrett Storms
author_role: Citrus founder  # optional. How a reader tells you from a guest.

# Only for sourced writing. Both or neither -- the schema refuses one alone.
source_name:
source_url:

publish: false               # false = draft, invisible to everyone.
                             # Flip to true when it is ready to be read.
---

The body starts here. Everything under the closing fence is the blurb.

Write it the way you would say it out loud. Blank lines separate paragraphs
and are preserved. Cite the Citrus number when you lean on one -- the cards
beside this text already show the percentile, so the prose earns its space by
saying what the number MEANS for a draft, not by repeating it.

# Canonical source and league-specific scoring cutover

**Decision for review:** approve the ordered production release below, or approve backend/web first and explicitly accept the documented Build 18 limitations until Build 19 is installed. Recommended default: schema and scoring → API/web → corrected native client → canonical activation. Keep cache quarantine separate until its external-caller evidence is accepted. Nothing here has been executed against production.

The reviewed numerical source is `tmp/projection-audit/source-reconciliation/lineup-candidate.json`, revision `6a71c8db63eb6ee43798f8ab963652b89c103d2f9dae718f3d51b0abb97dd117`, file SHA-256 `d2ee0bc9d55231b0541ba10f3c114c253f57c252923f41ad834967a595e111fa`. All 20 lineup gaps are resolved as documented projected scenarios. The file remains DRAFT. The release must use this source's exact numerical records; review metadata produces a new approved source revision, never a reused test-copy revision.

## Ordered execution

| Step | Exact action | Expected behavior / stop condition |
|---|---|---|
| 0. Pin and preflight | Record release Git SHA, current API image/revision/traffic and current web release. Repeat the draft-freeze gate, production migration list, source hash, directory/schedule and scoring-drift preflight. Pin prior function definitions and scoring drift evidence. Pin current active source revision (expected absent on first cutover). Capture the actual recovery bundle later, inside the activation transaction under its locks; an early preflight export cannot substitute for it. | Stop on a live/near draft, changed source, unexpected active run, unreviewed schema drift, or missing restorable snapshot. Do not infer that an old read remains current. |
| 1. Canonical schema | Apply the first three exact files below through Supabase `apply_migration`, in order, to project `iezwazccqqrhrjupxzvf`. | Empty published views exist; no active source means both existing cron entrypoints retain legacy behavior. No forecasts switch yet. New API readers must not precede this step. |
| 2. Scoring transaction | Capture affected league IDs before repair; execute the fourth exact migration and the scoped rescore companion in the same reviewed migration transaction. Save its complete composed SQL and fingerprint. | Saved settings JSON remains identical; derived multipliers and affected started-matchup totals/point breakdowns agree. Any failed matchup or verification aborts the transaction. Official NHL facts and frozen roster records are not rewritten. |
| 2b. Output write boundary | Apply `20260912073540_canonical_projection_write_boundary.sql` before any source activation. | The existing canonical materializer owns active ROS/future-daily writes through a private non-login database role. Direct obsolete writes cannot retain a valid stamp while replacing counts. Standard no-active-source fallback and historical daily repairs remain possible; stale-snapshot noncanonical writes receive an explicit retry-at-read-committed error. |
| 3. API then web | Merge the reviewed release commit only after steps 1–2b pass. The existing `Production Deploy` workflow builds shared/server/web, deploys API, then web (`deploy` depends on `deploy-api`). Verify the actual serving image/commit and `/api/health`; verify migrated empty-publication fallback and two materially different league rules with authorized sessions. | Existing request envelopes/raw field names remain. No canonical source is active. A missing published view would make the new dashboard withhold forecasts; this is why schema precedes deployment. Stop on API fingerprint mismatch or authorization/readiness failure. |
| 4. Native | Build and validate Build 19 through the existing native build/sync/archive path, then use the separately approved distribution action. Confirm the corrected Matchup/card/roster/draft behavior on the intended device build. | Hosting/API deployment does not replace Build 18's bundled JavaScript. If activation is requested earlier, explicitly accept the residual Build 18 behavior below; there is no new version gate in this change. |
| 5. Publish 2026 source | After source approval, create the approved source payload with unchanged players/teams/schedule/rates/workloads, recorded review evidence, only the explicit review blocker removed, and recomputed canonical SHA-256. Stage and validate using the SQL below; perform first activation with the first-publication runner below, which verifies absence under lock and saves the fresh recovery bundle before commit. | Atomic active pointer + ROS/daily materialization. Expect 674 projected players and 56,616 daily rows only if the same full calendar is still applicable; otherwise revalidate the current calendar, never force old counts. Nonprojected profiles stay visible without forecasts. |
| 6. Source-to-screen acceptance | On fresh corrected-client sessions, compare source/runtime stamps, player category contributions and league-specific totals across cards, draft, roster, free agents, Matchup and Scores. Verify actual zero/negative corrections, lineup eligibility, unavailable categories, and a background refresh. Observe the next existing ROS/daily scheduled cycle and health. | Matchup actuals remain measured facts, projections remain expected counts, and each league keeps independent rules. Do not call this complete based on only a health endpoint or a warm Build 18 cache. |
| 7. Optional cache quarantine | After release acceptance plus actual supported external-client checks, repeat `../projection-retirement/preflight-cache.sql`, retain a fresh matching backup, then execute the already tested rename and inverse-ready procedure. | Only `projection_cache` is renamed. Preserve data/OID/ACL/RLS/FKs. Do not drop tables, disable cron 31/34, remove model inputs or remove the called legacy fallback functions. No final drop is proposed. |

Exact migration order (repository-relative paths):

1. `supabase/migrations/20260912073140_canonical_projection_runs.sql`
2. `supabase/migrations/20260912073240_canonical_projection_materialization.sql`
3. `supabase/migrations/20260912073340_canonical_projection_refresh.sql`
4. `supabase/migrations/20260912073440_scoring_rules_replace_removed_keys.sql`
5. `supabase/migrations/20260912073540_canonical_projection_write_boundary.sql`

The executable scoring companions are [capture.sql](../../release/scoring-rescore/capture.sql) and [rescore.sql](../../release/scoring-rescore/rescore.sql). The exact composed transaction is prepared at `outputs/projection-reconciliation-20260912/scoring-cutover-transaction.sql`; its adjacent manifest pins all three input hashes. Execute it as one `BEGIN ISOLATION LEVEL REPEATABLE READ` session/transaction, not three connector calls. The [scoring companion README](../../release/scoring-rescore/README.md) names the temporary snapshot/result tables and calibration checks. Temporary scoring snapshots support pre-commit rollback. After commit, app rollback deliberately retains corrected rules/scores, as verified against the pinned prior API contracts in the companion README; stale preflight exports are not a post-commit recovery mechanism. The [actual-function full-schema rehearsal](../../../docs/verification/scoring-rescore-full-schema-20260912.json) passed default, zero, negative and category scoring, source preservation, line calibration and rollback of tables/functions.

For each migration, the connector call is `apply_migration(project_id="iezwazccqqrhrjupxzvf", name=<file name without numeric prefix>, query=<reviewed exact SQL>)`. Migration 4 composes the capture, unchanged migration and rescore SQL into one transaction so readers do not see repaired rules paired with stale stored totals. Do not use a bare `db push`: the repository's `scripts/db-push.mjs` intentionally refuses implicit production targeting. Do not use `ops/cloudrun/deploy.sh latest` for this cutover; the reviewed production workflow pins the release commit and retains its gates/configuration.

Approved payload staging and activation use bound values, not invented IDs:

```sql
-- Read-only immediately before activation; first deployment expects zero rows.
SELECT run_id, revision FROM public.canonical_published_runs WHERE season = 2026;
-- Supply the approved, locally hash-verified JSON; save the returned UUID.
SELECT public.canonical_stage_projection_run(:'approved_payload'::jsonb);
SELECT public.canonical_validate_projection_run(:'staged_run_id'::uuid);
-- Proceed only if valid=true. Run first activation through the operator
-- runner below so the locked backup is durable before COMMIT.
SELECT run_id, revision, source_run_id, source_revision,
       last_refresh_status, last_refresh_error
FROM public.canonical_published_runs WHERE season = 2026;
```

For the first publication, use [first-publication/run.py](first-publication/run.py) and its [operator instructions](first-publication/README.md) with an explicit psql service, returned run ID, approved revision, season and a new private backup path. The runner holds the season advisory lock and active/ROS/daily table locks while capturing database/date/schema, complete ROS and current-season future daily rows, and before/after fingerprints. It creates and fsyncs the SHA-256-bound external bundle before activation COMMIT. Recovery uses the same file in a separate transaction, verifies exact active CAS plus date/schema/output hashes, clears only the pointer, restores the captured scope and retains immutable history. Any intervening protected write or calendar/schema change refuses recovery. See the committed local [rehearsal evidence](../../../docs/verification/first-publication-recovery-20260912.json). This runner is deliberately first-publication-only; any preexisting active season stops it because ROS is a single global cache.

The approved payload must not be the rehearsal's `ac6291…` test copy. That copy contains local test-review metadata and was rolled back. The source review can approve a projected scenario without asserting a confirmed opening-night lineup. Roles do not add a hidden multiplier or retrain existing rates.

## Compatibility and rollback

[Actual Build 18 archive evidence](../../../docs/audits/build18-cutover-compatibility-20260912.md) establishes that its ROS/daily/dashboard requests and raw fields remain readable. New unconditional counts are not proven double-weighted by those archived branches. However, Build 18 still counts returned rows as appearances in some views, uses stored default points/fallbacks in others, and can retain a dashboard snapshot for the session. It cannot gain the new league-readiness, refresh or missing/zero handling from a server deploy. A fresh launch is necessary even to inspect newly fetched values. Corrected web and Build 19 are required for the full behavior; unsupported compatibility forecast categories such as plus/minus remain unavailable on corrected readers.

- **Before activation:** a failed migration transaction rolls back; installed empty canonical schema can remain while the prior API/web versions are restored. Legacy wrappers keep their old path. Scoring capture/repair/rescore is a single transaction; failure must not commit partial totals.
- **Failed activation or refresh:** the tested database transaction preserves the prior complete run/outputs. Failure health is explicit. No operator needs to delete immutable run history to recover.
- **Application rollback:** route Cloud Run traffic back to the freshly pinned prior revision and restore the pinned prior Hosting release. Observed baseline was `citrus-api-00310-q6p` / `6aef8ab7d5f4657245284377b53acbc915dc6d82`, but re-read before release. The exact traffic command is `gcloud run services update-traffic citrus-api --project=citrus-fantasy-prod --region=us-central1 --to-revisions="$CITRUS_PRIOR_API_REVISION=100"`. This operational command has not been run against production here. Retain the repaired scoring trigger, normalized rules, corrected totals and lines: the prior API already uses the same effective-rule scoring RPCs and unchanged result shapes. See the scoring companion README for the compatibility evidence and separate data-incident boundary.
- **After a committed first source switch:** execute the first-publication runner's `restore` operation with the exact saved activation bundle. The separately committed local rehearsal restored preactivation ROS/future-daily fingerprints and retained history. A stale revision, changed outputs, database/schema mismatch, changed database date or corrupt bundle refuses restoration. Retain the new write guard and numeric columns. If any refusal occurs, preserve the current state and diagnose it; do not bypass the checks or replay an early backup. Later canonical-to-canonical switches require their own valid calendar and expected-active CAS.
- **Cache quarantine:** inverse rename is tested on the actual 1,461-row backup and preserves the same object/data/security. Restore immediately on a demonstrated caller failure. Keep the backup and agree an observation period before any later deletion proposal.

Local evidence: full-schema model/refresh/RLS/failure checks; actual-source test-copy activation of 674 ROS / 56,616 daily rows with all 12 exported categories, expected workload, independent default scoring and revision equality; complete rollback fingerprints; actual cache restore/rename/inverse. These prove the named local operations, not production traffic rollback, installed-device behavior or every external host caller.

The database boundary protects active outputs even if an obsolete external invocation still runs. `nightly_projection_batch.py` and `run_daily_projections.py` catch/log projection write failures; neither invokes raw NHL-stat ingestion in this chain. The nightly path still calls the ROS RPC and proceeds to derived difficulty when that succeeds. Optional directory/talent updates in the manual daily path run before projection writes. An obsolete caller may waste work and log failures until its actual invocation is retired, but it cannot silently replace authoritative active rows. Do not disable guessed jobs; cron 31/34 remain the supported refresh entrypoints. Cache quarantine still requires its separate external-reader check.

## Exact approval boundary

The pending production actions are: the five migration applications (including scoped scoring recalculation and the output write boundary), release merge/deploy to API and Hosting, approved source staging/activation, any Build 19 distribution action, and—separately—the one-table cache rename. The current checkpoint was requested by the coordinating task: finish the narrow guard, rehearsal and concrete release plan before selecting a production action. It is not an explicit user prohibition or an approval requirement inferred from a skill. The next coordination decision is which release phase and client timing to execute; this message's authorized work does not execute those production mutations. Existing production workflow draft-freeze/environment gates remain in force, and native distribution remains a distinct release action. No override is proposed, and the old engine-specific F26/F27 protocol is not being applied to this API release.


## Local native validation record

The unsigned Build 19 candidate was built separately at client base `0a9fb633ad55350060d2bc6ab323c465bf20ff7c`; only both Xcode build numbers changed 18→19. [Validation receipt](/Users/gstorms/.codex/worktrees/native19-review/citrus/outputs/native-build19/validation.json) and [candidate archive](/Users/gstorms/.codex/worktrees/native19-review/citrus/outputs/native-build19/Citrus-Build19-unsigned.xcarchive). Native assertions/sync, shared typecheck, 51 tests across nine client files, Release simulator compilation and unsigned device archive passed; all 215 dist files match both bundles. Guard/scoring/recovery follow-ups change only backend SQL, operator tooling and evidence, so this client base remains the exact tested bundle input. Source is fetched at runtime. Signing/provisioning, launch, authorized two-league screen checks, OAuth/push/background refresh and distribution remain unperformed. Build 18 was untouched.
